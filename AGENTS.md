# FeeArb Agent Notes

Purpose
Maintain a durable, concise project brain-dump so new sessions can resume quickly.
Update this file after each meaningful change or decision.

Project Instructions
- Canonical user/operator instructions live in `instructions/` as text Markdown files.
- Add new instructions there, or update the existing topic file instead of creating duplicates elsewhere.
- Use clear task-based filenames and keep `instructions/00_README_ИНСТРУКЦИИ.md` as the index.
- `docs/` remains suitable for technical plans, subsystem history, and internal design notes.

Project Overview
- FeeArb is an async research + execution stack for cross-exchange funding arbitrage.
- Core layers: pipeline (data), orchestrator (decisions), execution (orders/positions), webapp (FastAPI + WS UI).
- Default active venues in docs are Bybit and MEXC; adapters for more exist but are not all active.
- Execution layer is mostly simulated today; manual trading is the active "live" surface.
- Position-driven exit/roll decisions should use the positions-market cache (fresh, separate from candidates refresh).

Live Trading Focus (Manual Enter/Exit/Roll)
- Manual UI: `webapp/templates/manual.html` + `webapp/static/manual.js`.
- Manual tests UI: `webapp/templates/manual_tests.html` + `webapp/static/manual_tests.js`.
- API endpoints in `webapp/app.py`: `/api/manual/enter`, `/api/manual/exit`, `/api/manual/roll`, `/api/manual/analyze`.
- Service glue in `webapp/services.py`: delegates to `execution/manual.py` (ManualTradeManager).
- Orderbook sources: websocket feed via `webapp/market_data.py` (MarketDataBus.get_orderbook), fallback to REST via ccxt.
- All live trading orders are isolated margin at 3x leverage.
- Live trade rules doc: `docs/manual_trade_rules.md` (WS-first positions, chunk reconciliation, fallback thresholds).

Dry Run Logic (ManualTradeManager in `execution/manual.py`)
- Dry run is a "plan-only" path; it builds a full execution plan and returns diagnostics, but never places orders.
- Entry point: `_handle_pair(...)`:
  - Calls `_build_plan(...)`.
  - If `payload.dry_run` or `plan.errors`, returns the plan immediately.
  - Otherwise executes the plan via `_execute_plan(...)` (or smart/fast enter/exit).
- `_build_plan(...)` does the heavy lifting:
  - Validates action, symbol, exchanges, qty/notional; can infer qty from positions for exit/roll.
  - Resolves qty from notional via ticker if needed.
  - For each leg:
    - Ensures exchange client (requires credentials unless ccxt supports public-only for that exchange).
    - Resolves ccxt symbol.
    - Fetches orderbook: prefers websocket (`orderbook_provider.get_orderbook`), else REST.
    - Extracts market constraints (min qty, min notional, amount step, contract size).
    - Applies optional min-notional overrides and buffer.
    - Scales orderbook for contract size, computes stats and liquidity.
    - Estimates fill, slippage, and max qty under slippage limit.
    - Adds errors if insufficient liquidity when `use_orderbook_check` is true.
  - Calculates funding meta, spread %, recommended qty/notional, and chunk sizing hints.
  - Optionally collects constraints for multiple exchanges if `constraints_exchanges` is provided.
  - Returns plan payload with `errors`, `warnings`, `stats`, `slippage`, `market_constraints`, etc.
- Key outcome: dry run is a comprehensive preflight check + sizing advisor, not a simulation of execution steps.

Notes / Observations on Dry Run
- It currently collects constraints for both legs and optionally for many exchanges.
  - This can be slow, especially if you only trade on two exchanges.
  - It may also require credentials for ccxt clients even for read-only data.
- `constraints_exchanges` is injected in `webapp/services.py` for dry runs using enabled analysis exchanges.
  - This makes dry run "wide" by default, not just the chosen pair.
- Dry run depends on orderbook data:
  - If websocket live orderbook is enabled, it uses WS; otherwise REST.
  - Failures show as errors/warnings in the plan, not as execution attempts.

Manual Tests (Web UI)
- Used for step-by-step validation of exchange connectivity and order paths.
- Endpoints: `/api/manual/test/limit`, `/api/manual/test/market`, `/api/manual/test/cancel`.
- Test logic in `webapp/services.py` uses the same ccxt gateway to fetch symbol, constraints, and orderbook.

Gate WS Notes (Futures v4)
- Two signature modes:
  - Private subscriptions use `auth` with signature `channel=<channel>&event=<event>&time=<time>`.
  - Trading requests use `event: "api"` with signature `api\n<channel>\n<req_param>\n<timestamp>`.
- Must send `futures.login` (`event: api`) before `futures.order_place`/`futures.order_cancel`.
  - Login payload includes `api_key`, `signature`, `timestamp`, `req_id`, `request_param: ""`, `headers: {}`.
  - Use server time offset (`time_ms` or `header.response_time`) and retry once on timestamp errors.
- Subscriptions need a contract param: payload `[<contract>]` or `[<uid>, <contract>]`; use `!all` for all contracts.
- Validated flow: login -> subscribe (`orders`/`positions`/`usertrades`) -> order place/cancel -> WS updates (open/filled/canceled) + positions/usertrades.

Kucoin WS Notes (Classic Futures)
- Private token via `/api/v1/bullet-private`, then connect to returned `endpoint?token=...`.
- Orders stream: `/contractMarket/tradeOrders` (optionally `:SYMBOL`).
- Positions stream: `/contract/positionAll` or `/contract/position:SYMBOL`.
- Wallet stream: `/contractAccount/wallet`.
- Validation: REST limit orders (manual tests) trigger `symbolOrderChange` updates on `/contractMarket/tradeOrders:SYMBOL` (open -> match -> filled), plus wallet updates and `/contract/position:SYMBOL` position changes. Order events carry `marginMode: ISOLATED` and `positionSide: BOTH`.
- Classic docs do not list `tradeFills`; use order updates or REST for fills.

Open Questions / Decisions to Make
- Should dry run skip private-client requirements and use public-only data when possible?
- Confirm target exchanges for live trading to limit the slow path.
- Kucoin: REST order requests include leverage=3, but position updates reported `realLeverage: 1.0`; check if explicit leverage-setting API call is required.

Recent Changes
- Unified Live auto-strategies are implemented. `/strategies` creates one-step or sequential Auto Enter/Auto Exit ladders with spread plus optional `short funding - long funding` triggers, USDT/coin/position-percent sizing, per-step preflight, persisted fixed targets and residual retries, and a 1% completion-dust threshold. Only the first unfinished step is visible to the scheduler. A single auto-agent loop now processes legacy Spread/V1 exits, ladder exits/entries, then Grid; one manual execution slot is enforced globally, exits precede entries, and equal ladder priorities use trigger edge then waiting age. Default runtime is 120 seconds, slippage limit 8 bps, market fallback is off, and tier chunk caps are 750/500/250 USDT. Existing Spread/V1 rules now retain fixed target/remaining quantities after partial fills and use the same 1% dust completion rule. New Grid saves launch directly into restricted Live after dry-run validation instead of creating new Shadow rules. Dashboard shows live strategies, marks positions with linked exit ladders, and exposes the current Auto Agent action/source/step/stage/elapsed time. State/history are `state/auto_strategies.json` and `logs/auto_strategy_history.jsonl`; operator guide is `instructions/09_LIVE_АВТОСТРАТЕГИИ_ВХОДА_И_ВЫХОДА.md`.
- Auto-arbitrage Grid now supports an explicitly armed restricted Live mode for small-volume validation. The preview table describes each level as concrete entry/exit conditions and both-leg BUY/SELL operations, with estimated chunk/cumulative notional. A strategy can start from no position as level 0 (`waiting first entry`); after confirmed spread samples it delegates exactly one chunk to existing `smart-enter`/`smart-exit`, then advances the live level only after a fresh account-position reconciliation matches the target hedged quantity. Live activation requires typing `LIVE <rule_id>`, allows one Live rule, enforces server caps of 50 USDT per chunk and 100 USDT total notional per leg, blocks matching Auto Exit or another running execution, disables market fallback, and pauses on execution/reconciliation mismatch. Dashboard now lists planned Grid strategies separately from real exchange positions. Historical percentile-based auto-selection was deferred to the later optimization phase. Added `instructions/08_АВТОАРБИТРАЖ_GRID_LIVE_МАЛЫЙ_ОБЪЕМ.md`; targeted Grid tests pass (`13 passed`).
- Remote smoke after the Grid deployment confirmed the refreshed page over Tailscale Serve (`https://desktop-0tl9bsa.tail3830e2.ts.net:8443`, HTTP 200, static version `v2026-06-11-01`). `app.feearb.ru` remained unstable with Cloudflare `502/530` despite a healthy local origin and active tunnel; debug showed successful origin responses followed by edge control-stream disconnects. HTTP/2 vs QUIC, direct backend vs Caddy origin, and cloudflared 2026.6.0 vs official 2026.5.2 were tested without resolving it, then all tunnel settings and the 2026.6.0 binary were restored. Use Tailscale as the current operational path; Cloudflare troubleshooting is documented in `instructions/05_WEB_ИНТЕРФЕЙС_ЧЕРЕЗ_CLOUDFLARE_TUNNEL.md`.
- Android public remote access now has a token-protected Tailscale Funnel fallback at `https://desktop-0tl9bsa.tail3830e2.ts.net:10000/`. Cloudflare `app.feearb.ru` DNS and tunnel routing were valid, but both Windows and WSL Linux connectors reproduced a provider-path failure: QUIC sessions timed out after initial registration, while HTTP/2 control streams became blackholed with local TCP sockets still `Established`; Cloudflare then alternated `502` and `530`. `cloudflared` was upgraded to `2026.6.0` and left configured for HTTP/2, but is not considered reliable on the current route. The new Funnel route proxies through canonical `config/caddy/Caddyfile`; Caddy validates the existing `X-FeeArb-Token` before forwarding and stamps a private proxy marker, while backend middleware also recognizes that marker after the next safe backend restart. Validation returned `401` without a token and `200` with the existing Android token and balance payload. `scripts/windows/start_feearb_public.ps1` now reloads the canonical Caddy config with the local token and ensures both Funnel 443 and token-protected Funnel 10000. The existing private Tailscale `:8443` route remains unchanged.
- Auto-arbitrage grid stages 0-1 are now implemented as a Shadow-only web subsystem. `execution/auto_arb_grid.py` provides tested inclusive negative-spread levels, hysteresis-aware entry/exit targets, bounded auto-sizing, and one-level-per-cycle transitions. `DataService` persists rules in `state/auto_arb_rules.json`, logs transitions to `logs/auto_arb_history.jsonl`, derives consistently oriented executable spreads from `long ask/short bid` for entry and `long bid/short ask` for exit, runs both enter and exit dry runs for safe chunk sizing, and requires configurable consecutive confirmations before virtual transitions. FastAPI now exposes analyze/rule/pause/resume/delete/history endpoints and `/auto-arbitrage`; the page offers coin/USDT budgets, automatic level/exit-gap suggestions, preview tables, and live Shadow cards. No order-placement path exists in this subsystem. Added operator guide `instructions/07_АВТОАРБИТРАЖ_SHADOW_РЕЖИМ.md`; verified 57 targeted tests, Python compilation, route coverage, template smoke, and diff checks.
- Auto-arbitrage grid mode has been designed in `docs/modules/auto_arbitrage_grid.md`. The proposed controller maps executable entry/exit spreads to a target hedged inventory level, reconciles that target against fresh real positions, and delegates one atomic chunk at a time to the existing manual enter/exit engine. The design includes coin/USDT budgets, dry-run and historical "magic wand" sizing, separate entry/exit thresholds with fee/slippage hysteresis, generation-safe range reconfiguration, persistent rule/lot state, execution ownership priorities, shadow-first rollout, a dedicated web page, and a later Android `Grid` tab. No trading code or live state was changed.
- Permanent Cloudflare Tunnel setup is now created for `https://app.feearb.ru/`. The named tunnel is `feearb` with ID `b848b4d9-bb7c-47ff-858b-ef025d2d469f`, its CNAME was added in Cloudflare DNS, and `config/cloudflared/feearb.yml` routes the hostname to `127.0.0.1:8000` using HTTP/2. Cloudflared now runs as a real automatic Windows service under `LocalSystem`; config/credentials were copied to the system profile, the service `ImagePath` was corrected per Cloudflare's Windows instructions, restart testing succeeded, and the connector established live edge connections. `scripts/windows/install_cloudflare_tunnel_service.ps1` can reproduce this service setup, while the existing FeeArb startup task only ensures the service is running. The `.ru` registry already reports `FEEARB.RU` as `REGISTERED, DELEGATED, VERIFIED` on `moura.ns.cloudflare.com` / `sky.ns.cloudflare.com`, while the parent `.ru` authoritative servers still returned NXDOMAIN minutes after registration, so public HTTPS verification remains pending DNS publication. `state/remote_access_token.txt` is now gitignored. Android should use `https://app.feearb.ru/` plus the remote token after DNS resolves; Tailscale `:8443` remains the fallback.
- Cloudflare remote-access groundwork was implemented while preserving Tailscale as the working fallback. `cloudflared 2026.5.2` is installed, the backend now detects Cloudflare-originated HTTP requests by Cloudflare headers and requires a constant-time checked `X-FeeArb-Token`, and a 43-character token is generated/persisted locally in `state/remote_access_token.txt`; local and Tailscale requests remain unauthenticated and unchanged. Android Settings now persist a masked `Remote access token`, and OkHttp attaches it to all API requests when non-empty. Regression tests verify Cloudflare detection/token validation, a live local middleware check returned `401` without the token and `200` with it, Android `assembleDebug` passed, and `FeeArb-cloudflare-ready-2026-06-10.apk` was produced with SHA-256 `CAE6F5D1EB0B6A3F79B4AA51A66501490BA3F199500C0CCE31DC751B6DB1CD9C`. An account-less Quick Tunnel was tested but rejected for live use because its random `trycloudflare.com` route was unstable (`520/530`); permanent completion requires the operator to finish `cloudflared tunnel login` and provide a domain already managed in Cloudflare. Tailscale HTTPS `:8443` continued returning HTTP 200 throughout.
- Android Tailscale diagnostics now include a tailnet-only HTTP fallback for phones that cannot open the private HTTPS/MagicDNS route correctly. In addition to `https://desktop-0tl9bsa.tail3830e2.ts.net:8443/`, Tailscale Serve now exposes `http://desktop-0tl9bsa:8080/` to the tailnet and proxies it to `127.0.0.1:8000`; the Android app permits cleartext traffic for this private development/operations path. The phone `s26-ultra` was online in the same tailnet with a direct peer connection, PC-to-phone ping succeeded, and both HTTP and HTTPS Serve routes returned the mobile positions payload locally. The operator guide now documents the HTTP fallback plus Android checks for Tailscale DNS and conflicting VPN/Samsung Secure Wi-Fi settings.
- User/operator runbooks are now centralized under `instructions/` with a Russian index and clearly named guides for Android remote access through private Tailscale Serve, Android build/install/safe testing, Cursor/emulator operation, and the separate public Tailscale Funnel web path. The Android remote guide records the exact app setting `https://desktop-0tl9bsa.tail3830e2.ts.net:8443/`, phone-side Tailscale setup, PC prerequisites, diagnostics, and the warning not to confuse the tailnet-only `:8443` backend with the public port-443 Funnel. On 2026-06-10, `tailscale serve status` confirmed `:8443 -> 127.0.0.1:8000`, and the remote mobile positions endpoint returned HTTP 200. Future user/operator instructions should be added to or updated in `instructions/`, with `docs/` retained for plans and technical history.
- Auto-exit regression tests are now isolated from live operator state. During the HOME investigation, repeated `tests/test_project_settings.py` runs were found to instantiate `DataService` against production `state/auto_exit_rules.json` and `logs/auto_exit_history.jsonl` before individual tests replaced selected stores, which could remove or replace real checkboxes and also polluted the production history with fixture events. The test case now patches auto-exit, hedge-cluster, de-risk outcome, and history paths to its temporary directory in `setUp()` before any service is created. Isolation was verified by hashing `state/auto_exit_rules.json` and checking the production history byte length before/after the full target suite: both remained identical while all 70 tests passed. The pre-test operator rules for ARIA, BABY, BTC, EDEN, and LAB were restored; HOME remains deliberately off after its partial run.
- Multileg spread auto-exit trigger semantics were repaired after `HOMEUSDT` incorrectly launched with a configured `-3.5%` threshold while the three-leg basket was still around `-4.504%`. The trigger event showed the regression clearly: weighted overall spread was `-4.5043686%`, but the automatically selected reduction pair `bybit long / kucoin short` was only `-0.5248427%` gross (`-0.6748427%` net), and the scheduler compared the pair net value against the basket target. `webapp/services.py` now separates trigger scope explicitly: two-leg rules still require executable pair net spread to reach `target + edge buffer`, while `multileg|multileg` rules require weighted live overall basket spread to reach the operator target; selected-pair live books remain mandatory for safe execution but cannot substitute for the overall condition. Added HOME-shaped regression coverage for overall `-4.504%` / pair `-0.525%` / target `-3.5%`, missing pair books, and unchanged two-leg fee-buffer behavior. Verified with `.venv\Scripts\python.exe -m pytest tests/test_auto_exit_multileg.py tests/test_project_settings.py -q` (`70 passed`) and `py_compile` (`webapp/services.py`, `tests/test_auto_exit_multileg.py`).
- Auto-exit one-shot checkbox handling was corrected after the latest `LABUSDT` V1 run showed both toggles being cleared even though execution was incomplete. Audit of `logs/auto_exit_history.jsonl` and `logs/manual_exec/2967c8cb0e2c.log` showed V1 triggered at `2026-06-07T19:50:26Z` for 50% of a 150 LAB hedge (target 75 LAB), but only 70 LAB exited and `remaining_qty=5`; the old scheduler disabled both `enabled` and `v1_enabled` immediately after receiving an async `execution_id`, before execution outcome was known. Trigger acceptance no longer changes checkbox state. `_cleanup_completed_auto_exit_spread_rules()` now disarms an `exit_once` rule only after a completed, error-free result explicitly reports `remaining_qty` within tolerance of zero; partial, canceled, failed, or outcome-unknown runs preserve the operator toggles and rebind the signature to residual positions. Regression tests cover LAB-style partial `70/75` preservation and fully completed one-shot disarming; verified with `.venv\Scripts\python.exe -m pytest tests/test_project_settings.py tests/test_auto_exit_multileg.py -q` (`67 passed`) and `py_compile` (`webapp/services.py`, `tests/test_project_settings.py`).
- Android physical-device testing now has a private remote-access path that does not depend on the phone sharing the PC's Wi-Fi. Tailscale Serve is configured on the workstation at `https://desktop-0tl9bsa.tail3830e2.ts.net:8443/`, proxying tailnet-only HTTPS to `127.0.0.1:8000`; `GET /api/mobile/positions` was verified through that address with HTTP 200. The existing public Funnel on port 443 was left unchanged because it is a separate Caddy/BasicAuth route. A fresh debug APK was built and copied to `android-app/app/build/outputs/apk/debug/FeeArb-mobile-test-2026-06-07.apk` (SHA-256 `10D19BB31A0B51FBAA9A1DB492B9F951A31AF35FE1B118FC3E2DF41BD758E034`). Remote phone setup and the warning against direct port forwarding/Funnel are documented in `docs/android_v1_testing.md`.
- Android position-card metrics were rearranged for faster spread comparison. The primary market row now shows `Enter spread / Next funding / Liq dist`; the row below shows `Live spread / Long qty / Short qty`. The visible `Imbalance` metric was removed from the card to free space, while backend imbalance calculation remains available for sizing safeguards and action confirmations. Verified `:app:assembleDebug`, APK reinstall on `Pixel_8`, and UI-tree/screenshot checks confirming both spread labels, both leg quantities, and no visible imbalance label (`android-app/app/src/main/java/com/feearb/mobile/FeeArbApp.kt`).
- Web and Android position controls now share coin-based partial sizing. A new `POST /api/position/action` contract refreshes account positions, calculates `hedged_qty = min(abs(long_qty), abs(short_qty))`, derives `action_qty = hedged_qty * percent / 100`, and then delegates to the existing smart enter/exit preflight or execution path. The web `Symbol Positions` table now exposes custom-percent `Add` / `Exit` controls and Auto Exit percentage; Android position cards are full-width, show both leg quantities, hedged quantity and imbalance, provide 25/50/75/100 plus custom sizing, and require preflight confirmation before execution. Auto Exit rules persist `exit_percent` (default 100) and `exit_once` (default true), apply the percentage to the freshly selected hedged pair, and disarm after a successfully accepted one-shot run. Android HTTP timeouts were raised to 120 seconds because fresh account plus exchange preflight can take 30-40 seconds. Verified Python compilation, `.venv\Scripts\python.exe -m pytest tests/test_project_settings.py tests/test_auto_exit_multileg.py -q` (`65 passed`), Android `assembleDebug`, APK reinstall on `Pixel_8`, live read-only payloads, and a canceled Android dry-run confirmation for BABYUSDT: long `39650`, short `40000`, hedged `39650`, 25% action `9912.5`. No live execution was confirmed (`webapp/app.py`, `webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `webapp/static/styles.css`, `android-app/app/src/main/java/com/feearb/mobile/*`, `tests/test_project_settings.py`).
- Android account data was split into dedicated `Balances` and `Positions` tabs. `Balances` now shows aggregate total/available/used values across returned account balances, reporting/healthy exchange counts, and separate per-exchange cards; `Positions` retains position filters, sorting, cards, and controls. Navigation is now `Balances / Positions / Manual / Settings`, with cold start opening `Balances`. Verified `:app:assembleDebug`, `.venv\Scripts\python.exe -m pytest tests/test_project_settings.py -q` (`43 passed`), APK install and cold launch on the `Pixel_8` emulator, UI-tree/screenshot rendering for both screens, and no fatal Android exception. The emulator is left running on `Balances`; no live order execution was performed (`android-app/app/src/main/java/com/feearb/mobile/FeeArbApp.kt`, `android-app/app/src/main/java/com/feearb/mobile/MobileViewModel.kt`, `android-app/README.md`, `docs/android_v1_testing.md`, `docs/cursor_android_testing_ru.md`).
- Cursor-based Android testing is now documented and runnable without Android Studio. Added `docs/cursor_android_testing_ru.md` with environment/plugin setup, emulator and Samsung workflows, safe test order, backend URL rules, and logcat troubleshooting. Added `.vscode/extensions.json` recommendations (`jetbrains.kotlin-server`, `vscjava.vscode-gradle`, `redhat.vscode-xml`) plus `.vscode/tasks.json` tasks for build, device listing, Pixel 8 emulator start, APK install, app launch, filtered logcat, and app-data reset. Environment audit on 2026-06-07 confirmed Cursor CLI, JDK 17, Android SDK/platform-tools/emulator, API 34/36/36.1, two AVDs, and configured `local.properties`; the recommended Kotlin/Gradle/XML extensions are not currently installed. Verified `:app:assembleDebug` (`BUILD SUCCESSFUL`), started `Pixel_8`, installed the APK, launched `com.feearb.mobile/.MainActivity`, found no fatal Android exception, and visually confirmed the Dashboard loaded live backend balances through the emulator default `http://10.0.2.2:8000/`. No live trading action was executed (`.vscode/tasks.json`, `.vscode/extensions.json`, `docs/cursor_android_testing_ru.md`, `android-app/README.md`).
- Auto-exit checkbox lifecycle was hardened after `GUNUSDT` showed v1 settings disappearing during partial auto-exit. Root cause: `_cleanup_completed_auto_exit_spread_rules()` treated an async run status of `completed` as proof that the position was fully closed and called symbol-wide cleanup with `clear_v1=true`; a completed GUN run still had residual exposure, and an older run could also erase a v1 checkbox re-enabled by the operator. Auto-exit rules now carry an operator-change `rule_generation`, triggered runs capture the exact rule key/generation, and completed-run reconciliation never blindly deletes spread/v1 settings. A matching completed run preserves the rule, rebinds its position signature to a remaining two-leg pair after a partial exit, marks one-leg residuals as protected/non-runnable, or marks a no-position state as awaiting restoration. If the operator changed the checkbox after the run started, generation mismatch prevents the old run from modifying the newer setting. Position-signature guards still prevent preserved settings from attaching to a genuinely new position. Auto-exit rule/update/cleanup events are now also persisted in `logs/auto_exit_history.jsonl` for post-restart diagnosis and recovery instead of existing only in memory. Regression coverage includes no-position preservation, GUN-style residual-pair rebinding, and stale-run/new-checkbox race protection; verified with `.venv\Scripts\python.exe -m pytest tests/test_project_settings.py tests/test_auto_exit_multileg.py -q` (`62 passed`) and `py_compile` (`webapp/services.py`, `tests/test_project_settings.py`).
- Binance manual smart-enter/limit submit was hardened against `-4014 Price not increased by tick size`. Root cause was that local price validation could rely on `market.precision.price` while Binance futures validates against `PRICE_FILTER.tickSize`; `execution/manual.py` now extracts `PRICE_FILTER.tickSize`, `LOT_SIZE.stepSize`, and min-notional filters from `market.info.filters`, prefers those over generic precision/limits, passes final amount/price through ccxt `amount_to_precision` / `price_to_precision` before `create_order`, and retries once on tick-size rejects with a locally re-rounded tick price. Added regression coverage for Binance filter tick size taking precedence over precision (`tests/test_manual_trader.py`). Verified with `.venv\Scripts\python.exe -m pytest tests/test_manual_trader.py -q` and `py_compile`; backend restarted as one service on `127.0.0.1:8000`.
- Auto-exit rules are now bound to a concrete position signature so cached toggles cannot attach to a later manual position. `webapp/services.py` builds `position_signature` from the current hedged legs when a spread/v1 rule is enabled (`symbol`, exchange/side set, qty, entry price), persists `bound_at` / `signature_status`, and checks the signature before any spread or v1 trigger. Legacy rules without a signature now skip with `unbound_position_signature` instead of executing; restored rules only become active when the current legs still match the stored qty/entry signature within tolerance. Symbol-level clearing was strengthened: `POST /api/auto-exit/clear-spread-cache` now accepts `clear_v1`, completed auto-exit clears all rules for that symbol, and the dashboard sends symbol-wide clear with `clear_v1=true` when both `spread` and `v1` are unchecked, including hidden `multileg` rules. Regression coverage was added for unbound legacy blocking, signature binding, and LAB-style multileg/v1 cleanup; verified with `.venv\Scripts\python.exe -m pytest tests/test_project_settings.py tests/test_auto_exit_multileg.py -q` (`webapp/services.py`, `webapp/app.py`, `webapp/static/app.js`, `tests/test_project_settings.py`).
- Auto-exit spread toggle recovery was made operator-controlled and safer after manual/finished exits. `webapp/services.py` now includes `restore_spread_on_missing` in auto-exit defaults, processes empty position snapshots through the missing-rule path instead of returning early, and uses that global setting to decide whether persisted spread rules survive missing positions or are auto-cleared after `auto_clear_no_position_sec`. A new `POST /api/auto-exit/clear-spread-cache` endpoint clears saved spread rules globally or by symbol; rules that are also `v1_enabled` keep v1 but lose the spread target/toggle. Successful completed async auto-exit runs now auto-clear spread rules for that symbol so old cached spread checkboxes do not attach to a new manual position. The dashboard Auto Exit settings expose `Restore spread toggles after missing positions` plus `Clear spread cache`. Regression coverage was added for restore-off auto-clear, manual spread-cache clear, and completed auto-exit cleanup (`webapp/app.py`, `webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `tests/test_project_settings.py`).
- Bybit funding-history adapter output now also corrects stale cached interval declarations. The funding-history service already normalized Bybit LAB to `1.0h`, but direct `BybitAdapter.funding_history()` could still return cached rows with `interval_hours=4.0`; `exchanges/bybit.py` now post-processes returned history by dominant timestamp delta and overrides materially conflicting declared intervals. Direct live adapter check now shows Bybit LAB hourly rows with `1.0h` (`20:00 +0.048184%`, `19:00 +0.120596%`, `18:00 +0.174217%`, ...). Added adapter regression coverage and verified with `.venv\Scripts\python.exe -m pytest tests/test_funding_history_adapters.py tests/test_webapp_funding_helpers.py -q` (`exchanges/bybit.py`, `tests/test_funding_history_adapters.py`).
- Bitget funding-history coverage was fixed for the standalone `/funding-history` page. The old Bitget adapter path used `/api/mix/v2/market/history-fundRate` with `*_UMCBL` contract symbols and commonly fell back to a single current funding point; live checks showed the current Bitget endpoint for LAB is `/api/v2/mix/market/history-fund-rate?symbol=LABUSDT&productType=USDT-FUTURES`, returning hourly `fundingRate` / `fundingTime` history. `exchanges/bitget.py` now uses that v2 endpoint, maps contract symbols back to API history symbols, parses `fundingTime`, and bypasses old one-point cache fallbacks when history is too short. Live `LABUSDT` Bitget now returns 30+ hourly rows (`17:00 -0.348%`, `16:00 -1.9343%`, `15:00 -1.6529%`, ...), with `1.0h` interval in the funding timeline. Added `tests/test_bitget_funding_history.py`; verified with `.venv\Scripts\python.exe -m pytest tests/test_bitget_funding_history.py tests/test_webapp_funding_helpers.py -q` and `py_compile` (`exchanges/bitget.py`, `webapp/services.py`).
- Funding-history interval normalization was hardened for stale/incorrect exchange-declared intervals. Live `LABUSDT` review showed Bybit cached rows carrying `interval_hours=4.0` even though timestamps were hourly; `_compact_funding_history_rows()` / `_resolve_funding_interval_hours()` now infer the dominant timestamp step and override declared/snapshot intervals when they materially conflict. Funding segments, timeline slots, exchange overview, and pair-window carry now use the corrected interval, so Bybit LAB appears every hour with `1.0h` like Binance/KuCoin. Added regression coverage for stale `4h` declarations over hourly timestamps and verified live (`tests/test_webapp_funding_helpers.py`, `webapp/services.py`).
- Funding-history display/slot logic was tightened after live `LABUSDT` review. Binance returns LAB funding settlements like `14:00:00.006Z`, while other venues return `14:00:00Z`; funding-history analysis now builds calculation segments from normalized `slot_ts_ms` so millisecond/second drift cannot split equivalent funding hours or skew short-window coverage. The UI now renders human-readable hourly slots and funding/carry values in exchange-style percentages instead of bps, while keeping potential PnL in USDT per `1000 USDT` notional. Live check on 2026-06-04 confirmed Binance `LABUSDT` resolves to `1.0h` with hourly settlements (`14:00`, `13:00`, `12:00`, ...), latest Binance funding around `-0.917105%`; page cache-buster advanced to `v2026-06-04-02`. Verified with `.venv\Scripts\python.exe -m pytest tests/test_webapp_funding_helpers.py -q`, `py_compile`, live service analysis, and page smoke on `127.0.0.1:8000` (`webapp/services.py`, `webapp/static/funding_history.js`, `webapp/templates/funding_history.html`, `webapp/app.py`).
- Notification routing now supports `ntfy` as the primary Android push channel for Telegram-blocked environments. `utils/notifications.py` accepts `ntfy` in the shared `NotificationRouter`, reads `NTFY_BASE_URL`, `NTFY_TOPIC`, optional `NTFY_TOKEN`, plus optional priority/tags/click envs, and publishes plain HTTP POST alerts to the topic. Protective/settings validation and dashboard selects now include `Ntfy`; default/persisted notification channel was switched to `ntfy -> none`, `.env.example` documents the new variables, and `POST /api/notifications/test` sends a live test message through the configured router. Verified with `.venv\Scripts\python.exe -m pytest tests/test_notifications.py tests/test_project_settings.py -q`, `py_compile`, direct ntfy send (`ok`), endpoint test (`{"status":"ok","primary_channel":"ntfy"}`), and a single refreshed backend process on `127.0.0.1:8000` (`utils/notifications.py`, `project_settings.py`, `risk/config.py`, `webapp/app.py`, `webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `data/settings.json`, `.env.example`, `tests/test_notifications.py`, `tests/test_project_settings.py`).
- Added a standalone funding-history analyzer page at `/funding-history` with API `POST /api/funding-history/analyze`. The page works on one symbol at a time, exposes all supported analysis exchanges as checkboxes with Binance + KuCoin checked by default, and renders best funding-carry windows, full pair/direction calculations, an exchange/time funding grid, and an SVG funding-rate chart. Backend math now treats funding events as settlement segments and prorates by exact overlap for `4h`, `12h`, `1d`, and `3d` windows, so mixed schedules like LAB hourly funding vs 4h/8h venues compare correctly by window instead of by latest-rate shortcuts. Live smoke on 2026-06-04: `LABUSDT` returned 1h history on Binance/OKX/Bybit/Gate/BingX/KuCoin, one weak Bitget point, no MEXC history; `BTCUSDT` returned usable history on the same set except MEXC, with Bitget still short. Verified with `.venv\Scripts\python.exe -m pytest tests/test_webapp_funding_helpers.py -q`, `py_compile` for `webapp/services.py` + `webapp/app.py`, and FastAPI TestClient page smoke (`webapp/services.py`, `webapp/app.py`, `webapp/templates/funding_history.html`, `webapp/static/funding_history.js`, `webapp/static/styles.css`, `webapp/templates/index.html`, `tests/test_webapp_funding_helpers.py`).
- Android mobile v1 was reshaped into the requested compact operator app. The first tab is now `Dashboard` with compact exchange balances plus position cards, `Manual` is simplified around action/symbol/exchanges/qty/notional/chunks/mode/expensive-leg plus `Dry Run` / `Execute` / `Stop`, and `Settings` now exposes only backend URL plus critical safety controls. Backend mobile payloads now include compact balances, and a new read-only `POST /api/mobile/manual-spread` endpoint provides executable buy/sell spread preview using WS orderbooks first, then positions-market cache/public REST fallback. Regression tests cover balance payloads and spread mapping; verified with `.venv\Scripts\python.exe -m pytest tests/test_project_settings.py tests/test_manual_trader.py -q`, FastAPI TestClient smoke for mobile endpoints, and `android-app\gradlew.bat :app:assembleDebug --no-daemon`. No live order execution was performed; no adb device was connected for install.
- Emergency de-risk history now includes post-factum outcome attribution. `webapp/services.py` persists pending follow-up checkpoints in `state/derisk_outcome_state.json` for live `trigger` / `orphan_trigger` actions, and appends matured `outcome` rows into `logs/derisk_history.jsonl` at `1m`, `5m`, `15m`, plus `to_next_funding` when applicable. Each outcome row stores the initial baseline snapshot, the later observed state, and a heuristic label (`improved` / `unchanged` / `worsened`) with compact reasons so later tuning can compare decision-time conditions against what actually happened next. Detailed subsystem roadmap/history was updated in `docs/modules/emergency_derisk_history.md` (`webapp/services.py`, `tests/test_project_settings.py`).
- Emergency de-risk now has a dedicated subsystem history/roadmap doc at `docs/modules/emergency_derisk_history.md`, intended to hold the detailed phase timeline while `AGENTS.md` keeps only the short index/update line. Persistent de-risk analysis history was also added at `logs/derisk_history.jsonl`: `webapp/services.py` now appends `event` rows for trigger/preempt/state-change events and `cycle` rows for compact per-cycle environment snapshots (settings, exchange health, balance stress, selected rows, chosen action), so later tuning can use real live-context history across backend restarts (`webapp/services.py`, `execution/storage.py`, `tests/test_project_settings.py`).
- Emergency de-risk safety was hardened further for ambiguous symbol states. `DataService._auto_derisk_cycle()` now aggregates duplicate visible legs on the same expected exchange/side instead of silently taking only the first row, so orphan sizing and qty-mismatch detection use the full visible exposure. It also now blocks live de-risk/orphan actions with `blocked_by_cluster_conflict` when a symbol shows unexpected extra legs outside the declared pair or when two hedged clusters overlap on the same expected leg (for example the same `symbol + exchange + side` assigned to multiple clusters). Diagnostics/event-log rows now expose `unexpected_legs`, `unexpected_leg_count`, `duplicate_visible_leg_count`, and `overlap_conflicts`, and new regression tests cover duplicate-leg orphan aggregation, extra-leg conflict blocking, and overlapping-cluster blocking (`webapp/services.py`, `tests/test_project_settings.py`).
- Emergency de-risk v1 was extended from diagnostics-only orphan detection into live orphan/panic handling. `ManualTradeManager` in `execution/manual.py` now supports structured stop signals (`requested`, `force_finalize`, `reason`), a dedicated `orphan_cleanup(...)` execution path for single-leg cleanup, and forced final reconciliation even after stop/preempt in fast/smart enter/exit modes when `force_finalize=true`. `DataService._auto_derisk_cycle()` in `webapp/services.py` now ranks confirmed orphan candidates separately from stressed intact pairs, keeps blocking orphan actions on unhealthy exchange data, and in live mode either launches `manual_orphan_cleanup(...)` with panic cleanup semantics or preempts a running auto-exit with `manual_exec_stop(..., force_finalize=True, reason=...)` so panic de-risk can take over without leaving an unmatched leg. Added regression coverage for forced final reconcile on stop, orphan cleanup runtime paths, orphan-trigger/preempt service flow, and updated preempt assertions (`tests/test_manual_trader.py`, `tests/test_project_settings.py`).
- Emergency de-risk v1 scaffolding was implemented as a separate risk-control contour with exchange-health gating, hedge-cluster registry, dust-aware sizing, live partial de-risk trigger, and auto-exit preemption hooks. Added backend helpers in `risk/derisk_manager.py`, new protective settings persistence/validation in `project_settings.py`, scheduler/state/diagnostics/event-log integration in `webapp/services.py`, rule APIs in `webapp/app.py`, and dashboard UI controls/panels in `webapp/templates/index.html` + `webapp/static/app.js`. The system now blocks orphan/panic actions when a venue is `auth_error`, `stale`, or otherwise untrusted, supports manual `standalone` vs `hedged_pair` marking via persisted `state/hedge_clusters.json`, exposes `Emergency De-Risk Exchange Health`, `Hedge Clusters`, `Emergency De-Risk Diagnostics`, and `Emergency De-Risk Log` panels, and includes regression coverage for exchange-health blocking, orphan confirmation, live partial trigger, preempt request, dust escalation to `full_cleanup`, and zero-valued de-risk setting normalization (`tests/test_derisk_manager.py`, `tests/test_project_settings.py`).
- Auto-exit spread toggles in `Symbol Positions` were hardened against transient API/account glitches and refresh-time leg mismatches. Root cause: auto-exit rules were already persisted in `state/auto_exit_rules.json`, but the scheduler could auto-remove them after `auto_clear_no_position_sec` when positions/legs temporarily disappeared from snapshots (`no_position`, `legs_missing`, `multi_leg_pair_rule`). `webapp/services.py` now treats user-created auto-exit rules as `persist_on_missing=true` by default, preserves that flag through normalization and rule updates, and skips auto-removal for persistent rules while still marking them missing/stale in diagnostics. This keeps the checkbox state from silently dropping after temporary exchange/API failures; the operator now explicitly removes a rule by unchecking it. Added regression coverage for default persistence and for keeping a rule after a temporary missing-position cycle (`webapp/services.py`, `tests/test_project_settings.py`).
- KuCoin private auth was hardened against `400002 / Invalid KC-API-TIMESTAMP` failures seen in logs on 2026-05-11. `ExchangeGateway` now proactively calls `load_time_difference()` for `kucoin` clients during initialization and explicitly classifies KuCoin timestamp errors (`KC-API-TIMESTAMP`, code `400002`) as retryable time-sync faults. `ProtectiveOrderManager` no longer bypasses that retry path for direct `create_order` / `cancel_order` calls, so KuCoin protective stop/take create/cancel flows now retry once after clock resync instead of failing immediately. The raw KuCoin private WS helper for `/api/v1/bullet-private` was also hardened to prefer server time and retry once on timestamp errors. Added regression tests for KuCoin timestamp classification, proactive time sync on client build, and protective order retry wiring (`execution/accounts.py`, `risk/stop_manager.py`, `webapp/ws_trade_kucoin_raw.py`, `tests/test_account_monitor.py`, `tests/test_stop_manager.py`).
- Gate integration was refreshed against current official Gate docs and live-tested on 2026-05-11. Code changes:
  - `exchanges/gate.py` now uses the current futures-only REST base URL `https://fx-api.gateio.ws/api/v4` and resolves settle path by contract family: `...USDT -> /futures/usdt/...`, inverse `...USD -> /futures/btc/...`. This removes the old invalid `futures/usdt/BTC_USD` pattern that produced historical `HTTP 400`.
  - Gate market snapshot fetch now treats bulk `/futures/{settle}/tickers` and `/contracts` as opportunistic fast paths only; if they timeout, it automatically falls back to per-contract endpoints so symbol probes still succeed instead of failing the whole adapter.
  - `ExchangeGateway.fetch_balance()` / `fetch_positions()` in `execution/accounts.py` now use Gate raw futures endpoints directly (`privateFuturesGetSettleAccounts`, `privateFuturesGetSettlePositions`, plus cached `publicFuturesGetSettleContractsContract` for `quanto_multiplier`) instead of ccxt generic balance/positions flows that were stalling on `GET /spot/currencies`.
  - `webapp/services.py` Gate debug/probe URL builder now also selects `btc` vs `usdt` settle dynamically instead of hardcoding `futures/usdt/...`.
  - Verification after patch: live Gate private WS login still returns `status=200`; live `ExchangeGateway.fetch_balance()` and `fetch_positions()` now succeed in this environment; live `scripts/exchange_probe.py --exchanges gate --symbol BTC` succeeds; live `GateAdapter.fetch_market_snapshots(['BTCUSD'])` also succeeds for the inverse contract path. Regression tests added in `tests/test_account_monitor.py` and `tests/test_funding_history_adapters.py`.
- Gate API diagnostics were rechecked live on 2026-05-11 from this environment. Current repeated backend warnings in `logs/app.log` are `execution.accounts: gate: account refresh failed: gate GET https://api.gateio.ws/api/v4/spot/currencies` (seen continuously on 2026-05-11 around `09:06` through `10:52` local log time). Direct checks show:
  - Gate public REST futures endpoints are reachable (`/api/v4/futures/usdt/contracts/BTC_USDT`, `/tickers?contract=BTC_USDT` return OK).
  - Gate private WS login succeeds (`futures.login` returns header `status=200`).
  - Gate raw signed private futures REST also succeeds when called directly through ccxt implicit methods (`privateFuturesGetSettleAccounts`, `privateFuturesGetSettlePositions`).
  - The failing path is specifically Gate `GET /api/v4/spot/currencies`, which times out in this environment and causes normal `ccxt fetch_balance()` / `fetch_positions()` gateway refresh to fail before futures data is returned.
  - Separate historical issue: old `gate adapter failed: HTTP Error 400: Bad Request` entries from 2025-10-28 / `data/debug/exchange_probe_btc_20251030_101950.json` were due to the market-data adapter using `futures/usdt/...` for `BTC_USD` (`BTC` probe expands to `BTCUSD` + `BTCUSDT`), while Gate inverse USD contracts actually live under `futures/btc/...`. So the old `400` was a request-construction bug, not a generic connectivity/auth failure.
- Russian Android operator documentation was rewritten into a practical step-by-step test runbook in `docs/android_install_and_test_guide_ru.md`. The guide now explicitly separates what to test on emulator vs physical phone, defines the safe execution order (`backend -> Settings -> Positions -> Manual Analyze -> Auto Exit save -> optional Execute`), documents current Android v1 screen behavior (`Positions`, `Manual`, `Settings`, execute confirmation/preflight), and adds concrete troubleshooting plus test-result capture guidance for first smoke tests.
- Binance manual leverage precheck is now more tolerant to false-negative `set_leverage` failures on symbol-based Binance futures setup. In `execution/manual.py`, `_fetch_binance_symbol_settings()` now also reads symbol leverage/margin mode via `fetch_leverage()` even when `fetch_positions()` is empty, and Binance leverage precheck now treats "already set" responses as noop, retries transient `/fapi/v1/leverage` failures, and re-reads the symbol settings after an exception before deciding to stop. This prevents cases where Binance returns a bare `POST https://fapi.binance.com/fapi/v1/leverage` error string but leverage is already/applied at `3x` from blocking manual enter. Added regression tests for already-set leverage without a position, error-with-successful-readback, and true mismatch that must still fail (`execution/manual.py`, `tests/test_manual_trader.py`).
- `/coin` analysis now includes a dedicated human-readable `Funding / spread view` above the raw funding grid. `DataService.analyze_symbol()` now attaches `visual_analysis` with pair-level summary text, normalized hourly funding-carry series in `bps` that correctly prorates mixed funding intervals like `4h vs 1h`, compact window rows (`4h/12h/24h/72h`), and chart-ready spread/funding timelines. The `/coin` frontend renders this as a summary line, a compact table, and two inline SVG charts (`Spread timeline`, `Funding carry timeline`) so the operator can visually inspect persistence and spread behavior without reading raw JSON/grids. Added regression coverage for the interval-normalization helper and visual payload generation (`webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `webapp/static/styles.css`, `tests/test_webapp_funding_helpers.py`, `tests/test_coin_history_persistence.py`).
- Coin-analysis plan was refined again for the actual `long-hold funding` use case on `binance + kucoin`. `docs/modules/coin_analysis_rebuild_plan.md` now documents: what data is already collected today; what exact API data is needed for a durable candidate/position analyzer; how to normalize mixed funding intervals like `4h vs 1h` using interval-overlap carry aggregation instead of naive last-rate comparison; why candle-close spread is only a proxy and exact spread history must come from synchronized local snapshots; recommended analysis horizons (`24h/72h/7d/14d`), target tables/charts, and the next engineering phases/tests.
- Backend snapshot polling path was optimized to avoid rebuilding the same heavy response on every `/api/snapshot` request. `DataService` in `webapp/services.py` now caches serialized market snapshots (`DataSnapshot.as_dict()`) per snapshot object and caches aggregated account payloads (`positions_by_symbol`, margin diagnostics/log, positions-market panel) while account/positions-market inputs remain unchanged. Added regression coverage in `tests/test_project_settings.py` to ensure snapshot serialization is reused until the snapshot object changes and account grouping is reused across repeated reads of the same account snapshot.
- Added Russian operator-facing install/test documentation for the Android app: `docs/android_install_and_test_guide_ru.md` and generated `docs/android_install_and_test_guide_ru.docx`. It mirrors the English guide but is localized for day-to-day operator use and covers backend startup, APK install on emulator/phone, base URL setup, smoke-test order, and troubleshooting.
- Android app operator instructions are now exported in a downloadable document form as well as plain text. Added `docs/android_install_and_test_guide.md` plus generated `docs/android_install_and_test_guide.docx` with step-by-step setup for backend startup, APK build, emulator install, phone install, base-URL configuration, connectivity checks, smoke-test order, and troubleshooting.
- Android v1 manual execution UX was hardened for first real smoke tests. The Compose app now shows an explicit confirmation dialog after preflight and before live `/api/manual/enter|exit|roll` submit, with a compact payload summary (action, symbol, venues, mode, chunking, slippage, margin mode). `Positions` and `Manual` also now render dedicated loading / empty / error cards so backend reachability problems or empty payloads are visible in-app instead of looking like blank screens. `docs/android_v1_testing.md` and `docs/android_v1_plan.md` were updated to reflect this safer/testable state.
- Android v1 crossed the first real testable threshold. In this environment the local Android SDK was configured via `android-app/local.properties`, `android-app\gradlew.bat :app:assembleDebug` now completes successfully, and the debug APK is produced at `android-app/app/build/outputs/apk/debug/app-debug.apk`. Fixes required to reach this state were bootstrap-level Android adjustments only: launcher XML theme now uses `Theme.AppCompat.DayNight.NoActionBar`, `androidx.appcompat:appcompat` was added, and Compose UI layout no longer relies on `Modifier.weight`, using simpler row/flow layouts instead. Backend mobile payload tests still pass (`python -m pytest tests/test_project_settings.py -q` -> `17 passed`).
- Android v1 launchability was hardened further: Gradle wrapper is now present and verified (`.\gradlew.bat -v` works), and the first actual `:app:assembleDebug` attempt currently fails only on missing Android SDK path, not on project bootstrap. Added `android-app/local.properties.example` plus setup notes in `android-app/README.md` and `docs/android_v1_testing.md` so the next session/operator can create `local.properties` with `sdk.dir` immediately and continue from real Android compile/runtime validation instead of repo setup friction.
- Android v1 is now packaged with Gradle wrapper files under `android-app/` and has an explicit testing runbook in `docs/android_v1_testing.md`. The guide covers backend startup, Android Studio sync, emulator/device base URL setup, smoke-test checklists for `Positions`, `Manual`, `Settings`, recommended live-testing order, known gaps, and the definition of a first testable v1. The Compose UI also exposes `Roll mode` in the manual screen so roll testing is not blocked by the initial scaffold (`android-app/gradlew*`, `android-app/gradle/wrapper/*`, `android-app/app/src/main/java/com/feearb/mobile/FeeArbApp.kt`, `docs/android_v1_testing.md`).
- Android v1 moved from plan-only to initial implementation. Added slim backend contracts `GET /api/mobile/positions` and `GET /api/mobile/manual-defaults` in `webapp/app.py` + `webapp/services.py`, so the phone client no longer needs to consume full `/api/snapshot`. `mobile_positions_payload()` now returns card-ready symbol rows with merged auto-exit state, filter flags (`risk`, `funding_soon`, `auto_exit_on`), expandable detail sections, and fallback live-spread semantics (`auto_exit` executable spread when present, otherwise summary mark spread). `mobile_manual_defaults_payload()` exposes enabled exchanges, main/roll mode options, expensive-leg options, and advanced manual defaults for the app. Regression coverage was added in `tests/test_project_settings.py`.
- Native Android v1 scaffold was added under `android-app/` as a separate Jetpack Compose module. It currently includes bottom navigation for `Positions`, `Manual`, and `Settings`; Retrofit client bindings to the new mobile/manual endpoints; SharedPreferences-backed base URL + advanced trade settings; `Positions` cards with filter/sort chips, inline auto-exit save, and expandable details; `Manual` analyze/execute/stop flow wired to existing backend APIs; and a `Settings / Advanced` screen that keeps chunking/hedge/safety parameters out of the main manual surface. This scaffold was added without Gradle available in the current environment, so first validation/build must happen in Android Studio/Cursor after sync.
- Mobile/Android v1 manual trade flow was adjusted per operator preference so chunk sizing is reachable directly from the main phone-first form instead of being buried in advanced controls. The `/mobile` Manual card now exposes a quick `Chunk qty` input next to the primary trade fields, sends it through the existing manual execution payload as `chunk_qty`, and surfaces `min_chunk_qty` / `recommended_chunk_qty` in analyze/preflight output while auto-filling the field from the plan when the operator leaves it blank (`webapp/templates/mobile.html`, `webapp/static/mobile.js`, `docs/android_v1_plan.md`).
- Auto-exit execution was retuned to behave closer to the operator's preferred manual smart-exit profile while increasing chunk count. Auto-triggered `smart-exit` payloads in `webapp/services.py` now use `reprice_sec=5`, `hedge_order_type=limit`, `hedge_limit_mode=passive`, `hedge_favorable_bps=2`, `hedge_adverse_bps=8`, `hedge_reprice_min_sec=6`, zero hedge/primary offsets, and `max_limit_deviation_bps=30`, so the second leg starts as passive limit and may escalate to market on adverse moves only when auto-exit tier/notional guards allow it. Tier policy defaults were also reduced sharply to make exits more gradual: `tier1 chunk cap=350 / cleanup cap=1500`, `tier2 chunk cap=250 / cleanup cap=800`, `lower_tier chunk cap=150 / cleanup cap=0`, with matching updates in persisted settings/UI defaults (`project_settings.py`, `webapp/services.py`, `webapp/static/app.js`, `data/settings.json`, `tests/test_manual_trader.py`, `tests/test_auto_exit_multileg.py`).
- Notifications are no longer hardwired to Telegram only. Added a shared `NotificationRouter` in `utils/notifications.py` with configurable `primary` / `fallback` channels (`telegram` or `pushbullet`, fallback may also be `none`), `.env` fallback loading for notification secrets, and note-style Pushbullet delivery via account token. `AccountMonitor`, `ProtectiveOrderManager`, and MEXC protective alerts now all route through this shared notifier, preserving existing summary-slot dedupe / alert cooldown logic while allowing `pushbullet -> telegram` fallback. Settings/UI were extended with `protective.notification_primary_channel` and `protective.notification_fallback_channel`, and dashboard labels were generalized from “Telegram alerts” to “Notifications” (`execution/accounts.py`, `risk/stop_manager.py`, `webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `project_settings.py`, `risk/config.py`, `data/settings.json`, `.env.example`, `tests/test_project_settings.py`, `tests/test_notifications.py`).
- Weekly `/coin` review now includes aggregated scorecards in addition to individual tags. `webapp/services.py` computes `entry_review.summary.action_scorecards`, `exit_review.summary.action_scorecards`, and global `summary.phase_scorecards` from stored outcome rows, tracking per-action/per-phase totals, correctness rate, average net delta, and average alternative delta. The weekly review UI in `webapp/templates/coin.html` + `webapp/static/coin.js` now renders dedicated `Entry action scorecard`, `Exit action scorecard`, and `Phase scorecard` tables, and regression coverage was extended in `tests/test_coin_export.py`.
- `Symbol Positions` was compacted further for desktop use without shrinking readability: the table now has a dedicated frontend/table style with wrapped multi-line headers for long columns (`Amount (USDT)`, `Exp. Funding`, `Next Funding`, etc.), tighter per-cell padding, narrower auto-exit controls, and a slightly larger effective desktop font than the generic `data-table--wide` mode. The no-longer-useful `Margin Used` column was also removed entirely to free horizontal space. Numeric formatting in `webapp/static/app.js` was tightened specifically for the widest columns so `Quantity` and `PnL` render with up to 2 decimals (trailing zeros trimmed) and `Exp. Funding` with up to 3 decimals, reducing horizontal waste while preserving meaningful precision (`webapp/templates/index.html`, `webapp/static/styles.css`, `webapp/static/app.js`).
- Main dashboard desktop layout was tightened to fit more panels and wide tables within one screen width without manual browser zoom-out. `webapp/static/styles.css` now applies a denser `@media (min-width: 1200px)` dashboard mode with reduced paddings, smaller panel headings, more compact `data-table` / `data-table--wide` cell spacing, smaller auto-exit controls/chips, and narrower event-log columns. This is a desktop-only presentation change intended to reduce horizontal overflow on the main page.
- Main dashboard `Margin Logic Log` was compacted for better scanability: the log under `Margin Diagnostics` now renders inside a short scrollable `log-box` so only a few recent rows are visible at once while the full current in-memory history remains accessible via vertical scroll (`webapp/templates/index.html`, `webapp/static/styles.css`).
- Experimental `Auto Exit V1` path was added as a separate opt-in rule per position so it can be tested safely on one or two live pairs without replacing the existing target-spread auto-exit. Auto-exit rules in `webapp/services.py` / `webapp/app.py` now support independent `spread_enabled` and `v1_enabled` flags on the same symbol+pair row; the dashboard `Symbol Positions` control in `webapp/static/app.js` now renders two toggles (`spread` and `v1`) while keeping the existing exit-spread input for the old mode. Backend auto-exit payloads now also expose a dedicated `v1_diagnostics` stream alongside the original spread diagnostics.
- Experimental `Auto Exit V1` logic now evaluates a lightweight hold-vs-exit model inside `_auto_exit_cycle()` using current executable close edge, funding-to-next normalized in `bps`, interval-aware decision windows (`1h`/`4h`/`8h` bucket), and a small reversion credit capped from entry-vs-mark spread drift. The new path supports immediate exits for take-profit multiples or strong negative funding near the funding boundary, plus soft exits only after confirmation cycles (`AUTO_EXIT_V1_CONFIRM_CYCLES`) to reduce noise. A separate `Auto Exit V1 Diagnostics` table was added to the main dashboard (`webapp/templates/index.html`, `webapp/static/app.js`) so the operator can inspect stage, minutes to event, funding bps, close-now bps, wait score, TP threshold, and pending confirmation count before trusting the experiment. Added regression coverage for v1 windowing/decision helpers, v1-only rule persistence, and running-state diagnostics (`tests/test_auto_exit_multileg.py`, `tests/test_project_settings.py`).
- Manual execution `P1` tail is now closed for predictable reduce-only rejects. In `execution/manual.py`, `_submit_order(...)` now performs a live pre-submit `fetch_positions()` check when `reduce_only=True`: if the venue has no matching open position on the reducible side, or the requested reduce-only qty exceeds the current position size, the order is blocked before `create_order(...)` with `error_type=reduce_only_required`. Submit-error classification was widened accordingly so local precheck messages and venue-side reduce-only rejects map to the same typed error. Added regression coverage for both no-position and qty-above-position reduce-only blocking in submit validation (`execution/manual.py`, `tests/test_manual_trader.py`).
- Manual execution `P1` hardening now exposes partial hedge failures as an explicit top-level execution risk, not just a leg-local error. In `execution/manual.py`, execution result payloads for manual flows now include `risk_flags`, populated from action `risk_state` values such as `partial_fill_exposure`, so a scenario where the primary leg fills and the hedge leg rejects is visible immediately in the returned execution state. Submit-time validation was also widened to use static market price bounds from ccxt metadata (`limits.price.min/max` or exchange `minPrice/maxPrice`) before `create_order(...)`, and submit-error classification now recognizes venue price-band/price-limit rejects as `price_band`. Added regression coverage for price-band classification, static max-price precheck blocking, and a smart-enter scenario where the primary fill succeeds, the hedge submit fails, and the result carries `risk_flags=["partial_fill_exposure"]` plus the leg `risk_state` (`execution/manual.py`, `tests/test_manual_trader.py`).
- Auto-exit scheduler regression and manual-submit guardrails were hardened together. In `webapp/services.py`, `_auto_exit_cycle()` no longer crashes on the `execution_running` short-circuit path: `append_diagnostic(...)` is now defined before its first use, so diagnostics continue to populate instead of spamming `UnboundLocalError` in the background loop. Protective-sync summary logging now also classifies credential failures separately (`auth_error`) from generic `error`/network cases when exchange responses contain invalid API-key/permissions markers. In `execution/manual.py`, submit-time validation now normalizes limit prices to `price_step`, rounds qty to `amount_step`, blocks below-min-qty / below-min-notional orders before `create_order(...)`, and classifies venue-side submit failures such as Binance TradFi-perps agreement requirement, reduce-only risk-control blocks, tick-size errors, auth errors, and venue risk-limit rejects via `error_type`. Hedge-submit failures after primary fills now surface `partial_fill_exposure` in collected action errors instead of looking like an undifferentiated order reject. Added regression coverage for auto-exit `execution_running` diagnostics, protective auth classification, limit-price tick rounding, pre-submit min-notional blocking, TradFi agreement classification, and partial-fill exposure tagging (`webapp/services.py`, `execution/manual.py`, `tests/test_project_settings.py`, `tests/test_manual_trader.py`).
- Main dashboard account section now includes a dedicated `Margin Diagnostics` table plus a short `Margin Logic Log` beneath it. `DataService._account_state()` exposes per-position diagnostics (`margin_mode`, leverage/leverage source, liquidation buffer, estimated base/min-required margin, max add/reduce capacity, decision, reason) and a rolling log that records only decision changes and real margin add/reduce actions. The frontend renders both separately from the positions table, and regression coverage ensures a KuCoin isolated high-leverage short produces `add_margin` diagnostics without duplicating decision-log entries on identical refreshes (`webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `tests/test_project_settings.py`).
- Margin-control operator settings are now explicit in persisted `protective` config and main dashboard UI instead of relying on hidden `AccountMonitor` defaults. Added `enforce_isolated_margin`, `enforce_leverage`, `target_leverage`, and `kucoin_isolated_topup_only` to settings defaults/normalization/validation (`project_settings.py`, `data/settings.json`), wired them through `DataService._apply_settings(...)` into `AccountMonitor.update_alert_settings(...)` (`webapp/services.py`, `execution/accounts.py`), and exposed them in the `Configuration -> Margin control` section of the dashboard (`webapp/templates/index.html`, `webapp/static/app.js`). Added regression coverage for persistence and validation of the new protective settings (`tests/test_project_settings.py`).
- KuCoin isolated margin control was hardened to avoid oscillating add/reduce behavior during volatility. In `execution/accounts.py`, leverage extraction now prefers `realLeverage`/effective-leverage fields before generic `leverage`, margin-used estimation now recognizes KuCoin `posMargin`/`positionBalance` fields earlier, and KuCoin isolated `target_leverage` enforcement is now top-up-only: `_kucoin_adjust_margin_for_leverage(...)` only adds margin when effective leverage rises above target and never reduces margin when the position is already below target leverage. Auto margin reduction is also explicitly blocked for KuCoin isolated positions (`reason=kucoin_topup_only`) so the liquidation-buffer controller cannot fight the leverage controller on the same venue. Added regression tests for `realLeverage` preference, KuCoin `posMargin` fallback, top-up-only leverage adjustment, and KuCoin reduce blocking (`execution/accounts.py`, `tests/test_account_monitor.py`).
- Auto-exit chunk sizing no longer uses a static scheduler-side tier coefficient. The executable-spread preflight in `webapp/services.py` now reports the raw book-capped executable chunk (plus policy notional cap) instead of multiplying by `0.75/0.60/0.40`, and triggered auto-exits now launch `smart-exit` with `auto_exit_dynamic_chunk=true`, `chunk_qty=None`, and `chunk_notional=<policy cap>`. On the execution side, `ManualTradeManager._apply_auto_exit_exit_overrides(...)` now preserves unset `chunk_qty` in this dynamic mode, so runtime chunking is re-evaluated from live orderbooks each cycle instead of being frozen to a small precomputed chunk. Regression coverage was updated for book-capped metrics and dynamic override behavior (`webapp/services.py`, `execution/manual.py`, `tests/test_auto_exit_multileg.py`, `tests/test_manual_trader.py`).
- Manual page mobile/Tailscale auth prompt spam was traced to frontend behavior under Caddy BasicAuth: `/manual` auto-opened extra raw order WebSocket streams and recreated them on every `Symbol` input change, which can trigger repeated browser auth challenges on mobile for `wss://` requests. `/manual` now keeps raw WS streams disabled by default behind an explicit `Enable WS raw streams` checkbox and debounces symbol-triggered reconnects (700ms) only after the operator enables them; the page also shows an inline warning about Tailscale + BasicAuth mobile prompts (`webapp/templates/manual.html`, `webapp/static/manual.js`).
- Auto-exit diagnostics panel now has operator-focused color cues in the frontend: `Status` is rendered as a colored chip (`trigger=green`, `wait/running=blue`, `skip/cooldown=neutral`), `Edge Δ bps` uses positive/negative value colors, and `Market Cleanup` is colored green/red for allow/block summaries. This change is UI-only and reuses existing dashboard value/status styles (`webapp/static/app.js`).
- Auto-exit diagnostics now includes explicit post-fee edge reserve in `bps` (`Edge Δ bps` = `net_spread_pct - required_net_spread_pct`) so the operator can see current distance to trigger at a glance, not just `Net %` vs `Need %`. While adding this, the auto-exit scheduler was also hardened so diagnostics storage is initialized before the `execution_running` early-return path, avoiding a potential runtime error when an agent is already active (`webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `tests/test_auto_exit_multileg.py`).
- Auto-exit diagnostics now shows planned execution order explicitly: backend derives `Primary Leg` / `Hedge Leg` from the same `suggest_expensive_leg(...)` logic used by execution (tier first, then fees/liquidity), and the dedicated diagnostics table also renders `Decision` reason (for example `lower_venue_tier`). This keeps the operator-visible ordering aligned with actual smart-exit routing (`webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `tests/test_auto_exit_multileg.py`).
- Auto-exit diagnostics table now exposes explicit market-cleanup fallback status per selected pair: backend computes `market_cleanup_summary` using the same tier/notional policy shape as execution (`allow`, `block:tier_blocked`, `block:notional_cap`, `block:cap_zero`) and the UI renders it as a separate `Market Cleanup` column in the dedicated diagnostics panel. Added regression tests for lower-tier and notional-cap blocked cases (`webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `tests/test_auto_exit_multileg.py`).
- Main dashboard now has a dedicated `Auto Exit Diagnostics` table separate from the positions table: auto-exit scheduler stores per-rule diagnostics snapshot (`status/reason`, rule pair vs selected pair, policy tier, gross/net/required spread, safe chunk, chunk/cleanup caps, update time) in `auto_exit.diagnostics`, and `webapp/static/app.js` renders it in its own panel instead of overloading `Symbol Positions`. Added regression coverage that `auto_exit_payload()` exposes diagnostics for UI consumption (`webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `tests/test_project_settings.py`).
- Auto-exit venue policy is no longer hardcoded only in `webapp/services.py`: default tier caps are now persisted under `settings.manual.auto_exit_policy` (`tier1=binance`, `tier2=okx`, `lower_tier=all others`) through `project_settings.py` / `data/settings.json`, and the auto-exit scheduler reads these settings when computing `chunk_notional_cap_usd`, `market_cleanup_notional_cap_usd`, and `edge_buffer_bps` for executable-spread preflight. The main dashboard Configuration -> Auto Exit block now exposes editable fields for those 3 caps across the 3 venue tiers; `webapp/static/app.js` preserves existing `manual` settings while saving the new policy. Added regression coverage for manual-policy persistence and scheduler policy override resolution (`project_settings.py`, `data/settings.json`, `webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `tests/test_project_settings.py`, `tests/test_auto_exit_multileg.py`).
- Coin-analysis service flow completed for remaining Milestone-3/5/9 gaps: focus collector now merges active sessions with held symbols (open paper + live/manual account legs) and tags `focus_reason` accordingly; session lifecycle APIs were expanded with explicit start/extend/stop endpoints (`/api/coin/sessions/start|extend|stop`); new context/history APIs were added for stored review (`/api/coin/history/focus/{symbol}`, `/api/coin/history/bootstrap/{symbol}`, `/api/coin/context/{symbol}`); and a dedicated position-watcher loop was added with scheduler/status/toggle/run controls (`/api/coin/positions-watcher/status|enabled|run`) that runs symbol analysis for held symbols in `position-only` mode (no candidate journal spam, cache bypass). Added regression tests for held-symbol focus collection, session wrappers, context/history helpers, and watcher cooldown/disabled behavior (`webapp/services.py`, `webapp/app.py`, `tests/test_coin_focus_collector.py`, `tests/test_coin_symbol_sessions.py`, `tests/test_coin_history_persistence.py`, `tests/test_coin_position_watcher.py`).
- Coin-analysis outcomes panel now includes a structured decision-review table (not only JSON): `/coin` renders latest outcome rows with `decision_id/action/phase/horizon/correctness/timing/net delta/evaluated time`, enabling fast operator read-through of recent decision quality. Existing raw outcomes JSON block is kept as a compact debug preview (`webapp/templates/coin.html`, `webapp/static/coin.js`).
- Coin-analysis retention/pruning is now operational end-to-end: storage gained `prune_coin_analysis_data(...)` and `get_coin_analysis_table_counts()` helpers (focus/funding/OI/features/decisions/outcomes/real observations/session cleanup + closed-paper pruning while preserving open paper positions), `DataService` added periodic retention scheduler with startup run + last report state, and new maintenance APIs were added: `GET /api/coin/maintenance/retention-status`, `POST /api/coin/maintenance/retention-run`. `/coin` now has a retention panel (max-age days / closed-paper days / run-now button / status JSON). Added DB regression test covering old-row deletion and open-position preservation (`analysis_storage/coin_db.py`, `analysis_storage/__init__.py`, `webapp/services.py`, `webapp/app.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_analysis_db.py`).
- Coin-analysis outcome auto-scheduler now has explicit operator controls: added backend toggle API `POST /api/coin/outcomes/auto-scheduler?enabled=0|1` and service method `set_coin_outcomes_scheduler_enabled(...)`; background loop now skips cycles while paused, and auto-status includes `scheduler_enabled` plus health reason `scheduler_paused`. `/coin` auto-status panel now has `Pause auto scheduler` / `Resume auto scheduler` controls with state-aware button disabling (`webapp/app.py`, `webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`).
- Coin-analysis `Position logic` UI is now operator-structured instead of raw-only JSON: `/coin` renders separate tables for `Paper positions` and `Real / Manual positions` with per-row action review fields (`action`, `decision_phase`, `minutes_to_next_funding`, `last decision time`, and latest available correctness review). Backend position rows now include `decision_ts_ms` and `minutes_to_next_funding`; when a prior reviewed decision exists for the same `state_ref`, rows also include latest outcome summary (`latest_correctness`, `latest_review_horizon`, `latest_timing_quality`, etc.). Added regression assertions for new position-review fields (`webapp/templates/coin.html`, `webapp/static/coin.js`, `webapp/services.py`, `tests/test_coin_history_persistence.py`).
- Coin-analysis auto-outcome observability now keeps short cycle history (not only last run): service records up to 50 recent auto-evaluator cycles and exposes tail in both `state_payload.coin_analysis.outcomes_recent_cycles` and `/api/coin/outcomes/auto-status` (`recent_cycles`). `/coin` auto-status block now shows a compact preview of the last cycles (`e/s/d/err` + optional `scope_symbol`) before full JSON, making trend/regression in evaluator throughput visible at a glance (`webapp/services.py`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`).
- Coin-analysis auto-outcome control loop now supports operator-triggered single-cycle execution from `/coin`: added `POST /api/coin/outcomes/auto-run` (optional `symbol` scope) that runs `evaluate_matured_coin_outcomes_once(...)` immediately and returns both cycle counters and refreshed auto-status. Service auto-cycle runner now accepts symbol scope (`scope_symbol`) for targeted runs without scanning all symbols, and `/coin` includes a `Run auto cycle once` button wired to refresh outcomes + health block after execution (`webapp/app.py`, `webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`).
- Coin-analysis auto-outcome status now includes health semantics: backend `get_coin_outcomes_auto_status(...)` computes `health.status` (`healthy|warn|stale`) with explicit reasons (`scheduler_not_running`, `cycle_not_started`, `cycle_delayed`, `cycle_stale`, `cycle_errors`, symbol backlog signals), and `/coin` now renders this as a colored health line (`success/info/error`) above the auto-status JSON block. This makes stale/delayed evaluator states operator-visible without reading raw counters (`webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`).
- Coin-analysis `/coin` UI now shows background outcome-evaluator health in a dedicated "Auto evaluator status" block: added API endpoint `/api/coin/outcomes/auto-status` and service method `get_coin_outcomes_auto_status(...)` exposing scheduler state, poll interval, last cycle counters (`evaluated/skipped/deferred/errors`), and symbol-specific pending horizon counts (`missing_by_horizon`). Frontend polls this status every 15s and refreshes it after manual outcome evaluate/load actions, so operator can see whether delayed auto-evaluation is progressing (`webapp/app.py`, `webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`).
- Coin-analysis delayed outcome evaluation is now automated in background: `DataService` starts a dedicated scheduler (`_coin_outcomes_scheduler`) that periodically scans decision journal rows, finds symbols with missing horizons, and runs `evaluate_coin_outcomes(..., only_matured=True)` for mature checkpoints only. Added maturity gating in evaluator (`deferred` counter + `only_matured/now_ms` controls) so early horizons are skipped until target time is reached (including `to_next_funding`/`to_exit` targets). Service state payload now exposes coin-analysis outcome cycle telemetry (`outcomes_last_cycle`), and regression test covers maturity gate behavior (`webapp/services.py`, `tests/test_coin_outcomes.py`).
- Coin-analysis outcome payloads now include explicit PnL decomposition and directional correctness: evaluator computes `direction_component_correct`, `spread_pnl_delta_pct`, `funding_pnl_delta_pct`, estimated execution costs (`fees_pnl_delta_pct`, `slippage_pnl_delta_pct`, `execution_costs_pct`), and `net_pnl_delta_pct`, plus `pnl_assumptions` (action-size ratio and cost model). Funding component is now horizon-aware (`15m/1h/4h/to_next_funding/to_exit`) using `net_funding_hourly` or `funding_to_next` fallback projection, and counterfactual net-vs-wait comparison now uses decomposed net values. Added regression assertions for new outcome fields and funding projection (`webapp/services.py`, `tests/test_coin_outcomes.py`).
- Coin-analysis outcome evaluator expanded toward full Milestone-10 decomposition: `evaluate_coin_outcomes` now supports dynamic horizons `to_next_funding` and `to_exit` in addition to `15m/1h/4h`, computes `horizon_target_ts_ms` per decision (from focus snapshot next-funding metadata or paper close timeline), and adds `size_appropriateness` classification to each outcome (`appropriate`, `too_aggressive`, `too_conservative`, etc.). Outcome summaries now include `size_appropriateness` distribution. Added regression coverage for `to_next_funding`/`to_exit` evaluation paths and new size-fit fields (`webapp/services.py`, `tests/test_coin_outcomes.py`).
- Coin-analysis Milestone-9 baseline extended to real/manual positions: `analyze_symbol` position logic now runs unified review for both `paper` and live account positions (from account snapshot) using the same `evaluate_position_signal` semantics, while keeping outputs separated as `position_logic.paper` and `position_logic.real_manual`. For matched live long/short legs, the service now builds paired real position decisions, persists them as `manual_position_review` journal rows with `state_ref=real-*`, and reports real-position coverage stats (`real_positions`, `real_decisions_saved`, `real_legs_detected`, `real_unpaired_legs`). Added regression test for real/manual position logic generation (`webapp/services.py`, `tests/test_coin_history_persistence.py`).
- Coin-analysis real/manual exit tracking is now persisted for outcome horizons: added `ca_real_position_observations` storage with helpers for open/closed snapshots by stable real `state_ref`, `analyze_symbol` now records matched real-pair observations as `open` and emits `closed` observations when a previously tracked pair disappears, and `evaluate_coin_outcomes(..., horizons=["to_exit"])` now resolves target time for both paper (`ca_paper_positions.closed_at_ms`) and real/manual positions (`ca_real_position_observations` closed events). Added DB and service regression coverage for real observation persistence and `to_exit` evaluation on real state refs (`analysis_storage/coin_db.py`, `analysis_storage/__init__.py`, `webapp/services.py`, `tests/test_coin_analysis_db.py`, `tests/test_coin_outcomes.py`, `tests/test_coin_history_persistence.py`).
- Coin-analysis outcomes now support operator-side filtering by `horizon`, `phase bucket`, and `action` end-to-end: `/api/coin/outcomes/{symbol}` accepts `horizons`, `phase_buckets`, `actions` query filters and returns applied filter metadata; service filters rows before recomputing summaries/scorecards. `/coin` Outcome Evaluation panel now has explicit filter controls plus `Load outcomes` button for ad-hoc slices (e.g., pre-boundary + ENTRY actions only), while evaluate flow reuses active filters when refreshing results (`webapp/app.py`, `webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`).
- Coin-analysis pre-boundary operator scorecard now includes an explicit traffic-light indicator (`green/yellow/red/gray`) with score and reason codes (`sample depth`, `hit/wrong rates`, timing help rates). The indicator is computed in backend summary payloads (`operator_scorecard_pre_boundary.traffic_light` plus per-horizon traffic lights) and rendered in `/coin` as a compact header above raw scorecard JSON for faster manual go/no-go reading before funding boundaries (`webapp/services.py`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`, `tests/test_coin_export.py`).
- Coin-analysis `/coin` now has a dedicated operator scorecard block for pre-boundary decision quality (the core 20-15m review zone): outcome summaries now compute and expose `operator_scorecard_pre_boundary` with `hit_rate_pct`, `wrong_rate_pct`, `mixed_rate_pct`, and `would_waiting_15m_help` / `would_exiting_15m_earlier_help` rates, plus horizon-level breakdown inside the same pre-boundary bucket. The scorecard is returned by outcomes APIs and export summary, and rendered in a separate UI panel for fast operator review (`webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`, `tests/test_coin_export.py`).
- Coin-analysis outcome analytics upgraded with timing-phase summaries aimed at the operator’s 20-15m pre-boundary workflow: outcomes API responses now include aggregated `summary` blocks (by horizon, by phase bucket, by phase+horizon, timing-quality, and wait/early-exit counters), with explicit phase bucketing (`pre_boundary`, `mid_interval`, `boundary`, etc.) for fast decision-quality review. Export payloads now also include `decision_outcome_summary` alongside raw `decision_outcomes`, and `/coin` outcomes UI now renders server-side summary directly. Added regression checks for summary correctness in outcomes/export tests (`webapp/services.py`, `webapp/static/coin.js`, `tests/test_coin_outcomes.py`, `tests/test_coin_export.py`).
- Coin-analysis outcome-evaluation loop v1 completed for manual decision quality checks: added service endpoints `/api/coin/outcomes/{symbol}` and `/api/coin/outcomes/{symbol}/evaluate` with horizon-based scoring (`15m/1h/4h`) against stored focus snapshots, including correctness/timing fields (`decision_correctness`, `funding_component_correct`, `spread_component_correct`, `timing_quality`, `would_waiting_15m_help`, `would_exiting_15m_earlier_help`, `net_pnl_delta_vs_alternative`). `/coin` UI now wires the Outcome Evaluation panel end-to-end (run evaluate + load stored outcomes + preview summary). Export payloads/timelines now include `decision_outcomes` and `decision_outcome` records for downstream ChatGPT review. Added regression coverage for outcomes service flow and export/timeline integration (`webapp/services.py`, `webapp/app.py`, `webapp/static/coin.js`, `analysis_storage/coin_db.py`, `analysis_storage/__init__.py`, `tests/test_coin_outcomes.py`, `tests/test_coin_export.py`, `tests/test_coin_analysis_db.py`).
- Coin-analysis replay/backtest hook v1 added: backend can now recompute candidate signals from stored feature snapshots and return a timeline with recomputed decision/action/score plus optional linkage to previously stored `manual_candidate` decisions via `features_ref`. New API endpoint `/api/coin/replay/{symbol}` supports limit/time-range filters and stored-decision comparison; `/coin` UI now has a replay panel (run button + replay summary preview). Also extended `candidate_rules` with `action_from_entry_score(...)` helper and added optional time-range filters to `get_feature_snapshots(...)` for deterministic replay windows (`analysis_decisions/candidate_rules.py`, `analysis_decisions/__init__.py`, `analysis_storage/coin_db.py`, `webapp/services.py`, `webapp/app.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_replay.py`).
- Coin-analysis export layer v1 added for ChatGPT/web review workflow: backend now builds symbol export packages (`schema_version=coin_export_v1`) containing raw market data (focus/funding/OI), derived features, decision journal, scores, and paper positions/events; added timeline exports as CSV and Parquet endpoints (`/api/coin/export/{symbol}`, `/api/coin/export/{symbol}/timeline.csv`, `/api/coin/export/{symbol}/timeline.parquet` with parquet dependency validation). `/coin` UI now includes one-click export buttons (JSON/CSV/Parquet) bound to current symbol/window/funding params (`webapp/services.py`, `webapp/app.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_export.py`).
- Coin-analysis `/coin` UI now supports paper-flow operations end-to-end: page now shows decision journal + position logic blocks, paper-entry form, paper positions table with quick actions (`HOLD`, `ADD_SMALL`, `PARTIAL_EXIT`, `FULL_EXIT`), and paper events viewer. Frontend now calls new paper endpoints and auto-refreshes analysis + paper data after actions; pair table columns were extended with selected direction/action and heading corrected to Binance+KuCoin (`webapp/templates/coin.html`, `webapp/static/coin.js`).
- Coin-analysis paper execution flow v1 added in backend: introduced coin paper APIs to open/manage paper positions (`/api/coin/paper/enter`, `/api/coin/paper/action`, `/api/coin/paper/positions`, `/api/coin/paper/events/{position_key}`), service methods for paper enter/add/partial-exit/full-exit lifecycle, and durable event journaling for every paper action in `ca_paper_events`. Added storage query helper `get_paper_events(...)` and regression tests covering full enter->add->partial_exit->full_exit flow plus event/position reads (`webapp/app.py`, `webapp/services.py`, `analysis_storage/coin_db.py`, `analysis_storage/__init__.py`, `tests/test_coin_paper_flow.py`, `tests/test_coin_analysis_db.py`).
- Coin-analysis in-position rule engine v1 is now connected for paper positions: added `analysis_decisions/position_rules.py` with explicit position actions (`HOLD`, `PARTIAL_EXIT`, `FULL_EXIT`, `ADD_SMALL`, `ADD_BLOCKED`) and explainable reason codes/scores; `analyze_symbol` now evaluates open `ca_paper_positions` for the symbol, emits `position_logic.paper` payload, and persists each review as `ca_decisions` rows with `mode=manual_position_review` + `state_ref=position_key`. Also exported `get_paper_positions` through `analysis_storage` package and extended decision constants with action enums (`analysis_decisions/constants.py`, `analysis_decisions/position_rules.py`, `analysis_decisions/__init__.py`, `analysis_storage/__init__.py`, `webapp/services.py`, `tests/test_coin_position_rules.py`, `tests/test_coin_history_persistence.py`).
- Coin-analysis candidate rule engine v1 and decision journaling are now wired end-to-end: added `analysis_decisions/candidate_rules.py` (action gating + explainable reasons), expanded reason code registry for coverage/interval blockers, switched `DataService._decide_coin_candidate` to the new rule engine, and `analyze_symbol` now persists every candidate recommendation into `ca_decisions` with `mode=manual_candidate` plus `decision_journal` payload in API response. Added storage query helper `get_decisions(...)` and regression tests for rule behavior + service-level decision persistence (`analysis_decisions/candidate_rules.py`, `analysis_decisions/reason_codes.py`, `analysis_decisions/__init__.py`, `webapp/services.py`, `analysis_storage/coin_db.py`, `analysis_storage/__init__.py`, `tests/test_coin_candidate_rules.py`, `tests/test_coin_history_persistence.py`, `tests/test_coin_analysis_db.py`).
- Coin-analysis derived spread + feature engine v1 baseline added: new `analysis_features` calculators now build directional pair features (`long_a_short_b` and `long_b_short_a`) from persisted context (derived executable spread, spread window stats, funding-to-next, OI divergence, decision phase), and `DataService._analyze_pair` now persists one feature snapshot per direction into `ca_feature_snapshots` (`feature_set_version=v1`) while returning selected direction/action in pair output and bot recommendation payload. Added regression tests for spread/feature math and service-side feature persistence (`analysis_features/spread.py`, `analysis_features/engine.py`, `analysis_features/__init__.py`, `webapp/services.py`, `analysis_storage/coin_db.py`, `tests/test_coin_feature_engine.py`, `tests/test_coin_history_persistence.py`, `tests/test_coin_analysis_db.py`).
- Coin-analysis historical persistence baseline added for Binance/KuCoin flow: `analysis_storage` now has typed upsert/query helpers for funding and OI history (`ca_funding_history`, `ca_open_interest_history`), `DataService._analyze_symbol_on_exchange` now persists fetched funding/OI rows into `state/coin_analysis.db` and reports upsert counters in `data_quality`, and KuCoin OI path now falls back to contract snapshot current OI when CCXT history/current calls are unavailable. Added regression coverage for DB history helpers, service-side persistence, and KuCoin OI fallback (`analysis_storage/coin_db.py`, `analysis_storage/__init__.py`, `webapp/services.py`, `tests/test_coin_analysis_db.py`, `tests/test_coin_history_persistence.py`).
- Windows venv launcher behavior verified: on this host, `.venv\Scripts\python.exe` is a redirector that spawns the base interpreter as a child even for a trivial sleep script, so a parent/child Python pair is expected and not specific to FeeArb/uvicorn. Operational target for Windows start/stop is therefore healthy API readiness plus killing both redirector and child PIDs, not forcing a single PID (`scripts/windows/start_feearb_public.ps1`, `scripts/windows/stop_feearb_uvicorn.ps1`).
- Windows public-start scripts hardened: `start_feearb_public.ps1` now launches FeeArb through a dedicated `.venv` Python launcher script (`scripts/windows/run_feearb_uvicorn.py`) and waits for a healthy `GET /api/settings` response before printing `OK`, instead of trusting an open port alone. `stop_feearb_uvicorn.ps1` now stops all FeeArb uvicorn/launcher processes, not just the current listener, eliminating orphan-process drift during restart cycles (`scripts/windows/start_feearb_public.ps1`, `scripts/windows/stop_feearb_uvicorn.ps1`, `scripts/windows/run_feearb_uvicorn.py`).
- Protective trigger-basis coverage extended to Gate and BingX as well: Gate protective stop/take orders now explicitly send `price_type=1` (mark price) and BingX sends `workingType=MARK_PRICE`, aligning their trigger evaluation with liquidation/mark-price logic instead of last-price defaults. Added regression tests covering raw order params for both exchanges (`risk/stop_manager.py`, `tests/test_stop_manager.py`).
- Protective trigger-basis alignment expanded beyond Binance: Bybit protective stop/take orders now explicitly use `slTriggerBy/tpTriggerBy=MarkPrice`, OKX uses `slTriggerPxType/tpTriggerPxType=mark`, and KuCoin uses `stopPriceType=MP`, keeping trigger evaluation closer to liquidation/mark-price risk logic on those venues as well. Added regression tests covering the raw order params for all three exchanges (`risk/stop_manager.py`, `tests/test_stop_manager.py`).
- Binance protective algo orders now explicitly use `workingType=MARK_PRICE` with `priceProtect=TRUE`, aligning stop/take triggers with mark-price/liquidation risk logic and reducing premature exits on contract-price wicks. Added regression test covering the raw Binance algo request params (`risk/stop_manager.py`, `tests/test_stop_manager.py`).
- Protective defaults tightened for low-stop-churn isolated 3x flow: stop gap from liquidation reduced to `2.5%`, stop re-quote threshold to `0.25%`, stale-stop force refresh age to `60s`, and protective scheduler cadence to `60s`; UI/config defaults were aligned to the same values (`risk/config.py`, `project_settings.py`, `data/settings.json`, `webapp/static/app.js`, `webapp/templates/index.html`).
- Auto-margin liquidation-buffer policy updated to `add when buffer <27%`, `reduce when buffer >33%`, target remains `30%`; defaults/settings were aligned to the same values for operator visibility (`execution/accounts.py`, `project_settings.py`, `data/settings.json`).
- Logging bootstrap now runs in FastAPI app startup path and always ensures `logs/app.log` file handlers (including uvicorn loggers that bypass root), so runtime logs are mirrored to file alongside console output (`utils/logging.py`, `webapp/app.py`).
- Protective cancel/replace safety tightened: `_sync_leg` now blocks new stop/take placement when any cancel request fails (`status=cancel_failed`) or when canceled ids still appear after forced re-fetch (`status=cancel_pending`), preventing duplicate/overlapping protective orders during stale exchange cancel states. Added regression tests for both paths (`risk/stop_manager.py`, `tests/test_stop_manager.py`).
- Protective scheduler timing now standardized to 5 minutes: risk default `position_check_interval_sec` is `300`, and protective defaults/runtime settings now include the same key to avoid implicit 10-minute fallback drift (`risk/config.py`, `project_settings.py`, `data/settings.json`).
- Protective post-sync verification and emergency retry added for margin-adjust path: after immediate targeted protective sync, service now runs explicit verify pass (`verify_protective_orders`) against fresh open orders; if issues remain (`missing/mismatch/invalid_side`), it executes a second emergency sync pass only for affected legs, then performs final verify and logs unresolved issues as errors. This is layered on top of forced pre-sync account refresh and forced existing-order cache bypass to minimize stale-stop windows (`webapp/services.py`, `risk/stop_manager.py`).
- Protective hardening for margin-adjust races: after successful auto margin add/reduce, the service now performs an immediate `AccountMonitor` fresh pull (positions/balances only, no extra alert/margin side effects) and then triggers targeted protective sync for affected exchange+symbol with `force_fetch_existing` to bypass cached open-order snapshots. Added stale-stop guard in `ProtectiveOrderManager`: if a stop order is older than `stop_force_requote_max_age_sec` (default 120s), sync forces re-quote even when price delta is below threshold. Added regression tests for stale-age stop update trigger and cache-bypass fetch behavior (`execution/accounts.py`, `webapp/services.py`, `risk/stop_manager.py`, `risk/config.py`, `project_settings.py`, `data/settings.json`, `tests/test_stop_manager.py`).
- Hardcoded liquidation-buffer auto-margin thresholds and immediate protective resync: add/reduce policy is now fixed in code to `add when buffer <25% -> target 30%`, `reduce when buffer >35% -> target 30%` (independent of runtime settings overrides). After successful margin add/reduce events, the service now immediately triggers a targeted protective stop/take sync for affected exchange+symbol instead of waiting for the periodic protective scheduler, reducing stale-stop risk after margin changes (`execution/accounts.py`, `webapp/services.py`, `tests/test_account_monitor.py`).
- Auto-margin policy switched to symmetric liquidation-buffer targeting: for isolated positions, if buffer is below `25%` it now adds margin to target `30%`; if buffer is above `35%` it reduces margin down to `30%` (when reduce is enabled). The old warning/panic add sizing and `target*1.75` reduce trigger were removed from execution logic. Margin add/reduce decisions now emit explicit success/skip/failure logs with buffer context. Added/updated regression coverage for add retry amount propagation under the new formula and reduce-to-target sizing (`execution/accounts.py`, `webapp/services.py`, `project_settings.py`, `tests/test_account_monitor.py`, `data/settings.json`).
- OKX isolated top-up fallback added for exchange cap errors: when `add_margin` fails with `59301`/`exceeds the maximum limit`, auto-margin now retries with smaller add amounts (`90% -> 80% -> 70% -> 60% -> 50%`) and succeeds if any reduced amount is accepted; non-cap errors still fail fast. Also fixed top-up result accounting so `_maybe_adjust_isolated_margin` preserves the actual applied amount (instead of overwriting with requested amount), keeping logs/alerts accurate. Added regression tests for both `_modify_margin` retry behavior and `_maybe_adjust_isolated_margin` amount propagation (`execution/accounts.py`, `tests/test_account_monitor.py`).
- Auto-exit UI guard for one-side symbols: summary rows now include per-side leg counts (`long_legs_count`/`short_legs_count`), and Symbol Positions only renders Auto Exit controls when both long and short sides exist; one-side totals are shown as `one-side` with `n/a` target/spread, preventing meaningless multileg target input when no opposite leg exists (`webapp/services.py`, `webapp/static/app.js`).
- Multileg auto-exit trigger upgraded to overall spread: symbol-level `multileg` auto-exit rules now compare target against weighted basket `overall` spread (long legs vs short legs, live mid by exchange with mark/entry fallback), then execute a normal two-leg smart-exit on the selected reduction pair (smallest leg + largest opposite) using a dynamic pair threshold (`pair_spread_now - buffer`) so pair execution follows overall-trigger timing. Dashboard multileg summary rows now expose Auto Exit toggle/target inputs (stored under `symbol|multileg|multileg`) and show live overall spread when available (`webapp/services.py`, `webapp/static/app.js`, `tests/test_auto_exit_multileg.py`).
- Auto-exit multileg reduction step added: when a symbol has multiple legs, auto-exit no longer hard-skips `multi_leg`; it now selects a two-leg exit pair by choosing the smallest absolute leg (long/short) and pairing it with the largest opposite-side leg, then runs a normal `smart-exit` for that qty to reduce leg count per cycle. For single-pair symbols, rule exchange matching behavior is preserved. Added helper unit tests for pair selection (`webapp/services.py`, `tests/test_auto_exit_multileg.py`).
- Universal exit dust-finalization added for reduce-only exits: after normal final reconcile, smart-exit and fast-exit now detect residual tiny legs against per-leg target end qty (`start_qty - requested_exit_qty`), prioritize the smaller dust leg, and attempt bounded market close (`exit_dust_max_legs`, default `1`); non-closeable min-order errors are downgraded to warnings instead of hard-failing execution (`execution/manual.py`). Added regression tests for dust selection/close, non-closeable min-size handling, and helper detection (`tests/test_manual_trader.py`).
- BingX leverage precheck robustness: invalid-parameter detector now also treats ccxt-side validation text (`setLeverage() requires a side argument, one of (LONG, SHORT, BOTH)`) as non-fatal invalid-params during precheck fallback flow, so this variant no longer hard-stops manual enter. Added regression test for side-required error handling (`execution/manual.py`, `tests/test_manual_trader.py`).
- BingX leverage precheck hardening: manual precheck now tries leverage-set variants in order (`side=LONG/SHORT` -> `side=BOTH` -> default/no-side params). If all BingX variants fail only with invalid-params (`109400`), precheck logs a warning and continues instead of hard-stopping execution; non-parameter errors still fail precheck. Added regression tests for invalid-param skip, hard-error block, and default-variant fallback (`execution/manual.py`, `tests/test_manual_trader.py`).
- Smart-enter absolute-cap regression fix for non-zero starts: absolute safety caps now use per-leg incremental target `start_qty + run_qty` (not plain `qty`), so second enters on the same leg/exchange (e.g., existing Binance long from another pair) no longer block hedge/reconcile at current size; caps still prevent overshoot beyond this run-adjusted absolute target (`execution/manual.py`, `tests/test_manual_trader.py`).
- Smart-enter absolute qty cap hardening: in addition to delta-based caps, pending hedge submits and final reconcile market top-ups are now capped by each leg’s current absolute position (`<= qty` target), so reconcile cannot push a leg above requested qty even when leg deltas are imbalanced or start positions are non-zero (`execution/manual.py`, `tests/test_manual_trader.py`).
- Smart-enter target cap hardening: pending hedge submits are now capped to remaining target size (`qty - current hedge delta`) using live positions (WS/REST fallback to observed fills), so hedge retries cannot accumulate beyond target; final reconcile also caps market top-up by lagging leg remaining target and skips reconcile if that leg is already at/above `qty`, preventing over-growth of the other leg when one side is already oversized (`execution/manual.py`, `tests/test_manual_trader.py`).
- Smart hedge adverse-market confirmation hardened: after fallback market submit, smart enter/exit now waits for fill confirmation (`_await_order_fill`, WS/REST) before re-queueing pending hedge qty, preventing repeated duplicate hedge submits when exchange returns `submitted` with no immediate `filled_qty` (observed on Kucoin). WS terminal `filled/closed` with zero fill is now treated as inconclusive in `_await_order_fill` and falls through to REST/trade-based resolution. Added regression test for submitted-market fallback confirmation (`execution/manual.py`, `tests/test_manual_trader.py`).
- Added Windows helper script `scripts/windows/stop_feearb_uvicorn.ps1` to stop only FeeArb `uvicorn webapp.app:app` listeners on `127.0.0.1:8000` (safe for dev switch in Cursor; leaves Caddy/Tailscale running).
- Telegram hourly position digest dedupe hardened across restarts/processes: summary sending now claims a per-slot marker file under `state/telegram_summary_slots` before send, so only one sender can emit for a given `YYYY-MM-DD HH` slot. Successful sends finalize to `.sent`; explicit HTTP failures release the claim for retry; ambiguous transport errors keep the claim to suppress duplicate bursts in the same slot (with marker auto-prune). Added regression tests for sent/http-error/ambiguous-error slot claim behavior (`execution/accounts.py`, `tests/test_account_monitor.py`).
- Manual WS event log scoping: per-run WS event callback now filters by the current execution legs' exchanges, so unrelated persistent streams (e.g., prior `bingx`) no longer pollute `manual` execution logs for pair-only runs like `binance/kucoin`; listenKey block markers are also applied only for tracked exchanges (`execution/manual.py`).
- Smart-exit/order-sync hardening: fixed a reprice path bug where smart-exit could submit a new primary limit immediately after cancel request (before cancel confirmation), causing overlapping/duplicate primary orders on slow WS exchanges (e.g., BingX). Reprice-cancel now pauses and continues the loop before any new submit, mirroring smart-enter safety. Hedge adverse-market fallback now counts fallback market fills into hedge result (`filled_qty/status/order_id`) instead of returning `partial/0`, and hedge pending qty is re-queued when still above minimum (sub-min remainder behavior unchanged). Added regression test for hedge adverse fallback fill accounting (`execution/manual.py`, `tests/test_manual_trader.py`).
- Manual live spread reconnect optimization: `/manual` now re-subscribes live spread only on subscription-relevant field changes (symbol/action/exchanges/spread-range/live-orderbook toggles), and suppresses redundant re-subscribe payloads; backend `/ws/manual` now keeps existing exchange WS loops when subscription key (`symbol + long_exchange + short_exchange`) is unchanged and applies config updates in-place (no `_shutdown()` restart), reducing unnecessary "connecting..." churn and exchange WS reconnects during unrelated form edits (`webapp/static/manual.js`, `webapp/manual_stream.py`).
- Windows autostart hardening for public Funnel UI: scheduled task installer now registers both `AtStartup` and `AtLogOn` triggers and enables automatic retry (`RestartCount=10`, `RestartInterval=1m`) so Caddy/FeeArb/Funnel self-heal after reboot/logon race conditions; ops doc updated with the new task behavior (`scripts/windows/install_autostart_task.ps1`, `docs/ops/tailscale_funnel_autostart_windows.md`).
- Gate isolated auto-margin add/reduce hardened: added Gate-specific margin path that normalizes `change` precision and supports hedge-mode `dual_comp` endpoint with `dual_side`; when generic Gate `add_margin` fails with `INVALID_PROTOCOL invalid argument: #3`, it now auto-falls back to the dual endpoint instead of failing the margin top-up. Added regression tests for direct dual-mode and protocol-error fallback (`execution/accounts.py`, `tests/test_account_monitor.py`).
- Kucoin funding staleness fix: adapter no longer keeps `contracts/active` forever in memory when reused via cached adapters; contract snapshot now refreshes with TTL (30s) and falls back to last good cache on transient fetch errors, so Symbol Positions funding rates stay fresh (e.g., `ORCAUSDTM` hourly swings) (`exchanges/kucoin.py`, `tests/test_kucoin_adapter_cache.py`).
- OKX next-funding fix: for instruments with hourly cadence (e.g., `OM-USDT-SWAP`), snapshot now uses the nearest upcoming settlement between `fundingTime` and `nextFundingTime` (instead of always `nextFundingTime`), eliminating +1 interval drift in UI (`exchanges/okx.py`, `tests/test_funding_history_adapters.py`).
- Performance/API-load refactor: added reusable adapter-instance cache (`get_adapter_cached`) and switched hot paths (snapshot pipeline + webapp funding/positions/analysis calls) to cached adapters to reuse per-exchange metadata/session state (notably Kucoin contracts cache), reducing repeated adapter init and contract metadata fetches (`exchanges/__init__.py`, `pipeline/data_pipeline.py`, `webapp/services.py`).
- Reduced funding/history overhead in service glue: `_load_funding_history_cached` now calls adapter-native `funding_history` directly with bounded lookback (`limit + 8`, capped), removing nested cache layers and cutting duplicate DB/cache work (`webapp/services.py`).
- Snapshot batching for public market data: Binance and Gate adapters now fetch bulk ticker/funding/contract payloads once per refresh and only fallback to per-symbol calls when needed, significantly reducing HTTP request count for multi-symbol refreshes (`exchanges/binance.py`, `exchanges/gate.py`).
- Coin analysis endpoint now has a short in-memory cache (TTL 90s, bounded size) to suppress repeated heavy historical pulls during rapid UI refresh/retry (`webapp/services.py`).
- Added regression tests for adapter caching and Binance snapshot batching (`tests/test_adapter_cache.py`, `tests/test_binance_snapshot_batching.py`).
- Funding interval/next-funding refactor completed across adapters + services: added shared helpers for timestamp normalization, interval inference, stale-next detection, and future-slot projection; removed hard-coded `8h` fallbacks from history paths; fixed BingX seconds-vs-milliseconds timestamp parsing bug; updated manual funding test + live funding fallback to compute `next_funding` from resolved interval/history instead of fixed defaults (`utils/funding.py`, `exchanges/binance.py`, `exchanges/bybit.py`, `exchanges/okx.py`, `exchanges/gate.py`, `exchanges/bitget.py`, `exchanges/mexc.py`, `exchanges/bingx.py`, `webapp/services.py`).
- Added regression tests for funding time/interval logic, service-layer interval resolution, and per-exchange history parsing (`tests/test_funding_utils.py`, `tests/test_webapp_funding_helpers.py`, `tests/test_funding_history_adapters.py`).
- Telegram position digest freshness and completeness improved: hourly send window widened to `:40..:59` (send once per hour slot after latest account refresh), summary now tries live per-leg `fetch_funding_rate` when funding is missing in raw positions, and duplicated settle suffixes are normalized in symbols (e.g., `RIVERUSDTUSDT` -> `RIVERUSDT`) (`execution/accounts.py`, `tests/test_account_monitor.py`).
- Telegram periodic summary is now a position digest (variant 3) instead of balance lines: per symbol it prints long-first exchange abbreviations, symbol, nominal, spread delta, funding delta %, and optional non-zero quantity delta (`qDelta`); margin/stop alert paths remain unchanged (`execution/accounts.py`, `tests/test_account_monitor.py`).
- Added a dedicated mobile UI route `/mobile` with phone-first quick actions (analyze/execute/stop), execution log polling, and per-position auto-exit controls using existing manual/auto-exit APIs; desktop headers now link to Mobile UI, and static build version was bumped to `v2026-02-15-01` (`webapp/templates/mobile.html`, `webapp/static/mobile.js`, `webapp/static/styles.css`, `webapp/app.py`, `webapp/templates/index.html`, `webapp/templates/manual.html`, `webapp/templates/manual_tests.html`, `webapp/templates/spread_monitor.html`).
- Static assets are now referenced via relative `/static/...` paths in templates (instead of `url_for('static')`) to avoid mixed-content / wrong-scheme issues behind HTTPS reverse proxies (notably Tailscale Funnel), so the UI renders identically on `http://127.0.0.1:8000` and `https://<machine>.<tailnet>.ts.net` (`webapp/templates/index.html`, `webapp/templates/manual.html`, `webapp/templates/manual_tests.html`, `webapp/templates/spread_monitor.html`, `webapp/templates/coin.html`).
- Added Windows ops docs + scripts to autostart a public Funnel-proxied UI with BasicAuth (Caddy on `127.0.0.1:18080` -> FeeArb on `127.0.0.1:8000`, Funnel points to Caddy). Scripts install a Scheduled Task running as SYSTEM at boot (`docs/ops/tailscale_funnel_autostart_windows.md`, `scripts/windows/start_feearb_public.ps1`, `scripts/windows/install_autostart_task.ps1`, `scripts/windows/uninstall_autostart_task.ps1`).
- Binance time-drift hardening: exchange gateway now runs signed `fetch_balance`/`fetch_positions` via time-sync-aware retry (on `-1021`/timestamp recvWindow errors it calls `load_time_difference()` and retries once), and Binance leverage precheck in manual execution now does the same for direct `fetch_positions` calls (`execution/accounts.py`, `execution/manual.py`).
- Manual Tests now includes a dedicated Coin Analysis block and backend endpoint `/api/manual/test/coin-analysis` (symbol/window/funding_points + optional full candles series), returning both compact summary and full analysis payload for candidate validation; static build bumped to `v2026-02-14-03` (`webapp/templates/manual_tests.html`, `webapp/static/manual_tests.js`, `webapp/app.py`, `webapp/services.py`).
- Coin analysis switched to on-demand historical pull (no pre-recording assumptions): core exchanges are Binance+OKX, defaults are now 72h (`4320` minutes) with larger funding history depth; API now returns pair-level spread analytics, funding-interval matching, OI history (ccxt 1h where available), and a `bot_logic` recommendation block (`webapp/services.py`, `webapp/app.py`, `webapp/static/coin.js`, `webapp/templates/coin.html`, `webapp/templates/index.html`, `webapp/static/app.js`).
- Auto-exit agent polling now treats `404 execution_not_found` as stale state: it clears the missing exec id, suppresses re-selection of that stale id from historical events, and stops repeated `/api/manual/exec/{id}` 404 spam; dashboard XHR errors now include HTTP status/detail metadata, and static asset version was bumped to `v2026-02-14-01` (`webapp/static/app.js`, `webapp/app.py`).
- Binance leverage/margin precheck now cancels Binance conditional/algo orders (`openAlgoOrders`) in addition to regular open orders before retrying margin-mode updates, preventing false `-4067` failures when TP/SL triggers remain open (`execution/manual.py`, `tests/test_manual_trader.py`).
- Fixed Binance protective-order side validation: `_fetch_existing` no longer overwrites the position `side` while parsing algo payload rows, preventing false `invalid_side=true` and unnecessary cancel/recreate loops (`risk/stop_manager.py`).
- Added regression test covering Binance algo-order side parsing so a valid long-close `SELL` stop is not flagged as invalid side (`tests/test_stop_manager.py`).
- Auto-margin top-up now derives `margin_used` from fallback fields (`initial_margin`, raw/info `positionInitialMargin`/`isolatedWallet`/`positionBalance`) when leverage is missing, so Binance isolated positions with `leverage=null` no longer skip top-up as `margin_used_missing` (`execution/accounts.py`, `tests/test_account_monitor.py`).
- Smart-enter/smart-exit pending-cancel loops now sanitize pending order ids and skip queuing null ids after WS terminal fills, preventing infinite `pending:[null]` waits and invalid REST status checks (`execution/manual.py`).
- Manual exit/roll precheck now normalizes position symbols (`-SWAP`, `USDTM`, duplicated settle suffixes like `USDTUSDT`) and side aliases (`buy/sell/net` -> `long/short`), plus logs a precheck position snapshot to diagnose false "expected ... found none" errors (`execution/manual.py`, `tests/test_manual_trader.py`).
- Manual exit precheck now infers side from position quantity sign when side is missing, preventing false “expected long/short found none” errors (execution/manual.py).
- Binance leverage/margin precheck now skips changes when an open position exists, and proactively cancels stale open orders when settings need adjustment; leverage set retries after order-cancel if blocked (`execution/manual.py`).
- Saved Cloudflare Tunnel + Access setup instructions to `docs/cloudflare_tunnel_setup.md`.
- Hedge adverse threshold now auto-floors to just over 1 tick (bps) so passive hedge orders don’t flip to market on a stationary 1‑tick spread (`execution/manual.py`).
- Auto-margin top-up now targets the buffer threshold (default 25%) without reserving free balance; any available funds are used and alerts reflect the new logic (`execution/accounts.py`).
- Saved long-term automation roadmap to `docs/automation_plan.md` for future use once Coinglass API is available.
- Auto-exit log now includes a clickable “log” link for the blocking execution when a cycle is skipped (`webapp/static/app.js`, `webapp/static/styles.css`).
- Auto-exit log now shows the blocking execution id/action when a cycle is skipped due to another run (`webapp/services.py`, `webapp/static/app.js`).
- Auto-exit agent now exposes a per-execution log endpoint and “Open log” button; manual exec logs can be viewed via `/api/manual/exec/{id}/log` (`webapp/app.py`, `webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`).
- Auto-exit agent panel now surfaces per-execution errors and log file path; manual executions also write a per-run log file under `logs/manual_exec` (`webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `webapp/static/styles.css`).
- Auto-exit now auto-clears rules after a short no-position/multi-leg window so stale rules don’t spam logs when the UI has no toggle (`webapp/services.py`).
- Added a link to the Spread Monitor page from the Manual header (`webapp/templates/manual.html`).
- Added a dedicated Spread Monitor page (`/spread-monitor`) with up to 3 live pair panels showing orderbooks + spreads via `/ws/manual` (`webapp/templates/spread_monitor.html`, `webapp/static/spread_monitor.js`, `webapp/app.py`).
- Added auto-exit v0: per-position auto-exit rules + settings UI, persisted rules store, and a head loop that triggers smart-exit using live WS orderbooks (single execution at a time) (`webapp/templates/index.html`, `webapp/static/app.js`, `webapp/static/styles.css`, `webapp/app.py`, `webapp/services.py`).
- Symbol Positions now show live spread (WS) alongside auto-exit controls, and the main page includes an Auto Exit Log panel; positions-market diagnostics were removed from the UI (`webapp/templates/index.html`, `webapp/static/app.js`, `webapp/static/styles.css`, `webapp/services.py`).
- Auto-exit logging now includes skip/wait reasons (cooldown, no legs, live missing, below target, execution running), with rate-limited log entries for readability (`webapp/services.py`, `webapp/static/app.js`).
- Auto-exit live spread now allows a looser WS freshness window (15s) to populate even on slower books (`webapp/market_data.py`, `webapp/services.py`).
- Fixed auto-exit UI state normalization to include live spreads + events so the Live Spread column and log panel update correctly (`webapp/static/app.js`).
- Auto-exit live spread now uses the same sign convention as the rest of the app, and the main page adds scrollable/copyable Auto Exit Log plus an Auto Exit Agent panel with live execution logs and a Stop button (`webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `webapp/static/styles.css`).
- Hedge limit monitoring now falls back to REST when WS reports a terminal status with zero filled qty, preventing false “filled=0” on Kucoin from skipping hedges (`execution/manual.py`).
- Kucoin margin-mode precheck now checks existing mode via positions and uses lowercase `isolated/cross` when setting, avoiding false failures (`execution/manual.py`).
- Manual precheck now attempts to set Kucoin margin mode (defaults to isolated) before execution and stops early if it cannot be set (`execution/manual.py`).
- Manual exec runs now expire based on `updated_at_ts` and never prune active (`running`) executions, preventing long-wait manual exits from returning `execution_not_found` (`webapp/services.py`).
- Smart enter/exit now allow repricing when WS misses the active order and REST confirms open; small deviations keep the order until price moves beyond `max_limit_deviation_bps` (`execution/manual.py`).
- Binance manual precheck now skips setting margin/leverage when current settings already match (isolated + 3x), using `fetch_positions` to read symbol settings and avoiding open-order margin-mode errors (`execution/manual.py`).
- Binance margin-mode precheck now cancels open orders for the symbol and retries if Binance blocks the change due to existing orders (e.g., old TP/SL), reducing `-4067` failures (`execution/manual.py`).
- Restored hedge adverse bps input (ticks removed from UI), added tick_bps hint in dry-run plan output, and bumped static build to `v2026-02-04-05` (`webapp/templates/manual.html`, `webapp/static/manual.js`, `webapp/app.py`).
- Removed hedge adverse bps control from manual UI (ticks only) and added `price_step` to dry-run plan output; bumped static build to `v2026-02-04-04` (`webapp/templates/manual.html`, `webapp/static/manual.js`, `webapp/app.py`).
- Added `hedge_adverse_ticks` (ticks override bps) for manual execution, updated UI, and bumped static build to `v2026-02-04-03` (`execution/manual.py`, `webapp/templates/manual.html`, `webapp/static/manual.js`, `webapp/app.py`).
- Added a precheck log entry when `exit_allow_flip` skips position capping (`execution/manual.py`).
- Exit allow-flip now skips precheck position capping and no longer hard-fails on position fetch errors during `_handle_pair` (`execution/manual.py`).
- Fixed manual Exit payload building so `exit_allow_flip` no longer crashes the Execute button, and bumped static build to `v2026-02-04-02` (`webapp/static/manual.js`, `webapp/app.py`).
- Smart-exit now checks if the active order is missing from WS and forces a REST status check (prevents silent fills/double submits when order stream is stale but ping/pong is alive) (`execution/manual.py`).
- Fixed smart-exit/smart-enter UnboundLocalError on `active_order_id` in REST sync, and added a safe post-cancel pause helper to smart-exit (`execution/manual.py`).
- Binance manual execution now sets margin mode + leverage in a hard precheck (with no-op allowance for “already set”), and skips per-order margin-mode calls for Binance (`execution/manual.py`).
- Added a Binance-only leverage test button/endpoint (symbol-only set, no position required) and bumped static build to `v2026-02-03-02` (`webapp/templates/manual_tests.html`, `webapp/static/manual_tests.js`, `webapp/app.py`, `webapp/services.py`).
- Bumped static build version to `v2026-02-03-01` so manual tests UI cache refreshes (`webapp/app.py`).
- Manual tests leverage card now includes margin mode and uses it for Binance before set_leverage (`webapp/templates/manual_tests.html`, `webapp/static/manual_tests.js`, `webapp/services.py`).
- Manual tests leverage now always uses symbol-based set for Binance (no position required) (`webapp/services.py`).
- Manual tests leverage setter now supports Binance without an open position (sets leverage by symbol), and Binance precheck uses `{}` params to avoid NoneType errors (`webapp/services.py`, `execution/manual.py`).
- Binance leverage now uses a hard precheck per symbol (execution stops on failure), and per-order leverage calls are skipped for Binance; background leverage enforcement skips Binance open positions (`execution/manual.py`, `execution/accounts.py`).
- Fixed smart-exit crash by initializing `pending_ws_rest_checked_at` (used when cancel is pending) (`execution/manual.py`).
- Roll mode now defaults to smart-roll (UI fallback + backend default), so roll uses smart chunking unless explicitly overridden (`webapp/static/manual.js`, `execution/manual.py`).
- BingX leverage sets now retry with `side=BOTH` when `LONG/SHORT` returns invalid-params (manual execution + leverage enforcement), and leverage params read BingX `positionSide/ps` when present (`execution/manual.py`, `execution/accounts.py`).
- Positions now fall back to avg/entry price fields (including BingX info payload) so unrealized PnL can compute when `entryPrice` is missing (`execution/accounts.py`).
- BingX market snapshots now use `/quote/ticker` for live prices, and Symbol Positions prefers BingX snapshot marks when they diverge from position data (`exchanges/bingx.py`, `webapp/services.py`).
- Kucoin funding snapshots now derive funding interval hours from contract granularity fields (preferring the smaller of current/standard) to reflect hourly cadence when present (`exchanges/kucoin.py`).
- Kucoin funding history now uses `from/to` time range (required by the API) and reads `timepoint` payloads; raw funding tests now hit the same time-range endpoint (`exchanges/kucoin.py`, `webapp/services.py`).
- Kucoin funding history now carries interval hours derived from contract granularity instead of hard-coded 8h (`exchanges/kucoin.py`).
- Kucoin funding history bypasses the outer cached loader to avoid stale interval data and uses the adapterвЂ™s cached fetch instead (`webapp/services.py`).
- OKX raw WS stream now accepts the client socket before reading messages to avoid "WebSocket is not connected" errors (`webapp/ws_trade_okx_raw.py`).
- OKX protective orders now derive `posSide` from raw position data (net/long/short) to avoid invalid posSide errors in algo orders (`risk/stop_manager.py`).
- BingX order WS now treats `listenKeyExpired` as a reconnect trigger and renews listen keys more frequently to prevent silent staleness (`execution/ws_orders.py`, `webapp/ws_trade_bingx_raw.py`).
- WS order streams now reset state on forced reconnects and suppress `CancelledError` from heartbeat/keepalive tasks to make reconnect/shutdown resilient (`execution/ws_orders.py`).
- Funding tests now include an optional raw API response toggle that returns fresh per-exchange snapshot/history payloads alongside the aggregated result (`webapp/templates/manual_tests.html`, `webapp/static/manual_tests.js`, `webapp/app.py`, `webapp/services.py`).
- Smart enter/exit now run chunks sequentially (primary fill -> hedge -> next chunk) and wait for cancel confirmations before new limit orders (`execution/manual.py`).
- Smart enter/exit now clear active primary order state on terminal statuses (WS/REST), unblocking hedge placement after fills (`execution/manual.py`).
- Smart enter/exit now hedge strictly per-chunk and drop sub-min hedge remainders (no cross-chunk `unhedged_qty` carry); hedging blocks next chunk until done (`execution/manual.py`).
- Removed `max_unhedged_pct` from manual UI/payload and backend schema (`webapp/templates/manual.html`, `webapp/static/manual.js`, `webapp/app.py`).
- Manual stop now skips any post-loop market reconcile/hedge actions in smart/fast modes (`execution/manual.py`).
- BingX order WS now logs explicit connect/listenKey milestones and heartbeat timeout details to `app.log` (`execution/ws_orders.py`).
- Symbol Positions now prefer position-sourced fields and only fill missing funding/mark from snapshots or live funding fetch (`webapp/services.py`).
- Limit pricing now filters orderbook "junk" levels (default 1% of chunk notional/qty), improves 1 tick over best non-self price, and excludes own active size (`execution/manual.py`).
- Hedge limit repricing/cancel now waits for terminal status before placing new orders or switching to market (`execution/manual.py`).
- WS raw streams now suppress `CancelledError` during shutdown to avoid noisy ASGI errors (`webapp/ws_trade_gate_raw.py`, `webapp/ws_trade_private_raw.py`).
- BingX WS raw keepalive now suppresses `CancelledError` on shutdown to avoid noisy ASGI errors (`webapp/ws_trade_bingx_raw.py`).
- OKX/Kucoin/Bitget/Bybit trade raw WS readers now suppress `CancelledError` during shutdown (`webapp/ws_trade_okx_raw.py`, `webapp/ws_trade_kucoin_raw.py`, `webapp/ws_trade_bitget_raw.py`, `webapp/ws_trade_bitget_trade_raw.py`, `webapp/ws_trade_raw.py`).
- Execution log now wraps and pretty-prints payload entries to avoid horizontal overflow (`webapp/templates/manual.html`, `webapp/static/styles.css`, `webapp/static/manual.js`).
- BingX swap user stream validated: REST order/cancel triggers `ORDER_TRADE_UPDATE`, `TRADE_UPDATE`, and `ACCOUNT_UPDATE`; initial `SNAPSHOT` flood is expected; ping requires literal `Pong`.
- Manual API defaults `async_run` to true so Execute uses async runs even if the UI cache is stale (`webapp/app.py`).
- Manual endpoints now log incoming payloads to app logger for debugging missing exec ids (`webapp/app.py`).
- Manual orderbook liquidity gating now checks chunk size (when provided) instead of full qty, with detailed error messages (`execution/manual.py`).
- Fixed dry-run crash when liquidity check passed (guarded missing message in `execution/manual.py`).
- Smart-exit no longer does pre-exit position alignment via market orders; market submits now include explicit `reason=` in logs (`execution/manual.py`).
Recent Changes
- BingX order/position WS now decode zlib/gzip frames and treat ping/pong messages as live signals, responding to ping variants (`execution/ws_orders.py`, `execution/ws_positions.py`).
- Live order/trade WS tracking added in `execution/ws_orders.py` for Bybit/OKX/Gate/Bitget/Kucoin/BingX.
- Smart enter/exit now sync primary fills and hedge control via WS order updates (REST only when WS is stale).
- Fast enter/exit (market) now waits for WS order fills and orderbook refill before next chunk; final reconcile uses REST positions.
- Manual UI now exposes market refill settings (bps/buffer/max wait) for Fast (market) mode.
- Smart enter/exit no longer uses trades-fallback for primary fills; REST status checks skip trade-based recovery when WS is stale.
- Manual UI now shows raw WS order streams for the selected long/short exchanges (auto-connect + subscribe).
- Manual UI now adds Copy buttons for live orderbook, dry-run plan, execution log, and WS raw logs.
- Manual action summaries now include per-action timestamps (`ts`) when available.
- WS order stream stale threshold raised to 45s; BingX WS now marks live on all event types and logs heartbeat gaps to `app.log` for diagnostics.
- Added per-exchange WS notes: `docs/bitget/NOTES.md`, `docs/gate/NOTES.md`, `docs/kucoin/NOTES.md` (BingX notes already in `docs/bingx/NOTES.md`).
- Added module notes: `docs/modules/manual_execution.md`, `docs/modules/ws_orders.md`.
- Manual UI raw WS order logs now normalize symbols per exchange (Gate `BASE_USDT`, Kucoin `BASEUSDTM`, Bitget uses symbol or `default`) and wait for Gate login ack before subscribing.
- LiveOrderTracker now passes the active symbol into WS order streams; Bitget order stream subscribes to symbol-specific `instId` (BASEUSDT) instead of `default`.
- Hedge limit status checks skip REST when WS is live to avoid false fills (BingX limit + market double-fill risk).
- Position reconciliation now runs only at entry/exit fallback; per-chunk WS position checks removed.
- Hedge limit monitoring prefers WS order updates over REST order status.
- Manual live trade rules updated for WS order/trade tracking (`docs/manual_trade_rules.md`).
- BingX swap WS notes captured in `docs/bingx/NOTES.md`.
- BingX swap user stream validated: REST order/cancel triggers `ORDER_TRADE_UPDATE`, `TRADE_UPDATE`, and `ACCOUNT_UPDATE`; initial `SNAPSHOT` flood is expected; ping requires literal `Pong`.
- Manual smart enter/exit now use WS order/trade updates for fill tracking; positions are only used at entry/exit fallback.
- Manual execution now matches base-only symbols (e.g., `GMT`) against full exchange symbols (e.g., `GMTUSDT`) when reconciling positions.
- Gate WS positions stream now signs private subscriptions (auth) to match the working manual-tests flow.
- Chunk reconcile uses min_qty_required * 1.15 as the market fallback threshold; adverse move default is 0.1% (10 bps).
- Live trade rules captured in `docs/manual_trade_rules.md`.
- Dry run now scopes `constraints_exchanges` to the selected pair (long/short or from/to) instead of all analysis exchanges.
- Manual UI hides min-notional/min-level inputs and removes the "Order minimums" panel from the page.
- Dry-run orderbook liquidity shortfalls are warnings (not errors); execution still treats them as errors.
- Smart enter/exit now clamp primary fill deltas to target qty and cap trades-fallback fills to expected size.
- Manual execution logs position snapshots (start/end) for the selected exchanges and symbol.
- Position symbols are no longer injected into the main universe; positions now use a separate positions-market snapshot cache.
- Isolated positions attempt auto margin top-up/reduction with cooldowns; failed top-ups surface in Telegram alerts.
- Added a standalone Bybit WS order test script at `scripts/wsbybit.py` for direct order-create responses.
- `scripts/wsbybit.py` now bootstraps the repo root onto `sys.path` so it runs outside module mode.
- Bybit WS manual trade adds `apiTimestamp`/`recvWindow`; manual tests trade log now shows full tx/rx stream.
- Manual tests WS trade log now appends raw frames + parsed payloads with timestamps (terminal-style).
- Manual trade WS fixes exchange check order and reports server-side exceptions to the client.
- `scripts/wsbybit.py` now waits longer for responses and logs pong/timeout warnings.
- `scripts/wsbybit.py` includes `apiTimestamp`/`recvWindow` in Bybit WS order.create payload.
- Bybit WS order/cancel now include `X-BAPI-TIMESTAMP`/`X-BAPI-RECV-WINDOW` headers plus string timestamps.
- Manual trade WS now forwards raw inbound frames and explicit tx events for full UI logging.
- Manual trade WS now logs server-side rx_raw and UI-bound payloads for debugging missing responses.
- Manual trade WS handles client disconnects cleanly (no crash log).
- Manual tests trade log now shows bybit/okx tx in top pane and raw rx frames in bottom pane.
- Manual trade WS now logs to `logs/manual_trade_ws.log` for server-side tracing.
- Manual trade WS uses Bybit trade-only connection to mirror wsbybit behavior.
- Manual tests trade log now dumps every WS message raw (no filters) in the bottom pane.
- Manual tests shows a log build version (`ws-trade-log-version`) to confirm updated JS is loaded.
- Manual trade WS now always uses a single Bybit trade WS connection (no private WS) to match `wsbybit.py`.
- Manual trade WS now waits synchronously for Bybit responses (wsbybit-style) after order.create.
- Manual trade WS no longer emits extra "order sent" info for Bybit to keep logs wsbybit-like.
- Manual tests now auto-fills order id from Bybit order.create ack payloads.
- `scripts/wsbybit.py` now supports fetch+cancel flow with delays after order.create.
- `scripts/wsbybit.py` now does cancel via WS and REST fetch is optional (`--fetch-rest`).
- Bybit manual trade uses private order stream + trade stream with reqId futures to await acks.
- Manual trade WS now reads trade acks directly (wsbybit-style) while private stream handles order updates.
- Bybit manual trade now uses a single trade reader loop with pre-registered reqId futures to avoid missed acks.
- Manual tests Bybit WS now uses a single trade socket and logs all raw frames (no private stream).
- Manual tests Bybit WS no longer waits for ack; reader loop emits raw/parsed events only.
- Manual tests Bybit WS removes private WS support code (trade-only).
- Manual tests page removes the private WS order test block (trade WS UI disabled).
- Manual tests adds a new WS Trade Raw (Bybit) block using /ws/trade-raw with plain text logging.
- WS Trade Raw block now has a one-click order button that auto-fills timestamp/reqId.
- Manual tests adds a WS Trade Private Raw (Bybit) block with quick subscribe buttons.
- WS Trade Raw block now includes a cancel button with auto timestamp.
- Bybit WS tests: trade ACKs, private order/execution/position streams validated; `execution.fast` + `order` + `position` enough for WS-only flow (with one REST snapshot at start).
- Manual tests now uses raw WS blocks for Bybit trade/private; full raw logs are written to `logs/ws_trade_raw.log` and `logs/ws_trade_private_raw.log`.
- MEXC: no WS buy test; keep public live spread only.
- Added OKX raw WS endpoint at `/ws/trade-okx-raw` with server-side login and raw logging to `logs/ws_trade_okx_raw.log`.
- Manual tests adds an OKX raw WS block with order/cancel/subscribe controls and raw send/log console.
- OKX WS trade sends a top-level `id` per request (required to avoid `Parameter id can not be empty`).
- OKX WS flow validated: `order` ack gives `ordId`, `orders` stream shows `state` transitions (live/filled/canceled), `positions` stream reflects fills (size change to 100 and back to 0 on sell).
- Added raw WS endpoints + manual tests blocks for Bitget, Gate, Kucoin, and BingX:
  - Bitget: trade WS `/ws/trade-bitget-trade-raw` for order/cancel; private WS `/ws/trade-bitget-raw` for `orders`/`positions` streams (login uses `timestamp + GET + /user/verify` signature).
  - Gate: `/ws/trade-gate-raw` with server-side signing for private messages; buttons for orders/positions/fills using `futures.*` channels.
  - Kucoin: `/ws/trade-kucoin-raw` uses `bullet-private` token; buttons subscribe to `tradeOrders`, `position:<symbol>`, and `wallet`.
  - BingX: `/ws/trade-bingx-raw` uses REST listenKey + `open-api-swap.bingx.com/swap-market?listenKey=...`; user stream is listenKey-driven and does not require subscriptions.
- BingX swap-only user stream (USDT-M); coin-m `cswap` endpoints are not used.
- New log files: `logs/ws_trade_bitget_raw.log`, `logs/ws_trade_gate_raw.log`, `logs/ws_trade_kucoin_raw.log`, `logs/ws_trade_bingx_raw.log`.
- Bitget trade WS now uses `wss://ws.bitget.com/v2/ws/private` with `op: "trade"` + channels `place-order`/`cancel-order` (per saved Bitget contract docs); UI sends required params (`marginCoin`, `marginMode`, `force`, etc.).
- Manual tests now use a single Bitget WS Trade Raw panel (no separate private block) with quick subscribe buttons for `orders`, `positions`, `fill`, and `account`.
- Bitget private WS streams (`orders`/`positions`/`fill`/`account`) validated; WS order placement requires VIP, so live/manual orders should go via REST while keeping WS for real-time updates.
- Gate WS auth signing now follows Gate v4 docs (`channel=<channel>&event=<event>&time=<time>`), and WS order/cancel use `futures.order_place`/`futures.order_cancel` with `event: api` + signed payload.
- Gate WS now sends `futures.login` (`event: api`) on connect so `futures.order_place` works without "Not login" errors.
- Gate WS login now tracks server time from `time_ms`/`header.response_time`, retries on timestamp errors, and uses server-time offsets for signing.
- Smart-enter now enforces `reprice_sec >= 3s`, adds 1s pause after cancel, and waits for a fresh WS order update before continuing (to reduce overfill on stale WS) (`execution/manual.py`).
- Smart-enter now blocks new submits when WS misses the active order id; waits for WS and then does a single REST check on the active order before continuing (prevents duplicate orders after WS reconnects) (`execution/manual.py`).
- Manual UI request errors now include method/url/status (and a short response snippet) instead of generic "Request failed" (`webapp/static/manual.js`).
- Fixed smart-exit runtime error from leftover `_require_fresh_ws_update` call (`execution/manual.py`).
- Manual execute now prechecks BingX leverage before trading; if not 3x it attempts to set leverage and fails fast on error (only for non-reduce-only legs). Also removed `marginMode` from BingX `set_leverage` params to match ccxt requirements (`execution/manual.py`).
- Raw WS streams for Bybit private and BingX now defensively accept the websocket inside `run()` to avoid "Need to call accept first" errors after reconnects (`webapp/ws_trade_private_raw.py`, `webapp/ws_trade_bingx_raw.py`).
- Protective order sync now reads stop/take prices from top-level order fields (e.g., BingX `stopLossPrice`/`takeProfitPrice`) so existing TP/SL are detected and not duplicated (`risk/stop_manager.py`).
- Bitget raw WS disconnect fixed by removing duplicate websocket accept that closed the socket (`webapp/ws_trade_bitget_raw.py`).
- Gate WS flow validated: `futures.login` + `futures.orders`/`futures.positions`/`futures.usertrades` subscriptions work; `futures.order_place`/`futures.order_cancel` via `event: api` return ack + result, and WS updates reflect open/filled/canceled states plus position/usertrades updates.
- Kucoin WS raw now reads key version from `ExchangeGateway.spec.options` to avoid crashes when `gateway.options` is missing.
- Kucoin classic WS flow validated: REST limit orders (manual tests) emit tradeOrders updates (open/match/filled), position changes on `/contract/position:SYMBOL`, and wallet updates.
- Kucoin/BingX WS raw panels now include REST order/cancel buttons (limit/market) plus order-id capture to drive WS updates.
- BingX WS raw now uses listenKey keepalive (30m) + close on shutdown; manual tests add extend/close listenKey buttons.
- BingX user stream ping now replies with literal `Pong` per docs to avoid disconnects.
- Symbol Positions now use a dedicated positions-market snapshot cache (per-exchange batch fetch, separate refresh interval) and prefer the freshest source by timestamp; per-position funding live fetches removed.
- Positions-market refresh is decoupled from ArbitrageScanner/Coinglass; universe extension for positions removed; UI adds diagnostics under Symbol Positions (last refresh, per-exchange status, diff hints).
- WS order liveness now uses heartbeat/ping traffic (not just order updates); manual execution probes reconnect before REST fallback.
- Manual UI includes per-exchange WS health settings (heartbeat interval/timeout, reconnect attempts/grace).
- Added WS order health probe script at `scripts/ws_order_health_probe.py`.
- WS order streams now mark live on any inbound WS message (including pings) and on connect to prevent false stale when only heartbeat traffic flows.
- Manual UI now autofills `Chunk qty (base)` from dry-run recommended chunk (max chunk hint), allowing manual edits afterward.
- Added "Force chunk qty" toggle to keep manual chunk size and bypass slippage max-chunk cap when enabled.
- Manual execution now supports stop requests: `/api/manual/exec/{id}/stop` flips `stop_requested`, UI Stop button wires to it, and smart/fast loops halt further chunks (smart modes cancel active primary order).
- Smart-exit no longer overrides user-specified qty/notional with live positions (only warns if requested qty exceeds positions).
- Manual tests UI now has per-exchange WS liveness monitors with script logs, silence timers, and ping probes (separate from raw WS logs).
- Manual tests UI now includes per-exchange Reconnect buttons for WS raw panels.
- Manual execution logs now emit "story" lines for WS health, start-of-run, primary fill deltas, and order submit/status (including hedge limits); manual log rendering hides JSON for story lines for readability.
- WS order health now marks outbound pings as live (ping-send) to avoid false stale on quiet streams; WS health story lines now show missing-stream errors.
- WS order health now exposes warmup state (`warming`, `since_start_sec`) and manual execution treats warmup as non-stale to avoid immediate REST fallback at stream startup.
- Manual execution now logs WS probe lifecycle (server ping, probe ping/pong, heartbeat timeouts, reconnect attempts) via LiveOrderTracker event callbacks.
- Manual execution now surfaces WS connect/auth/listenKey errors (auth missing/failed, listenKey failures, connect errors) from ws_orders streams to explain stale states.
- ws_orders no longer calls `stop()` inside `_connect_and_listen` (replaced with `_reset_connection`) to avoid self-cancel and to allow streams to actually receive frames.
- Manual execution now exposes `/api/manual/exec` for active run summaries; manual UI shows current execution id and resumes polling after reload.
- Settings update now tolerates `manual`/`protective` being null in `/api/settings` payloads to avoid 500s.
- Positions now carry `margin_mode_source`/`leverage_source`; Symbol Positions diagnostics show margin mode/leverage issues vs isolated 3x target.
- Accounts monitor now attempts to enforce isolated margin + target leverage (default 3x) per position with cooldowns; failures log warnings.
- Manual execution now logs positions snapshot sources (`rest`) on start/end snapshots, and logs a pre-final-reconcile positions snapshot (with ws/rest sources) before placing final reconcile orders.
- Protective order sync now treats BingX `110424` / Bitget `22002` as no-position and skips with info log instead of hard error.
- Bitget WS orders now prefer cumulative `accBaseVolume` (or `accFillSz`/`filledQty`) and treat `baseVolume` as per-fill delta to avoid undercounting fills.
- Manual tests now include per-exchange margin/leverage panels with position fetch + add/reduce isolated margin; new endpoints `/api/manual/test/position` and `/api/manual/test/margin/(add|reduce)`; positions now expose initial/maintenance margin for diagnostics.
- Bitget protective stop/take now uses `holdSide=buy/sell` for one-way mode to avoid `holdSide` errors.
- Manual execution removes `max_unhedged_sec` time-based hedge fallbacks; UI no longer exposes the field.
- Smart-exit final reconcile now compares start vs end deltas to only market-close the lagging leg.
- Manual execution logs now include the initial manual payload in execution logs.
- Manual UI log blocks are compacted for single-screen visibility (smaller log heights, no-wrap logs).
- Created code backup at `backups/feeArb-20260115-163416.zip` (excluded `.git`, `.venv`, `__pycache__`, `.pytest_cache`, `logs`, `data`, `snapshots`, `state`, `.env`) ahead of refactor analysis.
- Agreed timing updates: `parser_refresh_seconds=1200`, `exchange_refresh_seconds=300`, `account_refresh_seconds=60`, `positions_market_refresh_seconds=60`, `position_check_interval_sec=600` (stops).
- Aligned candidates cache TTLs to 20 minutes (`PARSE_CACHE_TTL_SECONDS=1200`, `FRESH_CACHE_SECONDS=1200`) to reduce source polling.
- Positions-market refresh now skips when accounts snapshot hasn't changed; funding live fetches use a short in-memory cache to avoid repeated API calls (`webapp/services.py`).
- Protective stop sync now skips legs with no targets and caches existing open-order fetches briefly to reduce duplicate API calls; positions-market fetches are concurrency-limited for stability (`risk/stop_manager.py`, `webapp/services.py`).
- Backups created before cleanup: `backups/feeArb-20260116-135855.zip` and `backups/deadcode-files-20260116-135920.zip`.
- Removed unused modules/settings: `pipeline/source_cache.py`, `orchestrator/realtime_validator.py`, `webapp/services.py:_extend_universe_with_positions`, `project_settings.py:refresh_intervals`, and deprecated risk settings (`balance_check_interval_sec`, `panic_close_batch_size`).
- Backed up and removed likely-unused artifacts/scripts: `scripts/futures_metrics_probe.py`, `scripts/wsbybit.py`, `scripts/ws_order_health_probe.py`, `scripts/test_mexc_ws_login.py`, `coinglass_arbitrage_scraper.py`, `contract_doc.html`, `temp_coinglass.html`, `temp_coinglass_app.js`, `_app.js`, `_app.pretty.js` (backup: `backups/probably-unused-files-20260116-143818.zip`).
- Exchange snapshot refresh now skips when sources/exchange set are unchanged, reusing the last snapshot to reduce API polling (`webapp/services.py`).
- Manual tests now include per-exchange funding checks (rate/interval/next funding) with a new `/api/manual/test/funding` endpoint and funding diagnostics logged to `logs/funding_tests.log` (`webapp/templates/manual_tests.html`, `webapp/static/manual_tests.js`, `webapp/app.py`, `webapp/services.py`).
- Manual margin/leverage tests now return parsed position views (margin mode/leverage awareness) alongside raw payloads, and the UI adds per-exchange leverage setters (`webapp/templates/manual_tests.html`, `webapp/static/manual_tests.js`, `webapp/app.py`, `webapp/services.py`).
- Bybit margin-mode detection now treats numeric strings (`"0"/"1"`) as cross/isolated, and add-margin uses `positionIdx` from the raw `info` payload to avoid one-way mode mismatches (`execution/accounts.py`).
- Bybit margin-mode detection now avoids generic `tradeMode` parsing and uses a heuristic to flag isolated when `positionBalance` ~= `positionIM` despite `tradeMode=0`, aligning with UI observations (`execution/accounts.py`).
- Bybit reduce-margin now uses `/v5/position/add-margin` with negative margin amounts instead of returning unsupported (`execution/accounts.py`).
- Manual tests now estimate withdrawable margin using `equity_est - maintenance_margin * 1.2` (buffered) and show max add/reduce in the position view output (`webapp/services.py`).
- Bybit max-reduce estimate now uses `positionBalance - positionIMByMp` (when available) to match UI withdrawable behavior; generic buffer math remains fallback (`webapp/services.py`).
- Added `docs/exchanges/margin_leverage.md` to log per-exchange margin/leverage findings.
- Bybit min-required margin now uses `positionValue/leverage * (1 + 1% buffer)` (simple model) to estimate withdrawable margin; tests expose this in `position_view` (`webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- Bybit manual tests validated: add margin, reduce margin, set leverage; isolated margin inferred via positionBalance vs positionIM hints.
- Bybit production references captured in `docs/exchanges/margin_leverage.md` (isolated inference, negative add-margin for reduce, max-reduce estimate).
- Margin test estimates now use the simple `positionValue/leverage * (1 + buffer)` formula across exchanges with per-exchange buffers (Bybit 1%, Bitget 1%) (`webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- Bitget leverage set observed: exchange UI updates to target leverage even when manual tests UI returns HTTP 500; investigate server error if recurring (`docs/exchanges/margin_leverage.md`).
- Fixed `/api/manual/test/leverage` crash by defining `before_estimates` and correcting error-path response (`webapp/services.py`).
- Leverage test now always builds `before_estimates` in `manual_test_leverage` to avoid NameError (`webapp/services.py`).
- Reapplied `before_estimates` assignment in `manual_test_leverage` after it was missing in the live file (`webapp/services.py`).
- Added `before_estimates` inside the actual `manual_test_leverage` path (was only in margin test), fixing the NameError (`webapp/services.py`).
- OKX margin adjust now uses raw `posSide` (net/long/short) to avoid "Position does not exist" errors on one-way positions (`execution/accounts.py`, `docs/exchanges/margin_leverage.md`).
- OKX margin reduce now uses initial margin (buffer 0%) as the minimum required, matching UI withdrawable calculations (`webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- Kucoin margin reduce buffer set to ~0.15% for test calculations (based on AXSUSDT screenshot) (`webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- Kucoin margin reduce estimates now target leverage 3x for max-reduce calculations (min margin = positionValue/3 + 0.15% buffer) and expose `target_leverage` in `position_view` (`webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- Kucoin reduce margin now uses raw `margin/withdrawMargin` request because ccxt lacks `reduceMargin`; requires extra API permissions (`execution/accounts.py`, `docs/exchanges/margin_leverage.md`).
- Kucoin leverage enforcement no longer calls `set_leverage` for isolated positions; it adjusts isolated margin to reach the target leverage (positionValue/target + 0.15% buffer) via add/reduce (`execution/accounts.py`).
- Manual execution now retries position fetches before all actions and fails fast if positions cannot be fetched; enter precheck validates balances/mark price (5% buffer) and scales qty/chunks down when needed, while exit/roll prechecks validate position sides and clamp qty to available (`execution/manual.py`).
- Margin control UI split: Protective stops no longer exposes anti-orphan or low-margin alerts; new Margin control block provides auto add/reduce + Telegram alerts for failed top-ups, and account-level low-balance alerts were removed (position-only) (`webapp/templates/index.html`, `webapp/static/app.js`, `execution/accounts.py`, `risk/stop_manager.py`).
- Manual live spread now stays enabled for roll mode, mapping roll legs to long/to and short/from for spread + orderbook display (`webapp/static/manual.js`).
- Kucoin manual/test orders now include `leverage=3` in order params and skip `set_leverage`/`set_margin_mode` calls to avoid 1x opens (`execution/manual.py`, `webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- Kucoin manual/test orders now default to one-way mode by sending `positionSide=BOTH` in order params (`execution/manual.py`, `webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- BingX margin add/reduce now passes `positionId` and `positionSide` from raw positions to satisfy API requirements (`execution/accounts.py`, `docs/exchanges/margin_leverage.md`).
- BingX reduce estimates now use `info.margin` and `info.maxMarginReduction` to match UI withdrawable values (`webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- Gate reduce estimates now prefer `info.margin` and `info.initial_margin` with a 3% buffer to align with UI max withdrawable (`webapp/services.py`, `docs/exchanges/margin_leverage.md`).
- Reduce margin now retries with smaller amounts on failure (10% then 5% additional reduction) across exchanges (`execution/accounts.py`).
- Manual Bitget raw WS no longer double-accepts the FastAPI socket (prevents immediate disconnects in the manual page raw stream) (`webapp/ws_trade_bitget_raw.py`).
- Smart-enter now pauses 1s after cancel, requires a fresh WS order update before resuming, and enforces >=3s reprice interval to reduce overfills (execution/manual.py).
- Raw WS streams (bingx/private) now stop cleanly when the client disconnects or receive_json throws, avoiding "accept first" errors (webapp/ws_trade_bingx_raw.py, webapp/ws_trade_private_raw.py).
- Manual execution now blocks order submit/cancel after BingX listenKey errors and forces WS reconnection before continuing (`execution/manual.py`).
- BingX raw WS stream now auto-reconnects on listenKeyExpired/404 keepalive or remote close to keep the manual tests stream live (`webapp/ws_trade_bingx_raw.py`).
- Hedge adverse fallback now waits for cancel confirmation before switching to market to avoid duplicate hedge orders (`execution/manual.py`).
- Smart enter/exit now forces a REST status check for pending primary cancels after a WS wait to avoid overlapping orders when WS updates lag (`execution/manual.py`).
- Positions-market refresh now still runs on interval even if accounts snapshot hasn't changed, so mark prices keep updating (`webapp/services.py`).
- Symbol Positions now prefer positions-market snapshot funding rate/next funding when available, so funding stays fresh (`webapp/services.py`).
- Account monitor now logs explicit margin add/reduce decisions and a per-refresh вЂњisolated margin check okвЂќ summary with min buffer, plus risk summary counts (`execution/accounts.py`).
- Manual tests funding UI now splits per exchange into snapshot/history subblocks with symbol examples, raw request/response panels, and history points; funding test endpoint now returns `funding_history` and honors `history_limit` (`webapp/templates/manual_tests.html`, `webapp/static/manual_tests.js`, `webapp/app.py`, `webapp/services.py`).
- OKX raw WS stream now guards accept/receive with WebSocketState checks and handles RuntimeError to avoid "Need to call accept first" errors (`webapp/ws_trade_okx_raw.py`).
- Symbol Positions now rechecks live funding when the positions-market snapshot is missing or stale (age > 2x interval / 90s) to avoid stuck funding on venues like BingX (`webapp/services.py`).
- Symbol Positions now treat positions-market funding as stale when snapshot age exceeds the refresh interval or next_funding_time is >5m in the past, forcing a live funding refresh (`webapp/services.py`).
- Binance reduce-margin estimates now prefer isolated wallet/margin fields and maintenance margin (no buffer), and maintMargin is parsed for positions (`webapp/services.py`, `execution/accounts.py`).
- Roll now supports `smart-roll` (smart chunked limit + hedge) and auto-switches to it when chunking is requested; roll UI exposes the new mode (`execution/manual.py`, `webapp/templates/manual.html`).
- Protective stop/take rounding now ignores cached tick sizes that are >= price (or that round to 0), preventing false invalid-price skips on low-priced symbols (`risk/stop_manager.py`).
- Binance protective price rounding now refreshes symbol meta from exchange info when cached tick sizes look invalid, to avoid stale tick errors (`risk/stop_manager.py`).
- Binance protective TP/SL now uses the futures algo conditional endpoints (create/list/cancel) instead of standard order endpoints, so stops/takes are placed as true conditional orders (`risk/stop_manager.py`).
- Fixed smart-exit crash by initializing pending WS REST retry state (`execution/manual.py`).
- Manual execution now requires start positions (errors if unavailable), retries end-position fetch once, and falls back to observed fills for final reconcile when positions are still missing; smart/fast enter/exit track hedge/observed fills for this fallback (`execution/manual.py`).
- Smart enter/exit now log positions snapshot at start and end (in addition to pre-final reconcile), for clearer visibility of what positions were seen (`execution/manual.py`).
- Fixed smart-exit hedge fill tracking variable binding after adding observed-fill reconcile (`execution/manual.py`).
- Manual exit now supports an "allow flip / skip position check" toggle (UI + payload), which disables reduce-only for exit; positions-based reconcile is still used when positions are available, with observed fills as fallback only if positions cannot be fetched (`execution/manual.py`, `webapp/templates/manual.html`, `webapp/static/manual.js`, `webapp/app.py`).

- Added Binance USDT-M adapter with premium index + book ticker snapshots, funding history, and symbol meta caching (`exchanges/binance.py`, `exchanges/__init__.py`).
- Added Binance to supported exchanges/defaults, commission map, env example, and analysis defaults (`config.py`, `project_settings.py`, `.env.example`, `webapp/static/app.js`).
- Added Binance orderbook WS support for manual spread + market data bus and symbol normalization helpers (`webapp/manual_stream.py`, `webapp/market_data.py`, `webapp/manual_symbols.py`).
- Added Binance raw WS trade panel + backend listenKey stream and JS wiring (`webapp/ws_trade_binance_raw.py`, `webapp/app.py`, `webapp/templates/manual_tests.html`, `webapp/static/manual_tests.js`).
- Funding test raw payloads now include Binance endpoints and manual tests show Binance symbol hints/examples (`webapp/services.py`, `webapp/static/manual_tests.js`).
- ArbitrageScanner candidate filtering now respects enabled exchanges (include list), and defaults focus on Binance/OKX (`parsers/arbitragescanner.py`, `pipeline/data_pipeline.py`, `webapp/services.py`, `project_settings.py`).
- WS order tracker now supports Binance listenKey streams for ORDER_TRADE_UPDATE with keepalive and health defaults (`execution/ws_orders.py`, `project_settings.py`).
- Verified Binance API keys via ccxt `fetch_balance` (USDT-M) on 2026-02-01.
- Accounts monitor now skips Binance margin-mode enforcement on open positions and passes explicit leverage param + validation to avoid Binance set_leverage errors (`execution/accounts.py`).
- Binance adapter now sanitizes input symbols (separators, double USDT suffix) and tolerates HTTP errors by parsing error payloads instead of raising on 400s (`exchanges/binance.py`).
- Protective stop/take placement now skips invalid prices (incl. Binance min tick) with clear status instead of raising, preventing stop sync crashes on tiny prices (`risk/stop_manager.py`).
- Binance protective stop/take now uses STOP_MARKET / TAKE_PROFIT_MARKET with stopPrice to satisfy Binance algo endpoints (`risk/stop_manager.py`).
- BingX protective sync now parses stopLoss/takeProfit JSON payloads and stopLossEntrustPrice to detect existing TP/SL orders and avoid duplicate placements (`risk/stop_manager.py`).

- Bybit protective stop/take now forces `tpslMode=Full` to avoid `slSize` max market size errors for market stops (`risk/stop_manager.py`).
- Added protective auto-rebalance agent (limit -> market fallback, chunking, cooldown/delta thresholds, MEXC excluded) with UI settings and execution wiring (`webapp/templates/index.html`, `webapp/static/app.js`, `webapp/services.py`, `execution/manual.py`, `project_settings.py`).
- Bumped UI static asset version to force refresh (`webapp/app.py`).
- Drafted a rebuild plan for the coin analysis module with `binance + kucoin` as phase-1 scope: exact cross-exchange spread history must be collected/stored locally (not inferred from candle closes), feature groups were defined for funding/spread/basis/OI/liquidity/economics, and the roadmap now explicitly separates manual hypothesis testing, decision journaling, position watching, and later advisory/auto exit modes (`docs/modules/coin_analysis_rebuild_plan.md`).
- Refined the coin analysis rebuild plan into a detailed implementation roadmap: locked separate storage in `state/coin_analysis.db`, split history collection into full-universe `15s` discovery snapshots plus tracked-symbol focus mode, made the decision engine direction-agnostic for both `long binance/short kucoin` and the inverse, and expanded the module scope to include paper-mode journaling, partial add/exit logic, export/replay hooks, and future auto-entry compatibility (`docs/modules/coin_analysis_rebuild_plan.md`).
- Reframed the coin analysis rollout after product clarification: phase 1 should be symbol-first and operator-driven, not full-universe/Coinglass-driven. The working loop is now `manual symbol selection -> analysis -> optional paper enter -> real-time monitoring`, and the same watcher must cover both paper positions and real/manual positions. Broad discovery collection is deferred as an optional later expansion only if needed (`docs/modules/coin_analysis_rebuild_plan.md`).
- Further refined the analysis module product rules: paper positions and real/manual positions should be shown in separate UI blocks, decision journaling must track whether a decision was later correct across multiple dimensions (not only final PnL), and the engine must be explicitly time-aware around the main `20-15 minute` pre-funding/pre-boundary review window because that is the operator’s primary deliberate decision zone unless there is earlier emergency deterioration (`docs/modules/coin_analysis_rebuild_plan.md`).
- Added an actionable implementation roadmap for the analysis module: milestone-by-milestone backend/UI build order, concrete vertical slice for the first usable version (`symbol -> analysis -> paper enter -> watcher`), likely module/file split, and definition of done for each stage so implementation can proceed without re-planning from scratch (`docs/modules/coin_analysis_rebuild_plan.md`).
- Started implementation of the analysis roadmap (milestones 0/1 foundation): added new package skeletons (`analysis_storage`, `analysis_registry`, `analysis_collectors`, `analysis_features`, `analysis_decisions`, `analysis_paper`), introduced decision constants/reason-code registry, implemented `state/coin_analysis.db` storage layer with schema/versioning and core helpers for instruments/pairs/sessions/focus snapshots/features/decisions/outcomes/paper positions/events, and added regression tests for schema idempotency and core CRUD flow (`analysis_storage/coin_db.py`, `analysis_decisions/constants.py`, `analysis_decisions/reason_codes.py`, `tests/test_coin_analysis_db.py`).
- Implemented milestone 2 (instrument registry and shared pair resolution): added Binance/KuCoin registry refresh module with exchange-symbol normalization, contract metadata extraction, pair-key generation, and persistence into `coin_analysis.db` (`ca_instruments` + `ca_pairs`); extended DB helpers with instrument/pair query APIs and added registry tests validating shared-symbol intersection and persistence (`analysis_registry/coin_instruments.py`, `analysis_storage/coin_db.py`, `tests/test_coin_instruments_registry.py`).
- Implemented milestone 3 (symbol session bootstrap): added `DataService` session lifecycle helpers (`bootstrap_symbol_session`, `list_active_coin_symbol_sessions`) backed by `coin_analysis.db` session rows, wired session bootstrap into `/coin/{symbol}` page open and `/api/coin/{symbol}` analysis path, added `/api/coin/sessions` for active-session visibility, updated coin-page core-exchange hint to Binance+KuCoin, and added regression tests for session TTL/extension/expiry behavior (`webapp/services.py`, `webapp/app.py`, `webapp/templates/coin.html`, `tests/test_coin_symbol_sessions.py`).
- Implemented milestone 4 (focus collector baseline): added periodic coin-focus scheduler in `DataService` with one-cycle collector (`collect_coin_focus_once`) that reads active symbol sessions, fetches live snapshots for selected analysis exchanges, and persists normalized rows into `ca_market_snapshots_focus`; added focus snapshot query helper/API (`/api/coin/focus/{symbol}`) and regression test covering session->collect->persist flow (`webapp/services.py`, `webapp/app.py`, `analysis_storage/coin_db.py`, `tests/test_coin_focus_collector.py`).
- Auto-exit safety phase 1 tightened around venue tiers: execution now treats `binance` as tier-1 liquidity and `okx` as tier-2 (`execution/manual.py`), `suggest_expensive_leg(...)` now prefers the lower-tier venue before fee/liquidity tie-breaks, and `smart-exit` now honors the plan’s suggested expensive leg instead of defaulting to `long` when no explicit override is passed. Auto-exit runs are tagged with `auto_exit_agent`, receive automatic safer overrides (smaller chunk from `recommended_chunk_qty`, limit hedge defaults, tighter limit-deviation cap), and market fallback/cleanup is now blocked on venues below tier-2 for auto-exit paths (hedge adverse fallback, final reconcile, dust finalize). Added regression tests for tier selection and auto-exit market-fallback gating (`webapp/services.py`, `execution/manual.py`, `tests/test_manual_trader.py`).
- Auto-exit safety phase 2 moved trigger logic from raw mid-spread to executable pair spread on a safe chunk. `webapp/services.py` now computes pre-trigger pair execution metrics from live books (`sell long bids` vs `buy short asks`) under an 8 bps slippage cap, applies an extra venue-tier safety factor to size the chunk (`binance/binance=0.75`, worst tier-2=0.60, lower tiers=0.40), stores the executable spread in `auto_exit.live_spreads`, and passes the computed `chunk_qty` into the launched `manual_exit`. This makes the auto-exit decision and displayed live spread reflect something closer to actually tradable spread instead of midpoint-only optimism. Added regression tests for chunk safety factor and executable-spread chunk sizing (`webapp/services.py`, `tests/test_auto_exit_multileg.py`).
- Auto-exit safety phase 3 added explicit venue policy on top of executable spread. Scheduler-side policy in `webapp/services.py` now derives pair taker fees, worst-tier venue policy, per-trigger chunk-notional cap, and an extra net-edge buffer (after fees) before auto-exit may start; auto-exit now requires `net_spread_pct >= target_spread_pct + edge_buffer_pct` instead of comparing raw gross spread to target. The launched payload also carries `auto_exit_market_cleanup_notional_max`, and execution-side guards in `execution/manual.py` now enforce not only tier gating but also market-cleanup notional caps for hedge adverse fallback, final reconcile, and dust-finalize market orders. Added regression tests for pair policy / executable metrics and notional-cap enforcement (`webapp/services.py`, `execution/manual.py`, `tests/test_auto_exit_multileg.py`, `tests/test_manual_trader.py`).
- Coin-analysis review/data layer expanded toward weekly strategy review. Added new `coin_analysis.db` tables for `ca_trade_activity` (paper/real/manual activity journal) and `ca_candidate_shortlist_snapshots` (tracked top-candidate snapshots), with retention/count coverage and CRUD helpers (`analysis_storage/coin_db.py`, `analysis_storage/__init__.py`). `DataService` now writes trade activity for paper enter/action flows and for detected real/manual open/closed pair observations, and the focus scheduler periodically captures a `markets_top3` shortlist by taking the top Binance+KuCoin opportunities from the main snapshot, re-running lightweight symbol analysis, persisting shortlist rows, and extending symbol sessions. New review/export APIs were added: `GET /api/coin/review/weekly` for 7-day activity/shortlist/positions/decision summary and `GET /api/coin/review/{symbol}` for a ChatGPT-friendly `coin_review_v1` package that wraps the weekly review plus per-symbol export. Added regression tests for the new DB helpers and review/export service flow (`webapp/services.py`, `webapp/app.py`, `tests/test_coin_analysis_db.py`, `tests/test_coin_export.py`).
- `/coin` now exposes the weekly review workflow in the UI instead of requiring raw API calls. Added a dedicated `Weekly review` panel with configurable `days/top-candidates`, preview tables for recent trade activity and shortlist history, and direct `review JSON/CSV` export buttons wired to the new review endpoints. Backend also gained CSV export helpers/routes for review payloads: `GET /api/coin/review/weekly.csv` and `GET /api/coin/review/{symbol}/timeline.csv`, flattening trade-activity + shortlist rows into a ChatGPT/Excel-friendly timeline (`webapp/templates/coin.html`, `webapp/static/coin.js`, `webapp/services.py`, `webapp/app.py`, `tests/test_coin_export.py`).
- Weekly review now has first operator heuristics instead of only raw history. `get_coin_weekly_review(...)` builds `review_tags` with simple transparent rules: `missed_entry` (strong shortlist row without nearby trade), `late_exit` (stored outcome says exiting 15m earlier would have helped and alternative delta is materially negative), and `stale_position` (open paper position older than 24h with no follow-up actions after entry). These tags are exposed in JSON/CSV review exports and rendered in `/coin` as a dedicated `Review tags` table plus summary counts (`webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_export.py`).
- Weekly review tags are now prioritized, not just listed. Each `review_tag` gets an `impact_score` and severity (`info/warn/high`) so the operator can sort weekly issues by likely cost/importance: `missed_entry` scores from shortlist strength + rank + edge, `late_exit` from counterfactual net delta and poor timing, and `stale_position` from idle age. Review summary now exposes `review_tag_severity_counts` and `top_review_items`, and the `/coin` UI shows score directly in the `Review tags` table plus compact “top review items” in the summary block (`webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_export.py`).
- Weekly review is now split into structured `entry review` and `exit review` slices in addition to the global tag list. Backend groups tags into `entry_review.tags` (`missed_entry`) and `exit_review.tags` (`late_exit`, `stale_position`), each with its own `summary.total`, per-tag counts, and `top_items`. `/coin` now renders dedicated `Entry review` and `Exit review` tables so the operator can review entry mistakes and exit mistakes separately instead of scanning the mixed tag feed. Missed-entry detection was also refined to suppress false positives when a trade happened near the shortlist timestamp on either side, not only after it (`webapp/services.py`, `webapp/templates/coin.html`, `webapp/static/coin.js`, `tests/test_coin_export.py`).
- `Entry review` now covers not only missed opportunities but also bad and correctly skipped entries. New tags in `get_coin_weekly_review(...)`: `bad_entry` for entry actions (`ENTRY_SMALL/ENTRY_STRONG/ADD_SMALL`) that later underperformed or were outright incorrect, and `good_no_trade` for `NO_TRADE/ADD_BLOCKED` decisions that correctly avoided a losing entry. These are included in `entry_review.tag_counts`, global `review_tags`, and CSV/JSON exports, with the same `impact_score/severity` prioritization as other weekly-review heuristics (`webapp/services.py`, `tests/test_coin_export.py`).
- `Exit review` now also tracks positive and negative hold/exit quality, not only late/stale cases. Added `good_exit` for `PARTIAL_EXIT/FULL_EXIT` decisions whose stored outcomes confirm exiting was the better choice versus waiting, and `bad_hold` for `HOLD` decisions that clearly underperformed even when they do not fit the narrower `late_exit` condition. These are included in `exit_review.tag_counts`, `exit_review.tags`, and export payloads with the same impact/severity ranking as the rest of weekly review (`webapp/services.py`, `tests/test_coin_export.py`).
- `Auto Exit V1 Diagnostics` now runs in shadow mode for ordinary active auto-exit rules even when the per-row `v1` toggle is off. The scheduler in `webapp/services.py` now computes the same `v1` hold/exit decision payload for spread-enabled rules and appends it to `auto_exit.v1_diagnostics` with `status=shadow` (or `skip` when context is unavailable), while actual `v1` execution still requires `v1_enabled=true`. The dashboard hint text was updated accordingly and regression coverage was added to ensure shadow diagnostics populate without calling `manual_exit(...)` (`webapp/services.py`, `webapp/templates/index.html`, `webapp/static/app.js`, `tests/test_project_settings.py`).
- `Auto Exit V1` take-profit was tightened to avoid early exits on weak funding edge. The TP threshold in `webapp/services.py` is now `max(40 bps, 4 x funding_to_next_bps)` instead of the older lower floor / window-varying multiplier, so examples like `funding_to_next=+0.05%` with `close_now=+0.30%` stay in `hold` because they do not clear the new `0.40%` minimum. Window payloads now report `TP K = 4.0x` consistently, and regression coverage was added for both the `40 bps` floor and the `4x funding` branch (`webapp/services.py`, `tests/test_auto_exit_multileg.py`).
- Manual-trade `Expensive leg (manual) = Auto hint` now resolves consistently during execution, not only in dry-run output. `execution/manual.py` now uses one shared primary-leg resolver across `limit-first-expensive`, `smart-enter`, `smart-exit`, `smart-roll`, and fast enter/exit ordering, so the plan’s `suggested_expensive_leg` (tier -> fee -> liquidity -> tie-break) is applied whenever no explicit override is chosen. Roll suggestions are also mapped correctly from `long/short` into `to/from`. Added regression coverage for auto-hint leg resolution and `limit-first-expensive` / `smart-enter` execution paths (`execution/manual.py`, `tests/test_manual_trader.py`).

Next Steps (Planned)
- Android/mobile app planning is now captured in `docs/android_v1_plan.md` so a new session can resume without rereading chat. Agreed v1 scope is intentionally narrow: `Positions` and `Manual Trade` first, with `Settings / Advanced` as a secondary surface; candidates, coin-analysis, and full desktop parity are out of scope. `Positions` should be card-based with collapsed/expanded detail and inline auto-exit editing, while `Manual Trade` should be a simplified analyze -> execute flow that keeps low-level hedge / reconnect / WS tuning hidden under advanced settings.
- Review and possibly trim dry-run scope (constraints collection and exchange set).
- Align dry-run latency with real trading needs (two-exchange focus).
- Re-evaluate which orderbook sources are required for dry-run vs live execution.
- Run manual margin/leverage tests per exchange (fetch position by symbol, add/reduce margin, capture liq/limits + logs).
- Head decision engine plan drafted in `docs/head_decision_plan.md` (auto-exit by spread, coordinator, concurrency rules).
- Plan: build a main "Head" decision module on the homepage to rank candidates by spread history stability, funding trend, liquidity/slippage, and available balances; select symbol/exchanges and hand off to a ManualTrade-like execution agent.
- Plan: design an Exit module with explicit stop/take criteria (spread compression/expansion vs entry, funding deterioration, time-in-trade, liquidity/market move guards) using positions-market cache + orderbook for sizing/exit pacing.

Planned Modules
- Head decision module (main page): rank candidates by spread history stability, funding trend, liquidity, and balances; select symbol/exchanges and hand off to a ManualTrade-like agent.
- Exit module: explicit stop/take criteria for spread compression/expansion and funding deterioration using positions-market cache + orderbook.

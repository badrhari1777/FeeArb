# FeeArb: реестр модулей и план безопасного рефакторинга

## Статус и цель

Этот документ является каноническим реестром production-модулей FeeArb на
период большого рефакторинга 2026-08. После каждого смыслового блока нужно
обновлять фактический статус, перенесенные уроки, тесты и следующий шаг.

Рефакторинг не является разрешением на изменение live-позиций. Перезапуск
backend и повторный `ARM PUMP LIVE 3000` остаются отдельными операторскими
действиями. Strategy Lab остается `research_only_no_trading`.

## Решения оператора

- сохранить Auto-Arbitrage Grid;
- удалить недоделанный Auto Strategy;
- вывести Auto Exit из runtime до полной замены на исследованный lifecycle;
- вывести из runtime любое автоматическое сокращение позиций и старую политику
  emergency de-risk;
- сохранить защитные стопы, контроль изолированной маржи, автоматическое
  добавление маржи, проверку свежести биржевых позиций и безопасный технический
  reduce-only executor без самостоятельного решения о сокращении;
- сохранить Manual Trade, ручной Spread Monitor и ручную Funding History;
- убрать старый Coin Analysis и старый candidate discovery из production;
- сохранить Pump Live, его signal feeder, управление капиталом и переводы;
- развивать Strategy Lab как единый источник будущих проверяемых гипотез;
- перед удалением любого старого кода сохранить его полезные алгоритмы, ошибки
  и обязательные regression cases в этом документе.

## Авторитетная runtime-граница

Production ownership зафиксирован в `webapp/runtime_modules.py`. Эти флаги не
являются пользовательскими настройками и не могут быть включены старым JSON или
формой браузера.

Активные контуры:

- account monitor и биржевые позиции;
- positions-market cache;
- protective orders и margin add;
- Manual Trade;
- Auto-Arbitrage Grid;
- Pump Live;
- Strategy Lab Observatory.

Выведенные из recurring runtime:

- Auto Exit;
- Auto Strategy;
- position-reduction/de-risk decision loop;
- legacy Coin Analysis;
- legacy candidate discovery.

## Реестр модулей

### Account Monitor, balances и positions

Статус: `KEEP_ACTIVE`.

Сохранить:

- exchange-authoritative позиции и балансы;
- явные freshness/status/error поля;
- нормализацию coin quantity и текущего USDT notional;
- отдельные Main и Pump account aliases;
- bounded polling только нужных бирж.

Доработать: выделить единый read-model и убрать повторные приватные запросы из
Manual, protection, Grid и web views.

### Manual Trade и общий execution kernel

Статус: `KEEP_ACTIVE_REFACTOR`.

Сохранить:

- независимые caps по qty и USDT notional с выбором меньшего объема;
- maker-first primary leg и немедленное хеджирование фактического fill;
- partial fill, cancel-confirm, retry, dust reconciliation;
- exchange constraints, contract multiplier и risk-limit preflight;
- WS-first order/position confirmation с REST fallback;
- dry-run как plan-only preflight.

Доработать: разделить planning, exchange gateway, execution state machine,
reconciliation и audit. Ручной exit/roll остается операторским действием.

### Protective orders и margin control

Статус: `KEEP_ACTIVE_SAFETY_CRITICAL`.

Сохранить:

- isolated margin и целевое плечо;
- stop/take classification разных бирж;
- gap-free replacement, Full-position semantics и close-all validation;
- margin add при ухудшении liquidation buffer;
- свежую повторную проверку после каждого изменения;
- fail-closed поведение при неизвестном состоянии биржи.

Не включать до отдельного исследования:

- auto margin reduce;
- auto rebalance;
- выбор позиции для сокращения;
- автоматическое закрытие прибыльной или убыточной позиции.

### Auto-Arbitrage Grid

Статус: `KEEP_ACTIVE_PRODUCT`.

Сохранить:

- ступенчатую state machine по фактической, а не номинальной позиции;
- симметричные partial-enter/partial-exit reversals;
- hedge repair и возможность выхода при заблокированном новом входе;
- adoption существующей позиции;
- ownership conflict и KuCoin risk-limit preflight;
- dust terminal state и exchange-authoritative reconciliation.

Будущее: Grid должен использовать общий execution kernel, но сохранять свою
отдельную стратегию и состояние.

### Auto Strategy

Статус: `REMOVE`.

Причина: недоделанный универсальный конструктор статических шагов не имеет
активных стратегий и будет заменен versioned proposals Strategy Lab.

Сохранить как regression knowledge:

- приоритет exit над enter;
- один общий execution worker;
- reconcile фактического прогресса шага;
- completion tolerance и защита от повторного выполнения.

### Auto Exit

Статус: `DISABLE_THEN_ARCHIVE`.

Причина: статическая формула не является доказанным lifecycle. Активных правил
на момент решения нет.

Сохранить для будущего `Lifecycle Executor`:

- executable bid/ask spread после fees;
- несколько подтверждающих циклов;
- position signature и verified missing-position logic;
- partial exit continuation и rule generation;
- venue liquidity tiers, aggressive-limit hedge и dust cleanup;
- строгую привязку результата к фактическому execution id.

Будущие решения `HOLD / PARTIAL_EXIT / EXIT / ROLL` должен выдавать отдельный
Strategy Lab policy package после chronological OOS, paper и shadow.

### Emergency de-risk и сокращение позиций

Статус: `DISABLE_DECISION_LOGIC`.

Старый score нельзя использовать для live-сокращений. Нужно сохранить только
безопасные примитивы: freshness veto, cluster ownership, orphan diagnostics,
reduce-only execution, max-action cap и reconciliation.

Новая политика должна исследовать funding, прогноз spread, liquidation buffer,
стоимость ожидания, доступный transfer и последствия выбора donor/loser/winner.
До ее promotion система не принимает самостоятельных решений о сокращении.

### Legacy Coin Analysis

Статус: `DISABLE_RUNTIME_ARCHIVE_DATA`.

Старая SQLite база не удаляется и не переписывается. После остановки writers ее
нужно подключать только read-only как исторический источник Strategy Lab.

Сохранить:

- held-position/manual-pin priority;
- funding interval normalization;
- feature/outcome identity и причинные horizons;
- data-quality veto вместо нулевых значений;
- position observation и operator decision journal.

Не переносить как торговое правило старые hand-written score thresholds.

### Legacy candidate discovery

Статус: `RETIRED`.

Главный dashboard больше не запускает старые Coinglass/ArbitrageScanner
collectors. Внешние источники принадлежат Candidate Observatory, служат seed и
обязательно проверяются собственным пятибиржевым feed.

### Pump Live

Статус: `KEEP_ACTIVE_SAFETY_CRITICAL`.

До закрытия live-позиций запрещен функциональный rewrite состояния. Дальнейшее
разделение допускается только с совместимой state migration и replay тестами.

Сохранить раздельные компоненты: signal admission, ladder state, isolated
margin, shared capital pool, transfers, protection, accounting, notifications
и restart recovery. Текущий Pump shadow schedule фактически является live
signal feeder для `main_pullback_tier`; его нельзя отключать без замены.

### Strategy Lab

Статус: `KEEP_ACTIVE_RESEARCH_ONLY`.

Strategy Lab формирует только versioned observations/features/proposals и не
имеет execution API. Текущий funding forecast остается research hold; простое
правило большого spread отклонено. Любая стратегия проходит
`historical -> chronological OOS -> unseen symbols -> paper -> shadow ->
operator-approved live`.

### Web, Android и diagnostics

Статус: `KEEP_SIMPLIFY`.

Главная страница должна показывать сначала общий капитал, биржевые счета,
Main/Pump позиции, protection/freshness и действующие модули. Редкие настройки
размещаются в нижнем закрытом `details`. Manual tests/raw WS остаются отдельной
operator/debug поверхностью и не должны загружать главную страницу.

## Безопасная последовательность

1. Отключить recurring workers retired-модулей неизменяемыми runtime flags.
2. Упростить главную страницу и убрать retired controls/routes из навигации.
3. Перенести тесты полезных state-machine свойств в будущие общие компоненты.
4. Удалить Auto Strategy production code.
5. Остановить Coin Analysis writers и сохранить SQLite read-only.
6. Разделить `DataService` на account/protection/manual/grid/read-model services.
7. Унифицировать exchange gateway/cache и измерить API load до/после.
8. Отдельно рефакторить Pump с совместимой state migration.
9. Удалять архивный код только после периода нулевых runtime/import обращений.

## Журнал

### 2026-08-13 — runtime boundary, block 1

- Зафиксированы решения оператора и полный реестр модулей.
- Добавлен immutable production contract `webapp/runtime_modules.py`.
- Общий двухсекундный worker больше не вызывает Auto Exit, Auto Strategy или
  position-reduction/de-risk; он обслуживает только сохраненный Grid.
- Legacy Coin Analysis focus/outcomes/retention/position-watcher больше не
  запускаются при startup и settings update.
- Код и данные retired-модулей пока не удалены; это обратимый первый этап.
- Pump Live, его signal feeder, Manual, Grid, protection и Strategy Lab не
  изменялись.

### 2026-08-13 — compact dashboard, block 2

- Главная страница заменена на компактный operational dashboard: общий капитал,
  Bybit Main/Pump, биржевые балансы, Main/Pump позиции, видимая защита,
  liquidation buffer, Pump ladder/margin state, Grid и runtime registry.
- В основной навигации сохранены Manual Trade, Grid, ручной Spread Monitor,
  ручная Funding History, Pump Live, Positions и Strategy Lab. Ссылки на
  Auto Strategy и старый Coin Analysis удалены.
- Редкие актуальные настройки аккаунт-монитора, positions-market, стопов,
  тейков и margin add перенесены в закрытый нижний `details`. Настройки
  auto-reduction, auto-rebalance, de-risk и Auto Exit не показываются.
- Добавлен отдельный `/api/dashboard`, который строится только из кэшированных
  runtime/account/Pump снимков. Он не выполняет биржевые запросы и не включает
  payload старых Auto Exit, Auto Strategy, de-risk и Coin Analysis.
- Один Pump status snapshot используется одновременно для балансов и позиций;
  браузеру больше не нужны параллельные `/api/snapshot` и
  `/api/positions/overview` для главной страницы.
- Старые API пока сохранены для совместимости отдельных страниц; production
  template больше не загружает большой `static/app.js`.
- Проверки: Python compile, JavaScript parse smoke через Windows JScript до
  ожидаемой runtime-границы `window`, `git diff --check`, focused regression
  `83 passed + 8 subtests`, полный suite `795 passed + 8 subtests`.
- Live backend не перезапускался: работающий Pump Live и его позиции этим
  блоком не затронуты.

Следующий шаг: убрать Auto Exit из подробного read-only Positions UI и затем
архивировать Auto Strategy/legacy Coin Analysis production routes и import
graph, сохраняя данные и regression knowledge.

### 2026-08-13 — retired web surfaces, block 3

- Подробный `/positions` больше не запрашивает, не показывает и не предлагает
  Auto Exit. Он оставлен чистым read-only представлением фактических Main/Pump
  позиций и биржевой защиты.
- Удалены HTML/API поверхности незавершенного Auto Strategy: `/strategies` и
  `/api/strategies*`. Внутренний legacy state-machine пока оставлен только как
  временный код-архив до переноса его полезных regression properties.
- Удалена старая `/coin/{symbol}` страница и ее большой frontend bundle.
  Исторические `/api/coin/*` export/read endpoints и SQLite пока оставлены для
  контролируемой миграции данных в Strategy Lab; recurring writers уже
  отключены block 1.
- Удалены неиспользуемые `static/app.js`, старый `/api/snapshot` и принудительный
  `/api/refresh`. Сохранение настроек больше не строит большой legacy state.
- Обновлены README и Android/web balance troubleshooting на новый
  `dashboard.js`/`/api/dashboard` контракт.
- Проверки: Python compile, JavaScript parse smoke для `positions.js`,
  `git diff --check`, focused regression `130 passed + 8 subtests`, полный
  suite `796 passed + 8 subtests`.
- Live backend не перезапускался; Pump Live не изменялся.

Следующий шаг: отделить полезные Auto Strategy execution invariants от
strategy-specific store/scheduler, затем удалить внутренний Auto Strategy код и
его persistence initialization из `DataService`.

### 2026-08-13 — Auto Strategy internal removal, block 4

- Из `DataService` удалены Auto Strategy store/history initialization,
  scheduler/task/queue, CRUD/analyze/reconcile/start/cycle методы и служебные
  поля manual execution run. `execution/auto_strategies.py` удалён.
- Исторические runtime-файлы `state/auto_strategies.json` и
  `logs/auto_strategy_history.jsonl` не удалялись и не изменялись.
- Независимые полезные свойства перенесены в
  `execution/execution_invariants.py`: первый незавершенный шаг, fixed-target
  partial reconciliation, dust/minimum tolerance и детерминированный выбор
  единственного кандидата с caller-owned priority.
- Старый trigger-by-static-spread/funding код не перенесён: это гипотеза, а не
  execution-инвариант. Будущие triggers принадлежат versioned policy Strategy
  Lab.
- Архивная инструкция 09 помечена `RETIRED`; её нельзя использовать как
  актуальный operator guide.
- Проверки: Python compile, focused regression `123 passed + 8 subtests`,
  полный suite `792 passed + 8 subtests`.
- Live backend не перезапускался; Pump Live не изменялся.

Следующий шаг: удалить production API/mutation paths Auto Exit и old de-risk,
оставив защитные stop/take/margin-add и технический operator-invoked reduce-only
executor.

### 2026-08-13 — Auto Exit control-surface removal, block 5

- Удалены REST-контракты чтения и изменения Auto Exit: `/api/auto-exit`,
  `/api/auto-exit/defaults`, `/api/auto-exit/rule` и
  `/api/auto-exit/clear-spread-cache`.
- Android больше не получает, не фильтрует и не изменяет Auto Exit. Из приложения
  удалены DTO, API-вызов, фильтр и переключатель правила.
- Явные ручные действия оператора `Add` и `Exit` в карточке Main-позиции сохранены;
  они не являются автономным сокращением позиции.
- `/api/mobile/positions` явно строится без Auto Exit. Исторические state/log-файлы
  этого модуля не удалялись и не переписывались.
- Защитные stop/take, margin control, Grid repair и Pump Live этим блоком не
  изменялись. Live backend не перезапускался.

Следующий шаг: убрать внутренний Auto Exit state-machine и его конфликт с Grid,
переименовать общий lock ручного исполнения и затем отдельным блоком удалить
старый de-risk/hedge-cluster decision layer.

### 2026-08-13 — Auto Exit и position de-risk removal, block 6

- Полностью удалены внутренний Auto Exit state-machine, его state/history
  initialization, специальные execution overrides и устаревшие тесты.
- Общая блокировка параллельных ручных/Grid исполнений переименована в
  `_running_manual_execution`; она сохранена как execution-инвариант.
- Старые сохранённые Auto Exit rules больше не читаются и не могут блокировать
  активацию или переход Grid.
- Удалены hedge-cluster API, autonomous de-risk candidate scoring/cycle,
  outcome tracking и `risk/derisk_manager.py`. Настройки этих модулей при
  загрузке отбрасываются из актуального settings contract.
- Удалён `reduction_candidates` из account payload. Исторические JSON/JSONL
  файлы не удалялись и не изменялись.
- Сохранены protective stop/take, isolated-margin add/release, orphaned
  protective-order cleanup, явные ручные `Add/Exit`, технический reduce-only
  executor и Grid hedge repair.
- Ключевые уроки удалённого кода перенесены в
  `docs/retired_auto_exit_derisk_lessons.md` для будущего Strategy Lab дизайна.
- Проверки: Python compilation, focused active-module regression
  `160 passed + 8 subtests`, полный suite `713 passed + 8 subtests`.

Следующий шаг: удалить legacy Coin Analysis production API/import graph после
проверки миграционных exports, затем разделить крупный `DataService` на активные
account/protection/manual/Grid/read-model сервисы.

### 2026-08-13 — legacy Coin Analysis runtime removal, block 7

- Удалены все production-маршруты `/api/coin/*`, тестовый маршрут
  `/api/manual/test/coin-analysis` и связанный блок Manual Tests.
- Из `DataService` удалены legacy sessions, focus/shortlist collectors, paper
  flow, decision/outcome/replay/review/export, retention и position-watcher.
- Ручные Funding History и Spread Monitor сохранены. Funding History больше не
  делает вторую запись в `state/coin_analysis.db`.
- `state/coin_analysis.db`, исторические данные и offline-пакеты
  `analysis_storage`, `analysis_features`, `analysis_decisions`,
  `analysis_registry` сохранены как read-only/compatibility-источник Strategy
  Lab. Граница миграции описана в `docs/legacy_coin_analysis_migration.md`.
- Удалены только тесты снятого production-runtime; прямые тесты схемы БД,
  feature/decision logic и Strategy Lab оставлены как защита исторических
  данных и воспроизводимости.
- Live backend не перезапускался; Pump Live, Grid и активный Strategy Lab
  collector этим блоком не изменялись.

Следующий шаг: разделить крупный `DataService` на активные
account/protection/manual/Grid/read-model сервисы и измерить повторные API
запросы до изменения общего exchange/cache слоя.

### 2026-08-13 — active-only runtime registry, block 8

- Runtime registry и главная страница теперь перечисляют только актуальные
  компоненты: account monitor, positions market, protective orders, Manual,
  Grid, Pump Live и Strategy Lab.
- Названия Auto Exit, Auto Strategy, position reduction, legacy Coin Analysis
  и legacy candidate discovery полностью удалены из публичного runtime payload
  и интерфейса, а не показываются как постоянно выключенные модули.
- Это изменение представления и статического контракта; фоновые задачи и
  торговое исполнение не менялись.

### 2026-08-13 — autonomous leg rebalance removal, block 9

- Удалён последний autonomous position-reduction путь `auto_rebalance`: его
  scheduler hook, state, настройки и shadow-кандидаты.
- Защитный цикл теперь управляет только stop/take, margin и проверенным orphan
  protective-order cleanup; он не может отправить ордер на сокращение позиции.
- Явные operator actions `Exit` и orphan cleanup, а также Grid hedge repair
  сохранены. Их reduce-only executor не является автономным решением.

### 2026-08-13 — dead market-fetch path cleanup, block 10

- Из `webapp/services.py` удалены неиспользуемые legacy Coin Analysis candle
  fetchers для Bybit, MEXC и CCXT, а также их мёртвые chart/window helpers.
- Эти функции уже не имели production caller после block 7; текущая частота API
  запросов не изменилась. Удаление закрывает альтернативный некэшированный
  путь, который мог быть случайно возвращён при будущих правках.
- Ручные Funding History/Spread Monitor и их адаптерный/cached путь сохранены.

### 2026-08-13 — account startup API deduplication, block 11

- `AccountMonitor` больше не делает немедленный фоновый refresh одновременно с
  обязательным `DataService.startup()` refresh. Первый scheduler tick ждёт
  настроенный account interval.
- На каждом backend restart устранён один полный дублирующий приватный проход
  `balance + positions` по всем включённым биржам.
- Startup по-прежнему fail-fast получает свежие accounts/positions до
  positions-market и protective sync; периодические refresh после старта не
  изменились.

### 2026-08-13 — legacy discovery contract removal, block 12

- Из production `AppSettings` и `/api/settings` удалены `sources`,
  `parser_refresh_seconds` и `exchange_refresh_seconds`; старый settings.json
  не может вернуть их в актуальный контракт.
- Из `DataService` удалены пустой legacy `DataSnapshot`, no-op market refresh,
  старый progress/exchange-status и CLI `webapp.manual_refresh`.
- Dashboard теперь показывает cached account exchange status и account
  timestamp вместо всегда пустого legacy discovery status/snapshot.
- Офлайн `pipeline/` не удалён: его воспроизводимые тесты и исторические
  наработки остаются вне production runtime. Strategy Lab имеет отдельный
  research-only intake.

### 2026-08-13 — account polling single-flight, block 13

- `AccountMonitor` сериализует periodic, forced и protective refresh; совпавшие
  обычные вызовы используют уже полученный snapshot и не дублируют private API sweep.
- Protective refresh после реального margin-action не объединяется с обычным: эта
  повторная проверка сохранена как safety boundary.
- Callback после margin-adjust запускается за пределами single-flight lock, поэтому
  его повторная safety-проверка не может создать deadlock.
- После изменения settings сначала обновляются accounts, затем из нового snapshot
  строится positions-market universe; быстрые повторные saves объединяются.

### 2026-08-13 — current web framework contracts, block 14

- HTML routes переведены на актуальный keyword-based `TemplateResponse` contract.
- Manual payload normalization использует Pydantic v2 `model_dump` и
  `model_fields_set`, с безопасным fallback для v1.
- URL, JSON/HTML contract, trading behavior и startup/shutdown lifecycle не изменены.

### 2026-08-13 — owned application lifecycle, block 15

- FastAPI startup/shutdown переведён с deprecated hooks на единый lifespan.
- Фоновый startup по-прежнему не блокирует HTTP, но теперь задача имеет явного
  owner: shutdown сначала отменяет/дожидается её, затем закрывает Pump lab и `DataService`.
- Регрессия проверяет как обычное завершение, так и cancellation незавершённого startup.

### 2026-08-13 — retired surface residue cleanup, block 16

- Из Android backend-contract README удалён больше не существующий Auto Exit endpoint.
- Из общего CSS удалены селекторы снятой Auto Exit/старой symbol positions table;
  актуальные position-action styles сохранены.
- Regression-файл с полезными общими execution properties переименован из
  `test_auto_strategies.py` в `test_execution_invariants.py`.
- Static cache version поднята до `v2026-08-13-dashboard-v5`.

### 2026-08-13 — Funding History service extraction, block 17

- Ручной Funding History вынесен из `DataService` в `webapp/funding_history.py`:
  в одном модуле теперь находятся history fetch/cache, interval normalization,
  carry windows, pair comparison, timeline и API response assembly.
- `DataService.analyze_funding_history()` оставлен как тонкий compatibility facade;
  HTTP contract и URL не изменились.
- Funding History остаётся operator-invoked: extraction не добавляет background polling.

### 2026-08-13 — Main positions read-model extraction, block 18

- Сборка read-only Main position cards и compact exchange balances вынесена в
  `webapp/main_positions_read_model.py`.
- `DataService.mobile_positions_payload()` теперь только берёт cached account/market
  snapshots и передаёт их чистому builder; новых API-запросов нет.
- Нормализация pair amount, real current/entry notional, funding, liquidation risk,
  multi-leg pair selection и mobile/web contract сохранены.

### 2026-08-13 — manual Spread Monitor service extraction, block 19

- Operator-invoked spread quote path вынесен в `webapp/manual_spread_service.py`;
  он отделён от order-capable `ManualTradeManager`.
- Приоритет источников не изменился: fresh WS orderbook, positions-market cache,
  затем public REST snapshot.
- `DataService.mobile_manual_spread()` и `_mobile_quote_for_exchange()` сохранены
  как compatibility facades, поэтому Grid analysis и API не меняются.
- Новых recurring tasks и API-запросов нет.

### 2026-08-13 — migrated runtime deployment and Pump resume, block 20

- После блоков 17–19 полный regression suite прошёл: `694 passed`.
- Backend безопасно остановлен и поднят каноническими Windows-скриптами; страницы
  `/`, `/funding-history` и `/spread-monitor` после запуска отвечают `HTTP 200`.
- До перезапуска Pump Live был явно disarmed. После запуска ledger ownership и две
  биржевые позиции восстановились; обычный preflight ожидаемо отметил только
  `pump_live_subaccount_has_existing_positions`.
- По явному разрешению оператора выполнен `ARM PUMP LIVE 3000`. Активный профиль —
  `v3_3000_pool600`: капитал `$3000`, deployable `$2400`, reserve `$600`, максимум
  четыре позиции, полный slot `$600`, первая нога нового входа `$120`.
- Три последовательных monitor cycle после ARM сохранили `armed=true`, живой monitor,
  отсутствие blocked/last/transient errors и две защищённые позиции `ACEUSDT` и
  `BMTUSDT`. У обеих есть exchange stop/TP, risk level `ok`; следующие лестницы
  остаются `deferred_distance`, то есть на бирже заранее не занимают маржу.
- Strategy Lab preflight collector не перезапускался и не изменялся; его launcher и
  Python worker продолжают прежний research-only 24h запуск.

Следующая граница рефакторинга: перед разделением Grid/Pump state machines сначала
зафиксировать persisted-state/replay contract. Торговую логику этих модулей не
переносить механически без воспроизводимого restart/recovery теста.

### 2026-08-13 — persisted-state/replay safety boundary, block 21

- Добавлен общий чистый контракт `execution/state_replay_contract.py` для Pump Live
  и Grid: schema validation, unique ownership, durable SHA-256 fingerprint,
  fail-closed restart projection и явный exchange-reconciliation plan.
- Pump snapshot не является разрешением повторить ордер: cold start всегда сбрасывает
  ARM и pending signals, сохраняет позиции/лестницы/risk policy/capital state и требует
  заново подтвердить ownership и protection на Bybit.
- Grid snapshot сохраняет полное правило и active execution reference, но process-local
  Manual run не реконструируется. После рестарта незавершённый execution может только
  пройти существующую сверку свежих long/short quantities; его id не доказывает fill.
- Grid получил явную схему `auto_arb_grid_state_v1`; старый schema-less v1 читается с
  migration warning и получает схему при следующем обычном сохранении.
- Pump status и Grid payload публикуют read-only `state_contract`; решения, ордера,
  scheduler cadence и exchange polling этим не изменены.
- Добавлен операторский read-only CLI `scripts/state_replay_audit.py` и техническое
  описание `docs/persisted_state_replay_contract.md`.
- Аудит текущих файлов: Pump valid с двумя ownership keys (`ACEUSDT`, `BMTUSDT`),
  Grid valid/empty с единственным legacy-schema warning.

Следующий шаг после полной регрессии: выделять state machine сначала на Grid, потому
что у него нет текущих правил/позиций. Pump переносить только после того, как новый
компонент пройдёт те же snapshot fingerprint и exchange resume fixtures.

### 2026-08-13 — state contract deployment, block 22

- Финальная регрессия контракта прошла: `704 passed`, `8 subtests passed`.
- Pump был штатно disarmed, backend перезапущен каноническими Windows-скриптами и
  после ownership-aware resume снова явно вооружён `ARM PUMP LIVE 3000`.
- Runtime API подтвердил `state_contract.valid=true`, два уникальных ownership key,
  active policy `v3_3000_pool600`, капитал `$3000` и slot `$600`.
- Grid runtime загрузил `auto_arb_grid_state_v1`, contract valid, rules/active
  executions `0`; legacy warning исчез после нормализации loader-ом.
- Три последовательных Pump monitor cycle после ARM: `armed=true`, monitor alive,
  no blocked/last/transient/position errors, protection issues `0`; ACE/BMT остались
  открыты и их следующие лестницы остались `deferred_distance`.
- Tailscale продолжает `Running`, Strategy Lab launcher/worker не перезапускались.

Развёрнутый контракт является read-only guard/diagnostic. Он не меняет entry/exit,
margin, ladder, Grid transition или exchange polling.

### 2026-08-13 — Grid state owner extraction, block 23

- Versioned Grid snapshot, JSONL history и mutation lock вынесены из `DataService`
  в `execution/grid_state.py::GridStateRepository`.
- Старые `_auto_arb*` поля пока являются aliases того же repository state/lock/store,
  поэтому transition/recovery logic и существующие тестовые hooks не раздвоены.
- Schema-less v1 нормализуется в памяти и при обычном save получает
  `auto_arb_grid_state_v1`.
- Повреждённый JSON больше не превращается молча в пустой Grid: repository публикует
  invalid `state_contract`, блокирует overwrite и сохраняет исходный файл для разбора.
- Логический duplicate ownership не блокирует save: существующий Grid cycle должен
  иметь возможность записать fail-closed отключение legacy-конфликта. Focused test
  подтвердил именно этот recovery path.
- Grid decision math, scheduler, exchange refresh, order submission и reconciliation
  остаются в `DataService`; это перенос ownership/storage, а не торговой логики.

### 2026-08-13 — Grid pure invariants extraction, block 24

- В `execution/auto_arb_grid.py` собраны чистые инварианты Grid: symbol/exchange
  ownership, conflict detection/message, completion/hedge tolerance, dust
  classification, flat-repair reset, level quantity matching и partial adoption.
- `DataService` сохраняет прежние `_auto_arb_*` фасады, но они делегируют чистому
  execution-domain модулю. HTTP/API и все callers остаются совместимыми.
- Добавлены прямые unit tests для CCXT symbol `TUT/USDT:USDT`, alias `kukoin -> kucoin`
  и прежней 1% quantity tolerance; Grid-focused regression: `64 passed`.
- Async exchange refresh, risk-limit preflight, execution/reconciliation и scheduler
  остаются на прежнем месте. Следующий перенос — transition reducer — будет отдельным
  блоком с golden input/output fixtures.

### 2026-08-13 — Grid owner/invariants deployment, block 25

- Full regression после блоков 23–24: `709 passed`, `8 subtests passed`.
- Перед deployment подтверждены Manual running `0`, Grid Live/active `0`, Pump contract
  valid и две защищённые позиции. Pump штатно disarmed.
- Backend перезапущен каноническими Windows-скриптами; после restart Grid repository
  публикует `auto_arb_grid_state_v1`, contract valid, issues `0`, rules `0`.
- Pump ownership-aware resume принят через `ARM PUMP LIVE 3000`; active policy
  `v3_3000_pool600`, капитал `$3000`, slot `$600`.
- Три последовательных monitor cycle: Pump armed, monitor alive, contract valid,
  ACE/BMT open, protection issues `0`, no blocked/last/transient/position errors.
- Tailscale остался `Running`; Strategy Lab процесс этим deployment не изменялся.

### 2026-08-13 — Grid confirmation reducer, block 26

- Post-decision confirmation вынесен в чистый
  `execution.auto_arb_grid.apply_grid_decision_confirmation`: idle/partial status,
  confirm samples, shadow apply, Live queue и entry risk cooldown.
- Reducer не вызывает биржи и не запускает order execution; для Live он возвращает
  только tuple `(rule_id, action, from_level, to_level)`, который исполняет прежний
  async boundary в `DataService`.
- `tests/fixtures/grid_transition_confirmation_v1.json` фиксирует golden input/output
  для idle flat, partial frontier wait, confirmed Live queue и fail-closed cooldown.
- Частичные reversal/rebase решения пока остаются в `DataService`; их переносить
  следующим отдельным reducer-блоком после фиксации аналогичных golden fixtures.

### 2026-08-13 — Grid pending completion reducer, block 27

- Терминальное завершение partial transition вынесено в чистый
  `complete_pending_grid_transition`: completion tolerance, non-closeable dust,
  live level/status reset, retry timestamp, deterministic decision и history event.
- Material remainder возвращает `None` и не меняет правило; frontier continuation,
  reversal и rebase остаются в прежнем async cycle.
- Прямые тесты фиксируют оба исхода: завершение остатка в пределах 1% и сохранение
  partial state при существенном остатке.

### 2026-08-13 — Grid reducers deployment, block 28

- Full regression confirmation + pending reducers: `715 passed`, `12 subtests passed`.
- Deployment preflight: Manual running `0`, Grid Live/active `0`, Pump contract valid,
  две позиции без position errors; Pump штатно disarmed.
- После канонического restart Grid contract valid/rules `0`, Pump ownership восстановлен
  и явно принят `ARM PUMP LIVE 3000` с policy `v3_3000_pool600`.
- Три monitor cycle после ARM: armed/monitor alive, Pump contract valid, ACE/BMT open,
  protection issues `0`, no blocked/last/transient/position errors.
- Tailscale `Running`; два Strategy Lab Python process (launcher + worker) продолжают
  прежний research-only запуск.

Следующий безопасный Grid-блок: зафиксировать golden fixtures и вынести partial
enter/exit reversal, reversal cancellation и fresh-position rebase. До этого async
exchange refresh и order boundary не переносить.

### 2026-08-13 — Grid partial reversal reducer, block 29

- Frontier continuation, partial enter/exit reversal, reversal cancellation и
  fresh-position rebase вынесены в
  `execution.auto_arb_grid.reduce_partial_grid_transition`.
- Reducer работает только с переданным состоянием и числами свежего snapshot:
  он не обращается к биржам, не запускает Manual execution и не повторяет ордер.
  Async refresh, risk preflight, submission и reconciliation остаются в
  `DataService`.
- `tests/fixtures/grid_partial_transition_v1.json` фиксирует пять replay-сценариев:
  продолжение partial exit на frontier, разворот partial exit, очистку zero-fill
  stale exit, отмену ещё не исполненного разворота и rollback partial enter.
- Старый inline-блок удалён из цикла, а результат reducer проходит прежнюю
  confirmation/Live queue границу. Сервисные регрессии подтверждают совместимость
  с существующими execution/recovery сценариями.

Следующая граница после deployment: отдельно вынести построение нового pending
transition и execution-result reconciliation. Биржевой I/O и фактический запуск
ордеров пока оставить в `DataService`.

### 2026-08-13 — Grid partial reversal deployment, block 30

- Focused regression после интеграции reducer: `58 passed`, `9 subtests passed`;
  полный проектный прогон: `716 passed`, `17 subtests passed`, одна известная
  `aiohttp` deprecation warning.
- Deployment preflight подтвердил Manual running `0`, Grid rules/active `0`,
  валидные Pump/Grid state contracts и `protection_issues=0`. ACE/BMT имели две
  биржевые позиции и четыре exchange-side TP/SL; Pump был штатно disarmed.
- Первый canonical launcher после остановки завершился без listener. Новые входы
  оставались disarmed, биржевые TP/SL не затрагивались. Повторный запуск штатно
  восстановил API и страницы `/`/`/positions` с HTTP 200.
- Restart recovery сопоставил ACE/BMT и четыре защитных ордера без ошибок. По
  ранее выданному разрешению принят `ARM PUMP LIVE 3000`: policy
  `v3_3000_pool600`, capital `$3000`, slot `$600`.
- Три последующих monitor cycle сохранили Pump armed/monitor alive, contract valid,
  две позиции, четыре защитных ордера, отсутствие blocked/last/transient ошибок,
  freeze и protection issues. Минимальный observed liquidation buffer был
  `32.52%`.
- Tailscale остаётся `Running`; два процесса Strategy Lab preflight
  (launcher + worker) не перезапускались и продолжают research-only сбор.

Следующая безопасная граница: вынести создание/нормализацию pending transition
как отдельный reducer, затем отдельно зафиксировать execution-result
reconciliation replay. До golden-тестов не переносить биржевой I/O или запуск
Manual execution из `DataService`.

### 2026-08-13 — Grid pending-transition builder, block 31

- Создание и нормализация нового/повторного pending transition вынесены в
  `execution.auto_arb_grid.build_grid_pending_transition`.
- Builder различает exact continuation и другой transition, восстанавливает
  missing origin quantity, сохраняет material remainder и применяет
  `rebase_from_positions` только к свежему hedged quantity.
- `tests/fixtures/grid_pending_transition_v1.json` фиксирует пять golden cases:
  новый enter, новый exit с фактическим quantity delta, продолжение partial exit,
  fresh-position rebase и запрет наследования quantities от другого transition.
- Position refresh по-прежнему выполняется до builder; completion tolerance,
  KuCoin risk-limit preflight, payload, Manual execution, history и reconciliation
  остаются в `DataService`. Торговая последовательность и thresholds не менялись.

Следующая отдельная граница — execution-result reconciliation replay. До её
golden fixtures не объединять builder с запуском или результатом Manual execution.

### 2026-08-13 — Grid pending builder deployment, block 32

- Focused regression: `59 passed`, `14 subtests passed`; полный проектный прогон:
  `717 passed`, `22 subtests passed`, одна известная `aiohttp` warning.
- Перед restart подтверждены Manual `0`, Grid rules/active `0`, валидные state
  contracts, ACE/BMT, четыре exchange-side TP/SL и `protection_issues=0`.
- Pump штатно disarmed, canonical launcher с первой попытки восстановил API и
  главную страницу. Restart recovery сохранил две позиции/четыре защитных ордера
  без contract или position errors.
- По действующему разрешению снова принят `ARM PUMP LIVE 3000`: policy
  `v3_3000_pool600`, capital `$3000`, slot `$600`.
- Три monitor cycle после ARM: armed/monitor alive, contract valid, blocked/error/
  transient/freeze отсутствуют, protection issues `0`; минимальный observed
  liquidation buffer `33.62%`.

Следующий блок начинать с классификации execution-result reconciliation и golden
replay; до этого не переносить async worker lookup, exchange quantity refresh или
реальные hedge-repair/order actions.

### 2026-08-13 — Grid execution-result reducer, block 33

- Accounting завершившегося transition worker вынесен в
  `execution.auto_arb_grid.reduce_grid_transition_execution`.
- Reducer получает свежие `hedged_qty`/`imbalance_qty`, считает observed fill,
  material remainder, tolerance и deterministic status/event. Результат worker
  сам по себе не считается доказательством fill.
- Зафиксированы шесть golden replay cases в
  `tests/fixtures/grid_execution_result_v1.json`: complete, partial,
  non-closeable dust, balance block, generic retry и leg imbalance.
- При imbalance reducer только возвращает `repair_required=true`. Fresh exchange
  refresh и реальный reduce-only hedge repair остаются async-границей
  `DataService`; поиск process-local Manual run также не переносился.
- Существующие service tests подтверждают прежние статусы, quantity accounting,
  retry delays и запуск repair worker только после fresh position evidence.

Следующая граница после deployment: отдельно вынести reconciliation исчезнувшего
process-local execution после restart. Missing worker id нельзя считать ни fill,
ни ошибкой без свежих exchange quantities.

### 2026-08-13 — Grid execution-result deployment, block 34

- Focused regression: `60 passed`, `20 subtests passed`; полный прогон:
  `718 passed`, `28 subtests passed`, одна известная `aiohttp` warning.
- Preflight: Manual `0`, Grid rules/active `0`, Pump/Grid contracts valid,
  ACE/BMT и четыре exchange-side TP/SL, protection issues `0`.
- Pump штатно disarmed. Первый canonical launcher завершился без listener; TP/SL
  оставались на бирже. Второй запуск восстановил HTTP 200, две позиции, четыре
  ордера и contract/protection без ошибок.
- Принят `ARM PUMP LIVE 3000`: policy `v3_3000_pool600`, capital `$3000`, slot
  `$600`. Три следующих monitor cycle сохранили armed/monitor alive, отсутствие
  blocked/error/transient/freeze и protection issues `0`; минимальный observed
  liquidation buffer `32.37%`.

Следующая граница: pure restart reconciliation для missing process-local worker.
Reducer должен принимать только persisted rule + fresh quantities и возвращать
balanced/repair/deferred решение; запуск repair остаётся в `DataService`.

### 2026-08-13 — Grid missing-worker restart reducer, block 35

- Restart reconciliation исчезнувшего process-local worker вынесен в
  `execution.auto_arb_grid.reduce_missing_grid_execution`.
- Только fresh exchange quantities разрешают очистить persisted
  `active_execution_id`: balanced legs возвращают partial/monitoring, flat repair
  сбрасывает stale level/transition, imbalance запрашивает hedge repair.
- При position refresh failure execution reference сохраняется, статус остаётся
  `waiting_reconcile`; persisted worker id не считается ни fill, ни terminal error.
- `tests/fixtures/grid_missing_execution_v1.json` фиксирует balanced partial,
  flat repair, imbalanced repair-required и refresh-deferred restart cases.
- Async exchange refresh, history append и фактический запуск reduce-only repair
  остаются в `DataService`.

Следующая безопасная граница после deployment: выделить reducer результата самого
hedge-repair worker. Затем Grid decision/recovery state machine будет достаточно
изолирована для отдельного orchestration owner без переноса order I/O.

### 2026-08-13 — Grid missing-worker deployment, block 36

- Focused regression: `61 passed`, `24 subtests passed`; полный прогон:
  `719 passed`, `32 subtests passed`, одна известная `aiohttp` warning.
- Preflight был чистым: Manual/Grid `0`, два Pump ownership, четыре TP/SL,
  state contracts valid и protection issues `0`. Pump штатно disarmed.
- Первый launcher завершился без listener; повторный canonical start восстановил
  HTTP/API, ACE/BMT и четыре защитных ордера без ошибок.
- Принят `ARM PUMP LIVE 3000`; три monitor cycle сохранили armed/monitor alive,
  отсутствие blocked/error/transient/freeze и protection issues `0`. Минимальный
  observed liquidation buffer `32.51%`.

Следующая граница — reducer результата hedge-repair worker; биржевой refresh и
реальный reduce-only cleanup по-прежнему должны оставаться в `DataService`.

### 2026-08-13 — Grid hedge-repair result reducer, block 37

- Результат reduce-only repair worker вынесен в
  `execution.auto_arb_grid.reduce_grid_hedge_repair_execution`.
- Успех определяется fresh imbalance, а не статусом worker: balanced legs
  возвращают partial/monitoring либо делают flat-reset; оставшийся imbalance
  переводит правило в `hedge_repair_retry`.
- Автоматический повтор разрешён только для завершённого cleanup или отсутствия
  явной ошибки. Явная ошибка сохраняется как blocked reason до следующего цикла.
- Четыре golden cases в `tests/fixtures/grid_hedge_repair_result_v1.json`
  фиксируют partial resume, flat reset, повторный cleanup и error wait.
- Reducer возвращает только `retry_repair`; реальный async cleanup остаётся в
  `DataService`.

После deployment следующий архитектурный шаг — собрать уже выделенные Grid
reducers под отдельным orchestration owner, сохранив `DataService` как I/O port.

### 2026-08-13 — Grid hedge-repair reducer deployment, block 38

- Focused regression: `62 passed`, `28 subtests passed`; полный прогон:
  `720 passed`, `36 subtests passed`, одна известная `aiohttp` warning.
- Preflight подтвердил Manual/Grid `0`, валидные contracts, ACE/BMT, четыре
  exchange-side TP/SL и protection issues `0`; Pump был штатно disarmed.
- Первый launcher снова завершился без listener; повторный canonical start
  восстановил API, две позиции и четыре защитных ордера без ошибок.
- Принят `ARM PUMP LIVE 3000`. Три monitor cycle сохранили armed/monitor alive,
  отсутствие blocked/error/transient/freeze и protection issues `0`; минимальный
  observed liquidation buffer `33.72%`.

Следующий блок: спроектировать `GridStateMachine`/orchestration owner вокруг уже
выделенных pure reducers. Начать с read-only facade и parity tests; не переносить
exchange/order I/O до доказанной эквивалентности полного cycle decision output.

### 2026-08-13 — GridStateMachine quote-cycle facade, block 39

- Добавлен чистый orchestration owner
  `execution.grid_state_machine.GridStateMachine` для одного quote cycle.
- Facade объединяет waiting-data, pending completion, partial reversal, обычное
  grid decision и confirmation; возвращает только event и optional Live intent.
- `tests/fixtures/grid_quote_cycle_v1.json` фиксирует шесть полных траекторий.
  Parity test сравнивает весь mutated rule и result facade с сохранённой legacy
  последовательностью.
- `DataService` делегирует facade готовые spreads, но сохраняет exchange fetch,
  state lock/save, history append, risk preflight и запуск Manual execution.
- Неиспользовавшиеся промежуточные расчёты pending quantities в async cycle
  удалены; thresholds и execution ordering не менялись.

Следующая граница после deployment: перенести orchestration методов start/reconcile
в отдельный owner через явные I/O intents, начиная с parity-only adapter. Реальные
биржевые вызовы до отдельной regression границы не переносить.

### 2026-08-13 — GridStateMachine facade deployment, block 40

- Parity/focused regression: `63 passed`, `28 subtests passed`; полный прогон:
  `721 passed`, `36 subtests passed`, одна известная `aiohttp` warning.
- Preflight: Manual/Grid `0`, state contracts valid, ACE/BMT, четыре TP/SL и
  protection issues `0`; Pump штатно disarmed.
- Первый launcher завершился без listener, повторный canonical start восстановил
  API и exchange ownership/protection без ошибок. Принят `ARM PUMP LIVE 3000`.
- Первый трёхточечный monitor script исчерпал собственный 60s timeout на третьем
  запросе; backend error не зафиксирован. Повторный запуск с 90s budget подтвердил
  три независимых `last_cycle_at_ms`.
- Во всех трёх снимках: armed/monitor alive, два ownership, четыре TP/SL,
  blocked/error/transient/freeze отсутствуют, protection issues `0`; минимальный
  observed liquidation buffer `33.28%`.

Следующая граница: parity-only orchestration adapter для start/reconcile I/O
intents. До подтверждения output parity реальные exchange/manual вызовы оставить
в `DataService`.

### 2026-08-13 — Grid transition-start parity planner, block 41

- В `GridStateMachine` добавлен dormant `plan_transition_start`, который строит
  pure I/O intent после уже выполненного fresh position refresh.
- Intent содержит submit qty, persisted transition, position target, entry risk
  target и completion tolerance; биржевой/risk/manual вызов не выполняется.
- Пять parity fixtures фиксируют новый enter, exit из фактического quantity,
  partial continuation, fresh rebase и no-order outcome внутри tolerance.
- Production `_start_auto_arb_live_transition` в этом блоке не переключался на
  planner: это сознательная parity-only граница без runtime поведения и restart.

Следующий шаг: подключить planner только к deterministic части start method,
оставив refresh/risk/manual execution в `DataService`, затем выполнить полный
deployment gate.

### 2026-08-13 — Grid transition-start planner integration, block 42

- `_start_auto_arb_live_transition` подключён к
  `GridStateMachine.plan_transition_start` после прежнего fresh position refresh.
- Предварительная проверка level range остаётся до exchange refresh; порядок
  fail-fast ошибок и лишних API-вызовов не изменился.
- Intent теперь является единым источником submit qty, pending transition,
  position target, completion tolerance и KuCoin entry risk target.
- Completion внутри tolerance по-прежнему завершает state без Manual order;
  refresh, risk-limit preflight, payload и реальный Manual execution остаются в
  `DataService`.
- Focused parity/service regression после wiring: `64 passed`, `28 subtests`.

Следующая граница после deployment: спроектировать reconcile I/O adapter вокруг
уже готовых execution/missing/repair reducers. Сначала только parity контракты;
exchange refresh и запуск repair не переносить одновременно.

### 2026-08-13 — Grid transition-start planner deployment, block 43

- Focused parity/service regression: `64 passed`, `28 subtests passed`; полный
  прогон: `722 passed`, `36 subtests passed`, одна известная `aiohttp` warning.
- Live preflight подтвердил Manual/Grid `0`, валидные Grid/Pump state contracts,
  две owned и две exchange Pump-позиции, четыре exchange-side TP/SL и
  `protection_issues=0`; Pump штатно disarmed перед рестартом.
- Первый canonical launcher завершился без listener, повторный запуск восстановил
  API `200`, ownership и protection без ошибок. После cold restart Pump оставался
  disarmed с живым monitor до явного разрешения.
- Принят `ARM PUMP LIVE 3000`, active policy `v3_3000_pool600`. Три независимых
  monitor cycle сохранили armed/monitor alive, две позиции, четыре TP/SL, отсутствие
  blocked/error/transient/freeze и `protection_issues=0`; минимальный наблюдавшийся
  liquidation buffer `34.62%`.

Следующая безопасная граница: parity-only reconcile I/O adapter для уже выделенных
execution/missing/repair reducers. Сначала зафиксировать intents и golden fixtures;
production exchange refresh и запуск repair в этом блоке не переносить.

### 2026-08-13 — Grid reconcile I/O parity planner, block 44

- В `GridStateMachine` добавлен dormant `plan_execution_reconcile_io`: он не
  обращается к биржам и не мутирует persisted rule или process-local Manual run.
- Planner различает отсутствие active execution, потерянный после restart worker,
  ещё работающий worker и terminal execution. Для terminal результата он явно
  выбирает существующий reducer: missing, transition, hedge repair либо balanced
  settle без pending transition.
- Шесть golden cases в `grid_execution_reconcile_io_v1.json` фиксируют routing и
  необходимость fresh position refresh; running worker не вызывает refresh.
- Production `_reconcile_auto_arb_execution` в этом блоке не переключался:
  exchange I/O, lock/save/history и запуск repair остаются прежними.
- Focused regression: `65 passed`, `28 subtests passed`; полный прогон:
  `723 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующий шаг: подключить planner только к верхнему routing production reconcile,
оставив reducers, exchange refresh, state mutation/save и repair launch на прежних
местах. После focused/full parity выполнить отдельный deployment gate.

### 2026-08-13 — Grid reconcile routing integration, block 45

- `_reconcile_auto_arb_execution` использует
  `plan_execution_reconcile_io` для missing/running/terminal routing.
- Перед terminal reduce planner повторно применяется к актуальному rule внутри
  lock, поэтому приоритет repair над pending transition и balanced-settle берутся
  из одного контракта.
- Process-local Manual lookup, fresh exchange quantities, active-field cleanup,
  существующие reducers, state save/history и последующий запуск hedge repair не
  переносились и не меняли порядок.
- Focused service/parity regression: `65 passed`, `28 subtests passed`; полный
  прогон: `723 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующая граница после deployment: выделить чистый post-refresh reconcile reducer
facade, который объединит уже существующие missing/transition/repair reducers.
Сначала parity fixtures; state save/history и repair launch оставить в I/O слое.

### 2026-08-13 — Grid reconcile routing deployment, block 46

- Перед рестартом live preflight подтвердил Manual/Grid `0`, валидные state
  contracts, две Pump ownership/exchange позиции, четыре TP/SL и
  `protection_issues=0`; Pump был штатно disarmed.
- Первый canonical launcher завершился без listener, повторный поднял API `200`.
  Cold-restart recovery сохранил две позиции и четыре exchange-side защиты;
  monitor был жив, входы оставались disarmed до явного ARM.
- Принят `ARM PUMP LIVE 3000`, policy `v3_3000_pool600`. Три разных
  `last_cycle_at_ms` сохранили armed/monitor alive, ownership `2/2`, TP/SL `4/4`,
  отсутствие blocked/error/transient/freeze и `protection_issues=0`.
- Минимальный наблюдавшийся liquidation buffer `34.83%`; Grid state contract
  валиден, активных Grid rules нет.

Следующая безопасная граница остаётся parity-only: pure post-refresh reconcile
facade над existing reducers. До доказанной эквивалентности не переносить
state persistence, history append или запуск hedge repair.

### 2026-08-13 — Grid post-refresh reconcile parity facade, block 47

- В `GridStateMachine` добавлен dormant pure facade
  `reduce_execution_reconcile_after_refresh`.
- Он получает уже завершённый position refresh, очищает либо восстанавливает
  active execution state, записывает observed execution и маршрутизирует прежние
  transition/repair reducers; exchange I/O не выполняет.
- Результат содержит только completion, необходимость последующего repair и event
  fragment. Save/history/repair launch остаются вне facade.
- Семь golden trajectories фиксируют partial/imbalanced transition, balanced и
  retry repair, обычный и paused settle, а также refresh failure с восстановлением
  persisted active snapshot. Сравниваются весь mutated rule и весь result прежней
  последовательности.
- Focused regression: `66 passed`, `28 subtests passed`; полный прогон:
  `724 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующий шаг: подключить facade внутри уже существующего lock после position
refresh. Event envelope, state save/history и фактический запуск repair оставить в
`DataService`, затем повторить полный deployment gate.

### 2026-08-13 — Grid post-refresh reconcile integration, block 48

- `_reconcile_auto_arb_execution` после прежнего fresh position refresh делегирует
  только in-lock state reduction в
  `GridStateMachine.reduce_execution_reconcile_after_refresh`.
- Facade возвращает event fragment, completion и repair intent; `DataService`
  по-прежнему формирует envelope, выполняет lock/save/history и только после lock
  запускает hedge repair.
- Process-local Manual lookup и exchange refresh не перемещались. Refresh failure
  восстанавливает тот же persisted active snapshot и остаётся fail-closed.
- Неиспользуемые прямые imports transition/repair reducers из service удалены.
- Focused regression после wiring: `66 passed`, `28 subtests passed`; полный
  прогон: `724 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующая граница после deployment: отдельно спроектировать pure hedge-repair start
intent (within-tolerance settle, non-closeable dust и cleanup request). До parity
fixtures не переносить Manual analyze/cleanup или exchange-side execution.

### 2026-08-13 — Grid post-refresh reconcile deployment, block 49

- Live preflight: Manual/Grid `0`, оба state contracts valid, две Pump
  ownership/exchange позиции, четыре TP/SL, `protection_issues=0`; Pump штатно
  disarmed перед restart.
- Первый canonical launcher снова завершился без listener; повторный поднял API
  `200`. Cold-restart recovery сохранил ownership `2/2`, protection `4/4`, живой
  monitor и disarmed entries до явного разрешения.
- Принят `ARM PUMP LIVE 3000`, policy `v3_3000_pool600`. Три независимых cycle
  сохранили armed/monitor alive, две позиции, четыре защиты и отсутствие
  blocked/error/transient/freeze; `protection_issues=0`.
- Минимальный наблюдавшийся liquidation buffer `33.04%`; Grid contract валиден,
  активных Grid rules нет.

Следующая безопасная граница: dormant pure hedge-repair start intent. Сначала
зафиксировать settle/dust/cleanup routing; Manual analyze и реальное cleanup I/O
не подключать до отдельного regression checkpoint.

### 2026-08-13 — Grid hedge-repair start parity planner, block 50

- Добавлен dormant pure `GridStateMachine.plan_hedge_repair_start`.
- По fresh long/short/hedged quantities он выбирает четыре outcome:
  `settle_within_tolerance`, `analyze_cleanup`, `settle_non_closeable_dust` или
  `submit_cleanup`.
- Intent фиксирует cleanup venue/side, направление закрытия, tolerance, exchange
  minimum и точный прежний payload для `manual_orphan_cleanup`.
- Planner не мутирует rule/quantities/preflight и не вызывает Manual/exchange API.
  Пять golden cases покрывают balanced legs, long/short surplus, dust и submit.
- Focused regression: `67 passed`, `28 subtests passed`; полный прогон:
  `725 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующий шаг: подключить planner к routing `_start_auto_arb_hedge_repair`, оставив
settle/dust state mutation, Manual analyze, cleanup submission, save/history и
worker ownership в `DataService`. После parity — отдельный deployment gate.

### 2026-08-13 — Grid hedge-repair start integration, block 51

- `_start_auto_arb_hedge_repair` вызывает planner до Manual preflight и повторно с
  полученным preflight для выбора dust либо cleanup submission.
- Tolerance, imbalance, venue/side, analysis request и cleanup payload теперь
  берутся из одного tested intent.
- Within-tolerance и non-closeable-dust state mutation, Manual analyze, реальный
  `manual_orphan_cleanup`, worker ownership, lock/save/history не переносились.
- Ошибка Manual analysis по-прежнему превращается в preflight `errors` и идёт по
  прежнему cleanup path; fail-open/fail-closed семантика не менялась.
- Focused regression: `67 passed`, `28 subtests passed`; полный прогон:
  `725 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующая граница после deployment: pure reducer для within-tolerance/dust settle,
оставляющий analyze/cleanup I/O в `DataService`. Сначала golden parity; не
объединять его с worker submission в одном изменении.

### 2026-08-13 — Grid hedge-repair start deployment, block 52

- Preflight подтвердил Manual/Grid `0`, valid Grid/Pump contracts, две Pump
  ownership/exchange позиции, четыре TP/SL и `protection_issues=0`; Pump штатно
  disarmed до restart.
- Первый canonical launcher завершился без listener, повторный восстановил API
  `200`. Cold restart сохранил ownership/protection, monitor alive и disarmed
  entries до явного ARM.
- Принят `ARM PUMP LIVE 3000`, policy `v3_3000_pool600`. Три разных cycle сохранили
  armed/monitor alive, позиции `2/2`, защиты `4/4`, отсутствие
  blocked/error/transient/freeze и `protection_issues=0`.
- Минимальный observed liquidation buffer `35.08%`; Grid contract valid, активных
  Grid rules нет.

Следующая безопасная граница: dormant pure reducer для результатов
`settle_within_tolerance` и `settle_non_closeable_dust`. Сначала parity fixtures;
Manual analysis, cleanup submission и worker ownership не переносить вместе с ним.

### 2026-08-13 — Grid hedge-repair settle parity reducer, block 53

- Добавлен dormant pure `GridStateMachine.reduce_hedge_repair_settle` для двух
  no-order outcome: imbalance within tolerance и exchange-minimum dust.
- Reducer обрабатывает partial/completed transition, worker dust, flat reset,
  active-field cleanup и retry timestamp, возвращая event fragment без I/O/save.
- Шесть golden trajectories сравнивают весь mutated rule и result с прежней
  production-последовательностью.
- Fixture подтвердил важную safety-семантику: `to_level=0` сам по себе не делает
  Grid плоским, если fresh `hedged_qty` остаётся положительным; сохраняется
  `live_level=1 / monitoring`.
- Focused regression: `68 passed`, `28 subtests passed`; полный прогон:
  `726 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующий шаг: подключить reducer только внутри двух прежних lock-блоков settle,
оставив event envelope/save/history и весь Manual cleanup path в `DataService`.

### 2026-08-13 — Grid hedge-repair settle integration, block 54

- Два прежних in-lock branch (`within_tolerance` и `non_closeable_dust`) теперь
  делегируют state mutation в `reduce_hedge_repair_settle`.
- Reducer возвращает event fragment; `DataService` добавляет `rule_id/ts`, ставит
  `updated_at`, сохраняет state и пишет history как прежде.
- Manual analysis, cleanup payload submission и worker ownership не переносились.
- Focused regression: `68 passed`, `28 subtests passed`; полный прогон:
  `726 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующая граница после deployment: dormant pure reducer результата cleanup worker
start (`execution_id` получен либо worker busy). Сначала parity fixture; не
переносить сам `manual_orphan_cleanup` вместе с state mutation.

### 2026-08-13 — Grid hedge-repair settle deployment, block 55

- Preflight: Manual/Grid `0`, valid Grid/Pump contracts, Pump ownership/exchange
  `2/2`, protection `4/4`, `protection_issues=0`; Pump штатно disarmed.
- Первый canonical launcher завершился без listener; повторный поднял API `200`.
  Cold restart сохранил позиции/защиту, monitor alive и disarmed entries до ARM.
- Принят `ARM PUMP LIVE 3000`, policy `v3_3000_pool600`. Три независимых cycle
  сохранили armed/monitor alive, отсутствие blocked/error/transient/freeze и
  `protection_issues=0`.
- Минимальный observed liquidation buffer `35.05%`; Grid contract valid, активных
  Grid rules нет.

Следующая безопасная граница: dormant pure reducer результата cleanup worker start
для веток `execution_id` и `worker_busy`. Сначала parity fixtures; сам
`manual_orphan_cleanup` и history append оставить в I/O слое.

### 2026-08-13 — Grid hedge-repair worker-start parity reducer, block 56

- Добавлен dormant pure `reduce_hedge_repair_worker_start` для результата уже
  выполненного `manual_orphan_cleanup`.
- Реальный `execution_id` получает repair ownership, сохраняет fresh start hedged
  qty и переводит rule в `repairing_hedge`.
- Явная worker error либо пустой результат сохраняют прежний retry и fallback
  `hedge_repair_worker_busy`.
- Reducer возвращает event fragment; Manual call/save/history не выполняет.
  Три golden cases сравнивают весь mutated rule и event.
- Focused regression: `69 passed`, `28 subtests passed`; полный прогон:
  `727 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующий шаг: подключить reducer только после прежнего `manual_orphan_cleanup`,
оставив submission, lock/save, `updated_at` и history envelope в `DataService`.

### 2026-08-13 — Grid hedge-repair worker-start integration, block 57

- После прежнего `manual_orphan_cleanup` его result передаётся в
  `reduce_hedge_repair_worker_start` внутри существующего lock.
- Reducer обновляет ownership/retry state и возвращает event fragment.
- Submission, `updated_at`, state save, `rule_id/ts` envelope и history append
  остаются в `DataService`; I/O ordering не менялся.
- Удалены ставшие лишними промежуточные cleanup/imbalance переменные service.
- Focused regression: `69 passed`, `28 subtests passed`; полный прогон:
  `727 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующая граница после deployment: оценить оставшиеся Grid state mutations в
`DataService` и выбрать минимальный parity-only блок; сначала inventory, без
одновременного переноса новых exchange/manual операций.

### 2026-08-13 — Grid hedge-repair worker-start deployment, block 58

- Preflight подтвердил Manual/Grid `0`, valid contracts, Pump ownership/exchange
  `2/2`, protection `4/4`, `protection_issues=0`; Pump штатно disarmed.
- Первый canonical launcher завершился без listener; повторный поднял API `200` и
  восстановил ownership/protection с живым monitor и disarmed entries.
- Принят `ARM PUMP LIVE 3000`, policy `v3_3000_pool600`. Три независимых cycle
  сохранили armed/monitor alive, позиции `2/2`, защиты `4/4`, отсутствие
  blocked/error/transient/freeze и `protection_issues=0`.
- Минимальный observed liquidation buffer `34.59%`; Grid contract valid, активных
  Grid rules нет.

Следующий шаг: inventory оставшихся Grid mutations в `DataService`, затем один
минимальный parity-only блок без переноса exchange/manual I/O.

### 2026-08-13 — Grid transition worker-start parity reducer, block 59

- Inventory выбрал минимальный симметричный блок: result state после уже
  выполненного `manual_enter/manual_exit`.
- Добавлен dormant pure `reduce_transition_worker_start`.
- `execution_id` получает ownership pending transition и фиксирует fresh start
  hedged qty; error/empty result сохраняют transition и прежний conflict retry.
- Reducer очищает `transition_starting`, pending confirmation и возвращает event
  fragment без Manual I/O/save/history.
- Четыре golden cases покрывают enter/exit success, explicit error и busy fallback.
- Focused regression: `70 passed`, `28 subtests passed`; полный прогон:
  `728 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующий шаг: подключить reducer внутри прежнего lock после Manual submission;
submission, exception cleanup, `updated_at`, save и history оставить в service.

### 2026-08-13 — Grid transition worker-start integration, block 60

- Result прежнего `manual_enter/manual_exit` передаётся в
  `reduce_transition_worker_start` внутри существующего lock.
- Reducer обновляет pending transition, worker ownership либо conflict retry и
  возвращает event fragment.
- Manual submission, exception cleanup `transition_starting`, `updated_at`, save,
  `rule_id/ts` envelope и history остаются в `DataService`.
- Focused regression: `70 passed`, `28 subtests passed`; полный прогон:
  `728 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующая граница после deployment: pure reducer для pre-submit start failures и
no-order completion (`position_refresh_failed`, `transition_complete`, risk-limit
block). Сначала inventory и отдельные parity fixtures; exchange/risk I/O не
переносить.

### 2026-08-13 — Grid transition worker-start deployment, block 61

- Preflight: Manual/Grid `0`, valid contracts, Pump ownership/exchange `2/2`,
  protection `4/4`, `protection_issues=0`; Pump штатно disarmed.
- Первый canonical launcher завершился без listener; повторный поднял API `200` и
  восстановил позиции/защиту, monitor alive, entries disarmed до ARM.
- Принят `ARM PUMP LIVE 3000`, policy `v3_3000_pool600`. Три независимых cycle:
  armed/monitor alive, позиции `2/2`, защиты `4/4`, без
  blocked/error/transient/freeze и protection issues.
- Минимальный observed liquidation buffer `35.31%`; Grid contract valid, активных
  Grid rules нет.

Следующая безопасная граница: pure pre-submit no-order reducer для refresh failure,
already-complete и risk-limit block. Exchange refresh/risk preflight остаются I/O.

### 2026-08-13 — Grid transition pre-submit parity reducer, block 62

- Добавлен dormant pure `reduce_transition_pre_submit_outcome`.
- Четыре outcome: fail-closed position refresh, already-complete transition,
  risk-limit blocked и risk-limit ready clearing.
- Reducer сохраняет точные cooldown/fields и возвращает optional risk event; сам
  exchange refresh и risk preflight не выполняет.
- Четыре golden cases сравнивают весь mutated rule и event.
- Focused regression: `71 passed`, `28 subtests passed`; полный прогон:
  `729 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующий шаг: подключить reducer внутри прежних lock-блоков после соответствующего
I/O; `updated_at`, save и history envelope оставить в `DataService`.

### 2026-08-13 — Grid transition pre-submit integration, block 63

- Refresh failure, already-complete и risk-limit result state делегированы в
  `reduce_transition_pre_submit_outcome` после прежних I/O calls.
- Exchange refresh и KuCoin risk preflight не переносились.
- `DataService` сохраняет `updated_at`, save и risk history envelope. Pure preview
  сохраняет прежний audit event даже при исчезновении rule до reacquire lock.
- Focused regression: `71 passed`, `28 subtests passed`; полный прогон:
  `729 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующая граница после deployment: провести новый inventory Grid mutations и
зафиксировать архитектурный checkpoint; не начинать более крупный перенос без
явной узкой parity boundary.

### 2026-08-13 — Grid transition pre-submit deployment, block 64

- Preflight: Manual/Grid `0`, valid contracts, Pump ownership/exchange `2/2`,
  protection `4/4`, `protection_issues=0`; Pump штатно disarmed.
- Первый canonical launcher завершился без listener; повторный поднял API `200`.
  Cold restart кратко показал ожидаемый `recovery_monitoring`, сохранив позиции,
  protection, monitor alive и disarmed entries.
- Принят `ARM PUMP LIVE 3000`, policy `v3_3000_pool600`. Три независимых cycle
  сохранили armed/monitor alive, позиции `2/2`, защиты `4/4`, отсутствие
  blocked/error/transient/freeze и protection issues.
- Минимальный observed liquidation buffer `34.82%`; Grid contract valid, активных
  Grid rules нет.

### 2026-08-13 — Remaining Grid mutation inventory, block 65

После вынесения quote-cycle, transition planning/result, reconcile и полного
hedge-repair lifecycle в `DataService` остаются три осмысленные группы:

1. Transition admission state: live-rule conflict, global Manual worker conflict,
   disabled/generation recheck и transient `transition_starting` reservation.
2. Exchange/risk I/O orchestration: position refresh, KuCoin risk preflight и
   Manual enter/exit/cleanup; это намеренно остаётся I/O port.
3. Snapshot/service bookkeeping: live spread fields, timestamps, lock/save/history
   envelope; это не trading state machine и не требует механического переноса.

Следующая минимальная parity boundary — dormant transition-admission reducer для
conflict/paused/reservation outcomes. Generation recheck и actual Manual call не
переносить в одном блоке; I/O orchestration оставить в `DataService`.

### 2026-08-13 — Grid transition-admission parity reducer, block 66

- Добавлен dormant pure `reduce_transition_admission`.
- Outcomes: matching Live rule conflict, global Manual worker conflict, disabled
  pre-submit recheck, `transition_starting` reservation и exception release.
- Reducer не выполняет conflict lookup или Manual I/O; возвращает только admission
  result и мутирует persisted/in-memory rule state по прежней семантике.
- Пять golden cases сравнивают весь mutated rule.
- Focused regression: `72 passed`, `28 subtests passed`; полный прогон:
  `730 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующий шаг: подключить reducer к прежним admission points; lookup, generation
comparison, Manual call, timestamps и save оставить в `DataService`.

### 2026-08-13 — Grid transition-admission integration, block 67

- Пять прежних admission/reservation mutation points делегированы в
  `reduce_transition_admission`.
- Live conflict lookup, global Manual worker lookup, generation/enabled recheck и
  actual Manual submission остаются в `DataService`.
- Timestamp/save ownership не менялись; reducer не принимает внешних решений.
- Focused regression: `72 passed`, `28 subtests passed`; полный прогон:
  `730 passed`, `36 subtests passed`, одна известная `aiohttp` warning.

Следующая граница после deployment: проверить, что production Grid state-machine
extraction завершён на выбранной архитектурной границе, удалить только реально
неиспользуемые compatibility wrappers/imports и прогнать dependency inventory.

### 2026-08-13 — Grid transition-admission deployment, block 68

- Preflight: активных Manual/Grid исполнений `0`, Grid rules `0`, Pump ownership и
  exchange positions `2/2`, защитные ордера `4`, `protection_issues=0`, replay
  contract valid; Pump штатно disarmed перед остановкой backend.
- Canonical stop освободил `127.0.0.1:8000`; launcher поднял API, Caddy, Tailscale
  Funnel и Cloudflare Tunnel. После cold restart Pump восстановил обе позиции и
  четыре защиты, monitor alive, entries disarmed, `last_error` пуст.
- Принят `ARM PUMP LIVE 3000`. Три независимых цикла сохранили `armed`, monitor
  alive, позиции `2/2`, защиты `4`, `protection_issues=0`, без blocked/error.
- Наблюдаемый liquidation buffer: `34.44%`, `34.39%`, `34.03%`; активных Grid
  rules нет.

Следующая безопасная граница: dependency inventory после extraction. Удалять
только wrappers/imports без production и test call sites; I/O orchestration и
service bookkeeping не переносить механически в state machine.

### 2026-08-13 — Grid extraction dependency cleanup, block 69

- AST и repository-wide inventory подтвердили отсутствие call sites у шести
  compatibility wrappers: ownership key/share, transition tolerance, hedge
  tolerance, non-closeable dust и flat-repair reset.
- Удалены только эти wrappers и шесть импортов, существовавших исключительно
  ради них. Активные completion/dust/level helpers, missing-execution reducer,
  risk preflight, exchange refresh и Manual submission сохранены.
- Production extraction признан завершённым на выбранной границе: pure state
  transitions принадлежат `GridStateMachine`; внешний I/O, locks, timestamps,
  save/history envelopes и quote snapshot bookkeeping остаются в `DataService`.
- Проверки: Python compile; focused Grid/service `75 passed`, `28 subtests`;
  полный прогон `730 passed`, `36 subtests`, одна известная `aiohttp` warning.

Следующий независимый cleanup: удалить только dormant legacy scheduler handle,
poll interval, shutdown branch и coroutine, поскольку Grid уже запускается
единственным production `_automation_scheduler` под runtime module gate.

### 2026-08-13 — Dormant Grid scheduler cleanup, block 70

- Repository/AST inventory подтвердил: legacy `_auto_arb_scheduler`, его task
  handle и отдельный poll interval не имели start/call sites.
- Удалены dormant coroutine, неиспользуемые instance fields и недостижимая
  shutdown-ветка. Действующий `_automation_scheduler` и runtime gate
  `auto_arb_grid` не менялись; ручной refresh и `_auto_arb_cycle` сохранены.
- Проверки: Python compile; focused Grid/startup/dashboard `48 passed`; полный
  прогон `730 passed`, `36 subtests`, одна известная `aiohttp` warning.

Следующая безопасная граница после deployment: аудит оставшихся compatibility
aliases `GridStateRepository -> DataService` и разделение только там, где можно
сохранить тот же lock/save/history contract отдельным parity-тестом.

### 2026-08-13 — Grid dependency-cleanup deployment, block 71

- Перед restart: Manual/Grid `0`, Pump armed/monitor alive, ownership/exchange
  `2/2`, защиты `4`, valid replay contract, `protection_issues=0`; Pump штатно
  disarmed.
- Canonical restart восстановил API и внешние ingress, обе позиции и четыре
  защиты; entries оставались disarmed, `last_error` пуст.
- После `ARM PUMP LIVE 3000` три цикла: armed/monitor alive, позиции `2/2`,
  защиты `4`, `protection_issues=0`, без ошибок. Liquidation buffer наблюдался
  `33.41%`, `33.42%`, `33.39%`; Grid rules `0`.

Cleanup активирован. Следующий этап — read-only inventory aliases и repository
ownership; live state/order semantics на этом deployment не изменялись.

### 2026-08-13 — Grid repository alias audit, block 72

- `_auto_arb_store` имел только assignment и ни одного reader/call site; alias
  удалён. Реальный store остаётся единолично у `GridStateRepository`.
- `state`, `lock` и `history` aliases пока сохранены осознанно: production имеет
  десятки lock/history обращений, а service tests напрямую используют те же
  объекты как mutation/audit hooks. Их массовая замена не даст нового safety
  свойства и неоправданно расширит regression surface.
- `_save_auto_arb_config` сохранён как узкий facade над repository `save()`:
  он удерживает единый fail-closed persisted-state contract во всех mutation
  envelopes и остаётся полезной архитектурной границей.

Итог Grid-фазы: storage owner и pure state machine разделены; `DataService`
оставлен orchestration owner. Следующая новая фаза — Pump parity extraction,
начиная только с dormant pure reducer и snapshot/replay fixtures без live wiring.

### 2026-08-13 — Pump cold-restart parity reducer, block 73

- Добавлен dormant pure `PumpStateMachine.reduce_cold_restart`; он не читает
  биржу, не сохраняет файл, не запускает monitor и пока не подключён к controller.
- Reducer повторяет fail-closed restart mutation: ARM всегда сбрасывается;
  recovery counters обнуляются; при открытых tracked positions выбирается
  `recovery_monitoring`, при flat active state — `disarmed_after_restart`.
- Golden fixture покрывает open position, flat active и flat disabled; сравнение
  выполняется с независимой legacy-реализацией по полному mutated state и I/O
  intent (`save_state`, `start_recovery_monitor`).
- Focused Pump/replay regression: `128 passed`; полный прогон `731 passed`,
  `36 subtests`, одна известная `aiohttp` warning.

Следующий шаг: подключить reducer ровно в constructor после `_load_state()`, а
persist/start-monitor оставить прежними I/O-действиями controller. Затем провести
real cold-restart ownership/protection gate до повторного ARM.

### 2026-08-13 — Pump cold-restart reducer integration, block 74

- `PumpLiveController` создаёт `PumpStateMachine` и делегирует ему только pure
  mutation сразу после `_load_state()`.
- `_save_state_locked()` и `start_monitor()` остались в controller и выполняются
  по reducer intent; thread creation, ordering и `start_recovery_monitor` gate
  не менялись.
- Existing controller restart/recovery tests плюс golden parity прошли:
  focused Pump/replay/web isolation `129 passed`; полный прогон `731 passed`,
  `36 subtests`, одна известная `aiohttp` warning.

До live deployment обязателен прежний fail-closed сценарий: disarm, cold restart,
проверка replay contract, ownership `2/2`, protection `4/4`, затем явный ARM и
несколько monitor cycles.

### 2026-08-13 — Pump cold-restart reducer deployment, block 75

- Preflight: Manual/Grid `0`, Pump armed, ownership/exchange `2/2`, protection
  orders `4`, valid replay contract, `protection_issues=0`; Pump штатно disarmed.
- Canonical cold restart поднял API/Caddy/Tailscale/Cloudflare, восстановил обе
  позиции и четыре защиты, оставил entries disarmed; monitor alive, contract
  valid, ownership keys `2`, `last_error` пуст.
- После `ARM PUMP LIVE 3000` активна policy `v3_3000_pool600`. Три monitor cycle:
  armed, позиции `2/2`, защиты `4`, protection issues `0`, blocked/error пусты.
- Наблюдаемый liquidation buffer: `32.26%`, `32.16%`, `32.22%`.

Cold-restart reducer production-validated. Следующая минимальная Pump parity
граница — operator disarm state mutation; notifications/events/save остаются I/O
controller и переносятся только отдельным этапом после golden fixtures.

### 2026-08-13 — Pump disarm parity reducer, block 76

- Добавлен dormant pure `reduce_disarm` с явным `now_ms` и без I/O.
- Сохранена ключевая safety-семантика: новые входы выключаются всегда; monitor
  остаётся включён при tracked open positions и выключается только на flat;
  pending signals и portfolio freeze/recovery state очищаются.
- Golden fixtures сравнивают полный mutated state с legacy-реализацией для open
  и flat cases, включая custom reason и timestamp.
- Focused Pump regression `119 passed`; полный прогон `732 passed`, `36 subtests`,
  одна известная `aiohttp` warning.

Следующий шаг: вызвать reducer внутри прежнего `disarm()` lock, не меняя порядок
save, audit event, wake и status response.

### 2026-08-13 — Pump disarm reducer integration, block 77

- `PumpLiveController.disarm()` делегирует pure state mutation в
  `PumpStateMachine.reduce_disarm` внутри того же `RLock`.
- Persisted save, `disarmed` audit event, monitor wake и итоговый status response
  остались в прежнем порядке и в controller.
- Focused Pump/controller regression `120 passed`; полный прогон `732 passed`,
  `36 subtests`, одна известная `aiohttp` warning.

Следующий deployment одновременно проверяет обе новые Pump-границы: live disarm
должен сохранить monitoring двух позиций, а cold restart — восстановить ownership
и protection до нового ARM.

### 2026-08-13 — Pump disarm reducer deployment, block 78

- До restart: Manual/Grid `0`, Pump positions/exchange `2/2`, protection orders
  `4`, `protection_issues=0`, `last_error` пуст.
- Live disarm через reducer дал `status=monitoring`, `entry_armed=false`,
  `monitor_enabled=true`, monitor thread alive и open positions `2`: защита не
  была остановлена вместе с новыми входами.
- Cold restart восстановил ownership keys `2`, positions `2/2`, protection `4`,
  valid replay contract и оставил entries disarmed до подтверждения.
- После `ARM PUMP LIVE 3000` три цикла сохранили armed/monitor alive, позиции
  `2/2`, защиты `4`, protection issues `0`, без ошибок. Liquidation buffer:
  `32.75%`, `32.73%`, `32.91%`.

Обе первые Pump state-machine границы production-validated. Следующая безопасная
parity boundary — `stop_monitor` precondition/state outcome; она должна отдельно
доказать fail-closed запрет остановки monitor при любой owned open position.

### 2026-08-13 — Pump stop-monitor parity reducer, block 79

- Добавлен dormant pure `reduce_stop_monitor` без file/thread I/O.
- При любой tracked open position reducer выбрасывает прежний
  `pump_live_monitor_required_while_positions_open` до mutation; fixture
  подтверждает полную неизменность state.
- Flat outcome сбрасывает ARM/recovery state, ставит `status=stopped` и возвращает
  только intent остановить thread.
- Focused Pump regression `120 passed`; полный прогон `733 passed`, `36 subtests`,
  одна известная `aiohttp` warning.

Следующий шаг: подключить reducer внутри прежнего lock; persisted save, stop/wake
events и status response оставить в controller.

### 2026-08-13 — Pump stop-monitor reducer integration, block 80

- `PumpLiveController.stop_monitor()` делегирует precondition/state mutation
  reducer-у внутри прежнего `RLock`.
- Save, `_stop.set()`, `_wake.set()` и status response сохранены в controller и
  выполняются только после успешного flat outcome.
- Добавлены controller-level regressions: owned open position не меняет state и
  не выставляет stop event; flat controller сохраняется как stopped и будит loop.
- Focused Pump regression `123 passed`; полный прогон `735 passed`, `36 subtests`,
  одна известная `aiohttp` warning.

Следующая граница — inventory оставшихся lifecycle mutations. Приоритет отдавать
operator/emergency/recovery control state; ladder/margin/order execution не
переносить до отдельной модели exchange effects.
